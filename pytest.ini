[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# 覆盖率：统计 app 包（排除 connectors 的 bin/venv），最低 70%
addopts = --cov=app --cov-report=term-missing --cov-fail-under=70
