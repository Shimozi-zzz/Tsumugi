# Tsumugi 开发流程简报

> 本文件为开发过程同步记录，既是验收审阅材料，也是后续开发的重要参考。
> 约定：**每次修改文件的操作都必须在此追加一条变更记录**，写明改了什么、
> 关键点/坑位，方便回溯与复习。

---

## 一、项目速览（Quick Reference）

- 项目：Tsumugi 个人知识图书馆（Phase 1 = 本地 RAG 核心）
- 核心原则：检索/prompt 逻辑手写，不依赖 LangChain
- 技术栈：FastAPI + SQLAlchemy + SQLite + Chroma + bge-small-zh-v1.5 +
  DeepSeek API + React/Tailwind(v4) + pytest
- 后端默认端口 **8001**（8000 在本机被系统保留端口占用，见坑位 #1）
- 前端默认端口 **5173**（Vite dev，`/api` 自动代理到后端）
- 客户端：**Electron** 桌面壳（复用同一前端，自动管理后端，见下）
- 测试入口：`.venv\Scripts\python.exe -m pytest -q`（当前 **102 passed**，
  覆盖率 81.5%，门槛 70%；单测调试加 `--no-cov`，见坑位 #23）
- 启动入口：`start.bat`（纯 ASCII 编码，见坑位 #3）
- 前端主题：**4 套**（春樱 Sakura 默认 / 夏·空 / 夜紫书斋 / 默认），
  右上角下拉切换，localStorage 记忆（`tsumugi-theme`）

### 客户端（Electron）与 Web 同步机制

- 同一份前端代码、同一个后端（8001），**数据天然同步**：
  - Web 模式：浏览器访问 Vite(5173)，`api.js` 走 `/api` → Vite 代理 → 后端
  - 客户端模式：Electron preload 注入 `window.tsumugiBridge.apiBase =
    "http://127.0.0.1:8001"`，前端直接访问后端（CORS 允许）
- Electron 主进程（`frontend/electron/main.cjs`）自动管理后端：
  启动时若 8001 未监听则 spawn `.venv` 的 uvicorn；退出时 `taskkill /T /F`
  清理进程树；若端口已被 Web 端占用则复用（不重复起）。
- 加载逻辑：未打包时优先连 Vite(5173)（与浏览器体验一致），失败回退到
  `dist/index.html`（需先 `npm run build`）；打包模式（app.isPackaged）用
  resources/backend/tsumugi-backend.exe（PyInstaller 产物）。
- 环境变量 `TSUMUGI_SMOKE=1` 时启动 3 秒后自动退出，用于自动化冒烟测试。

### 目录结构（最新）

```
Tsumugi/
├── start.bat                 # 一键启动（ASCII 编码）
├── 开发流程简报.md            # 本文件
├── README.md                 # 项目展示（作品集用）
├── app/
│   ├── main.py               # FastAPI 入口（含静态文件挂载）
│   ├── config.py             # 配置（pydantic-settings + 环境注入）
│   ├── database.py           # SQLAlchemy 引擎/会话/轻量迁移
│   ├── models.py             # Item/Chunk/Tag/Source
│   ├── schemas.py            # Pydantic schema
│   ├── embeddings.py         # embedding 模型懒加载单例
│   ├── vectorstore.py        # Chroma 封装
│   ├── ingest.py             # 切分 + 入库 + 去重 + 删除
│   ├── retrieval.py          # 向量检索 + tag 过滤 + 排序去重
│   ├── rag.py                # prompt 拼接 + DeepSeek SSE 调用
│   ├── images.py             # 外部图片本地缓存
│   ├── connectors/           # Connector 插件层
│   │   ├── base.py           # 统一接口 + 声明式 Connector
│   │   ├── registry.py       # 注册/发现
│   │   ├── persistence.py    # 声明式配置持久化
│   │   └── bangumi/          # 内置 Bangumi 实现
│   └── api/routes.py         # API 路由（含文件上传、SSE 流式）
├── frontend/                 # Vite + React + Tailwind v4
│   ├── src/
│   │   ├── App.jsx           # 应用壳（数据加载 + 主题切换）
│   │   ├── components/DesktopView.jsx  # 三栏桌面视图
│   │   ├── api.js            # 后端 API 封装
│   │   ├── themes.js         # 主题系统
│   │   └── index.css         # 主题 CSS 变量
│   └── electron/main.cjs, preload.cjs   # 桌面客户端
├── tests/                    # 102 用例（ingest/retrieval/rag/routes/
│   │                         #   connectors/persistence）
├── scripts/
│   ├── dedup_history.py      # 历史重复清理工具
│   ├── backend.spec          # PyInstaller spec
│   └── build_dist.bat        # 一键打包脚本
├── docs/decisions/           # ADR-lite 决策记录（0001~0009）
├── Dockerfile / docker-compose.yml / nginx.conf
├── requirements.txt
├── pytest.ini
└── .env.example
```

---

## 二、环境与运行

### 启动步骤

```powershell
# 1. 配置
copy .env.example .env          # 然后编辑 .env，填 DEEPSEEK_API_KEY
# 2. 后端
.\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8001
# 3. 前端（另开终端）
cd frontend && npm run dev
# 或直接双击根目录 start.bat（自动开两个窗口 + 浏览器）
```

### 环境变量（详见 .env.example）

| 变量 | 必填 | 说明 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | 是 | 不填可启动，问答时返回友好错误 |
| `HF_ENDPOINT` | 默认 hf-mirror | HuggingFace 模型下载端点（国内镜像） |
| `CHUNK_SIZE/CHUNK_OVERLAP` | 默认 512/64 | 切分参数 |
| `TOP_K/MAX_CHUNKS_PER_ITEM` | 默认 5/1 | 检索参数 |
| `MAX_CONTEXT_LENGTH` | 默认 4000 | prompt 上下文预算 |

---

## 三、关键坑位与参考（重要，反复会用到）

### #1 端口 8000 被系统保留，默认改 8001
本机 `Get-NetTCPConnection -LocalPort 8000` 显示 OwningProcess=4（System 保留
端口），任何服务都绑不上 8000。故后端与 vite 代理统一用 8001。

### #2 Python 环境：3.14 装不了旧依赖，重建为 3.12
- 原 `.venv` 基于 Python 3.14：`pydantic-core 2.14.6`（pydantic 2.5.3 依赖）
  与 `chroma-hnswlib 0.7.x` 均无对应 wheel 且源码编译失败。
- 解决：`.venv` 用 Python 3.12 重建（`py` 启动器在本机因用户名非 ASCII 不可用，
  直接用 `$env:LOCALAPPDATA\Programs\Python\Python312\python.exe -m venv .venv`）。
- `chromadb 0.4.x→1.0.13`：1.x 用 Rust core，**不再依赖 chroma-hnswlib**，
  且兼容 pydantic v2。`httpx 0.26→0.28.1` 等一并升级，见 requirements.txt。
- 本机 PyPI/直连 HF 不通：pip 用清华镜像
  `-i https://pypi.tuna.tsinghua.edu.cn/simple`，HF 用 `hf-mirror.com`。

### #3 bat 编码：必须纯 ASCII，运行时 chcp 65001
- 系统默认 cmd 码页是 **936(GBK)**，bat 里写 UTF-8 中文会乱码/解析出错。
- `start.bat` 全部英文注释；开头 `chcp 65001 >nul` 切换控制台 UTF-8，
  让 uvicorn/vite 的中文日志正常显示。
- 若以后要在 bat 里加中文，须以 **ANSI(GBK)** 编码保存。

### #4 NLTK 安全钩子误判（venv 在项目目录内）
NLTK 3.10+ 的 `inisec` 会阻止"从 CWD 导入"的模块；但 `.venv` 位于项目目录内，
导致 site-packages 里 `regex` 等被误判而 `import sentence_transformers` 失败。
解法：`NLTK_DISABLE_IMPORT_SECURITY=1`（config.py setdefault + venv 内
sitecustomize.py 双保险）。

### #5 chromadb 1.0.x 的 `{"$in": [...]}` 校验有 bug
`validate_where` 对 `$in` 误报 "Expected where operator to be one of..."。
检索过滤改用 `{"$or": [{"item_id": i} for i in ids]}`（单 id 用等值），
等价且兼容（`app/retrieval.py::_build_item_where`）。

### #6 PowerShell 传参/编码的坑
- 写文件/JSON 用工具而非 `Out-File`，避免 BOM/编码问题（曾出现 UTF-8 BOM
  被存进文档，已在 ingest 加 `text.lstrip("\ufeff")` 兜底）。
- PowerShell 里 curl 内联 JSON 会吃掉引号 → 用 `--data-binary "@file.json"`。
- `$` 在双引号里会被 PowerShell 插值（如 `$or`）→ 用单引号或写临时脚本。
- `Start-Process npm` 不可用（npm 是 cmd 脚本）→ 用 `cmd.exe /c npm ...`。
- 一键起多命令时工具会报 `ChildProcess.kill` 噪音，忽略即可。

### #7 Chroma collection 命名规则
Chroma 1.x collection 名必须 3-512 字符 `[a-zA-Z0-9._-]`，单字符名（如 `t`）
报错。应用内用 `tsumugi` 没问题，测试用临时名需注意。

### #8 测试基建（可复用）
- `tests/conftest.py` 提供：内存 SQLite（**StaticPool** 保证 TestClient 线程
  可见）+ `FakeCollection`（numpy 余弦） + hash embedding 打桩。
- `retrieve_chunks` 测试里向量必须是**单位向量**，cosine 才能精确等于设定值。
- chroma/embedding 在测试中全 mock，测试秒跑（0.8s），不依赖模型下载。
- pytest.ini：`asyncio_mode = auto`、`pythonpath = .`。

### #9 npm 11 的 allow-scripts 会拦截 postinstall（Electron 二进制下载）
- 本机 npm 11 默认拦截未批准的 install scripts：`npm install electron` 装好
  包但**不会下载 electron.exe**（`node_modules/electron/dist` 缺失）。
- 解决：`$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"`
  后手动执行 `node node_modules/electron/install.js` 下载二进制。
- 验证：`node_modules\electron\dist\electron.exe` 存在 + `npx electron --version`。
- 另外注意 npm 输出的 `allow-scripts` 是警告不是错误，判断安装成败要看产物。

### #10 Electron 主进程用 CommonJS（.cjs）
- `frontend/package.json` 为 `"type": "module"`，若主进程写成 `.js` 会被当 ESM
  导致 `require` 报错。Electron 主进程/preload 统一用 **`.cjs`** 扩展名。

### #11 端口统一走 `TSUMUGI_PORT`（单点配置）
- 之前 8001 硬编码在 main.py / vite.config.js / electron/main.cjs / start.bat
  四处，易漏改。现统一：
  - `app/config.py` 新增 `tsumugi_port: int = 8001`（读环境变量 `TSUMUGI_PORT`）
  - `main.py`、`vite.config.js`、`electron/main.cjs`、`start.bat` 全部读取
    该环境变量
- 想换端口只需改 `start.bat` 里的 `set "TSUMUGI_PORT=xxxx"` 一处。

### #12 SQLAlchemy Session 线程安全（检索进线程池的前提）
- `retrieve_chunks` 原来使用路由 `get_db` 的请求级 session；若放进线程池，
  Session 跨线程共享不安全。现改为：函数内自行 `SessionLocal()` 并在 finally
  关闭（传 db 时仍用传入的），路由不再传 db。

### #13 路由同步阻塞 → run_in_threadpool
- `search/rag/query/rag/query/stream` 三个 async 路由里的 `retrieve_chunks`
  （embedding 推理 + Chroma 查询，秒级）会阻塞事件循环，改用
  `fastapi.concurrency.run_in_threadpool` 包装。
- 注意：入库（ingest）仍是同步直调（依赖请求级 session），本轮未动，
  记为待办——需要 threadpool 时需改为自建 session。

### #14 CORS 收紧
- 原 `allow_origins=["*"]` + `allow_credentials=True` 不规范。现改为白名单：
  `http://localhost:5173`、`http://127.0.0.1:5173`、`"null"`（Electron
  file:// 加载时 Origin 为 null）。
- 已实测：Origin `null` 与 `localhost:5173` 放行，任意第三方 Origin 无
  allow-origin 头（被拒）。

### #15 DeepSeek Key 改 SecretStr
- `config.py` 的 `deepseek_api_key` 改为 `pydantic.SecretStr`，避免 repr/日志
  泄露明文；`rag.py` 用 `.get_secret_value()` 读取。测试同步改为注入
  `SecretStr`。

### #16 测试环境的 SessionLocal 打桩（threadpool 改造的连带影响）
- 检索/入库内部自建 session（`from app.database import SessionLocal`），在
  线程池场景不再用路由传入的请求级 db。若测试不处理，会**误操作真实的
  `./tsumugi.db`**（测试内存库 vs 真库 schema 不一致报 "no such column"）。
- 解法：conftest 加 autouse fixture `_patch_session_local`，把
  `app.ingest.SessionLocal` / `app.retrieval.SessionLocal` 替换为测试工厂。
- 教训：改了"谁创建 session"的职责后，测试必须同步打桩模块内引用的工厂，
  不能只依赖路由的 get_db override。

### #17 FastAPI 裸 bool 参数默认是 Query 不是 Form
- 上传接口想用 multipart 传 `force` 字段，若写成 `force: bool = False`
  FastAPI 会把它当 **query 参数**，`-F force=true` 不生效。须显式
  `force: bool = Form(False)`。

### #18 `npm ci` 会重装 node_modules，electron 二进制又丢
- `npm ci` 严格按锁文件重装全部依赖，postinstall 被 allow-scripts 拦截后，
  electron 的 `dist/electron.exe` 会缺失。本地跑过 `npm ci` 后需重新执行
  `node node_modules/electron/install.js`（带 ELECTRON_MIRROR）。
- CI 不受影响：CI 的 electron 只当 devDependency 装包，不打包，
  已用 `ELECTRON_SKIP_BINARY_DOWNLOAD=1` 跳过下载。

### #19 SessionLocal patch 必须改模块属性（from ... import 取的是模块属性）
- conftest 之前只 patch `app.ingest.SessionLocal` / `app.retrieval.SessionLocal`，
  但 `routes._ingest_sync` 内部是 `from app.database import SessionLocal`（在
  函数体执行时读取模块属性），漏 patch 会**误操作真实 ./tsumugi.db**——
  实测测试把图片/文本写进了真实库并污染数据。
- 修复：统一 patch `app.database.SessionLocal`（模块属性），所有
  `from app.database import SessionLocal` 全局生效。
- 教训：测试污染真实库很难察觉，跑完 pytest 后应随手检查真实库条目数。

### #20 Session 关闭后再访问 Item 懒加载属性会报 DetachedInstanceError
- ingest 自建 session 在返回前 `db.close()` 后，路由再访问 `item.chunks`/
  `item.tags`（lazy relationship）会抛 DetachedInstanceError。
- 解法：在 session 打开时就把需要的字段读出来（`_ingest_sync` 内构造
  IngestResponse 再返回），线程池只回传纯数据对象。

### #21 FastAPI `Optional[List[str]]` query 参数须显式 `Query(None)`
- 不写 `Query()` 时，`List[str]` 参数被当作标量处理，`?tag=a` 绑定为
  `None`（实测最小复现确认）。必须 `tag: Optional[List[str]] = Query(None)`
  才能正确接收多次/列表参数。
- 影响：`GET /items` 的 tag 筛选曾因此失效（返回全部条目），已修复。

### #22 模块顶层 `from ... import` 会固化引用，patch 需按模块逐个来
- `from app.database import SessionLocal` 写在**模块顶层**时，`SessionLocal`
  会在 import 时被拷贝进 `app.retrieval`/`app.ingest` 的命名空间。此时只
  `monkeypatch.setattr(app.database, "SessionLocal", ...)` 对它们**无效**
  （顶层已绑定旧引用）。
- 必须同时 patch `app.retrieval.SessionLocal`、`app.ingest.SessionLocal`
  等所有在顶层 import 的模块（函数体内 `from ... import` 则每次执行时读取
  模块属性，patch 模块即可生效）。
- 触发场景：检索/入库自建 session + 线程池（`run_in_threadpool`）时暴露。
- 后续统一做法：**新代码避免模块顶层 import SessionLocal**，改函数体内
  import（如 routes._ingest_sync、connectors.persistence._session），
  测试只需 patch `app.database.SessionLocal` 一处。

### #23 单跑测试时覆盖率门槛误报
- pytest.ini 的 `--cov-fail-under=70` 会作用于**单测运行**：只跑某个测试
  文件时覆盖率只统计该模块相关代码，容易低于 70% 报 FAIL。
- 解决：单测调试用 `--no-cov`；全量回归跑 `pytest -q`。

### #24 本地文件路径 → 静态 URL 约定（file_pathToUrl）
- `Item.file_path` 存 `./data/uploads/xxx` / `./data/thumbnails/xxx`（相对
  路径），浏览器不能直接加载。后端挂载 `/static/uploads`、`/static/thumbnails`；
  前端 `filePathToUrl()` 把相对路径转成 `/static/...`（Electron 下转后端
  绝对 URL）。
- 改路径约定时两端需同步；vite dev 要代理 `/static`（已加）。

### #25 Tailwind v4（LightningCSS）会压缩自定义 CSS 颜色
- `rgba()` → 8 位 hex（如 `#f48fb124`）、hex 大小写归一（`#F09199`→`#f09199`）。
- 校验构建产物时用压缩后的格式匹配，否则会误判"主题缺失"。

---

## 三·五、已知简化 / 待办（汇总）

- **Electron 打包未完整验证**：PyInstaller 打包（onnxruntime/chromadb 数据
  收集 >15 分钟）在本机超时；electron-builder 配置就绪，待有资源/CI 验证
  （`scripts/build_dist.bat` 一键脚本已备）。
- **Docker 本机未验证**：无 Docker 环境；Dockerfile/compose 已写，CI 有
  docker job（main 分支构建后端镜像验证）。
- **历史重复数据未清理**：去重功能上线前的旧数据 content_hash 为 NULL，
  `scripts/dedup_history.py` 已备（dry-run 预览 + --apply 执行），等库大了
  手动跑。
- **桌面视图每页最多 200 条**：grid 走后端筛选 `limit:200`，个人库足够；
  超大数据量需加分页/加载更多。
- **前端无单元测试**：pytest 只覆盖后端；前端靠 build + 冒烟。可加 Vitest
  冒烟（作品集加分，成本中）。
- **声明式 Connector 仅 search**：无 get_detail/分页/认证握手（Phase 4 范围
  内够用）。
- **缩略图缓存策略**：仅"收藏入库"时缓存（ADR 0007），默认展示外部链接。

---

## 四、变更记录

### 2026-08-05 Phase 1 主体开发（历史补录）

- **requirements.txt**：依赖版本整体对齐（chromadb 1.0.13、fastapi 0.115.6、
  pydantic 2.10.4、httpx 0.28.1、pytest 8.3.4 等）。
  关键点：解决 Python 3.14 无 wheel 问题（见坑位 #2）。
- **app/config.py**：pydantic-settings 改造；embedding 模型/镜像/LLM 超时等
  配置项；启动时 setdefault `HF_ENDPOINT`、`NLTK_DISABLE_IMPORT_SECURITY`。
- **app/database.py**：改用 SQLAlchemy 2.0 `DeclarativeBase`（原
  `declarative_base()` 已弃用）。
- **app/models.py / schemas.py**：沿用骨架模型；schema 增加 ItemOut 带
  tags/chunks_count、IngestResponse、SearchResponse。
- **app/embeddings.py / vectorstore.py**（新增）：embedding 懒加载单例 +
  Chroma 封装（禁用遥测）。
- **app/ingest.py**：实现段落/标题感知切分 + 句子边界硬切 + overlap（512/64）；
  text 入库（embedding→Chroma→SQLite）失败回滚；图片仅存元数据；
  delete 级联清向量 + 清理孤立 tag；BOM 兜底。
- **app/retrieval.py**：tag 先 SQL 筛 ids 再作 Chroma where；内容去重 + 每
  item cap（默认 1）；批量回填 title/tags 避免 N+1。
- **app/rag.py**：手写 prompt 模板；httpx 直连 DeepSeek，手写 SSE 解析；
  超时(60s)/重试(非流式 1 次)/错误映射为 LLMError。
- **app/api/routes.py**：items CRUD + 上传（文本/图片分流）+ search +
  rag/query + rag/query/stream（SSE：sources→chunk*→done/error）。
- **app/main.py**：建表 + 数据目录创建；默认端口 8001（坑位 #1）。
- **frontend/**（新增）：Vite+React+Tailwind v4；App.jsx 导入/问答(流式)/
  资料列表；api.js 封装 SSE 解析；vite proxy `/api`→8001。
- **tests/**（新增）：49 用例，pytest 全绿（0.8s）。
- **docs/decisions/0001~0004**：切分策略 / 检索排序去重 / prompt 截断 /
  依赖环境 4 份 ADR-lite。

### 2026-08-05 start.bat（新增）

- 新增根目录 **start.bat** 一键启动（后端 8001 + 前端 5173 + 自动开浏览器）。
- 关键点：**纯 ASCII 编码**（已校验 0 非 ASCII 字节），规避 GBK 码页乱码；
  首行 `chcp 65001 >nul` 让运行时中文日志正常；`%~dp0` 定位目录；
  缺 `.env`/`.venv`/`node_modules` 时给出提示或自动安装。

### 2026-08-05 客户端 UI 模式（Electron，web 与客户端同步）

- **frontend/electron/main.cjs**（新增）：主进程。端口探测(8001)→未监听则
  spawn 后端→创建窗口→退出时 taskkill 清理后端进程树；加载逻辑"先连
  Vite(5173) 再回退 dist"；`TSUMUGI_SMOKE=1` 冒烟自动退出。
- **frontend/electron/preload.cjs**（新增）：`contextBridge` 注入
  `window.tsumugiBridge.apiBase = "http://127.0.0.1:8001"`。
- **frontend/src/api.js**（修改）：`API_BASE` 取 preload 注入值（客户端），
  否则回退 `/api`（Web 走 Vite 代理）→ 前后端同一后端，数据同步。
- **frontend/package.json**（修改）：`"main": "electron/main.cjs"`，
  新增 `"electron": "electron ."` 脚本，devDependencies 加 electron@43.3.0。
- **start.bat**（修改）：支持 `start.bat electron` 客户端模式（app 自动管
  理后端）；`start.bat` 不带参数仍为 Web 模式。
- 关键点/坑位：
  - electron 二进制 postinstall 被 npm allow-scripts 拦截，手动
    `node node_modules/electron/install.js` + npmmirror 镜像下载（坑位 #9）。
  - 主进程/preload 用 `.cjs` 避免 ESM 冲突（坑位 #10）。
  - 冒烟测试已实测：自动拉起后端→health 200→SMOKE_OK→退出后端口释放。
  - 回归：pytest 49 passed，`npm run build` 通过。

### 2026-08-05 AGENTS.md 协作模式增补

- **AGENTS.md**（修改）：协作模式新增原则 5「**允许先行反馈优化意见**」——
  Agent 在执行中发现问题可主动先行反馈（附理由与影响范围），由用户决策后
  实施；强相关小优化可直接做但需在变更记录说明。

### 2026-08-05 第一批问题修复（质量收尾）

背景：按上一轮优化建议清单，先执行明确存在的问题 1-5（README 等其他项
暂缓，等项目完成后再做）。

- **app/retrieval.py**（修改，坑位 #12）：`retrieve_chunks` 改为自建并关闭
  session（`own_session` 判断），支持在**线程池**中安全调用，不再依赖请求级
  session。
- **app/api/routes.py**（修改，坑位 #13）：`/search`、`/rag/query`、
  `/rag/query/stream` 的同步检索改用 `run_in_threadpool`，避免阻塞事件循环。
- **app/ingest.py**（修改）：① 入库失败回滚时**反向删除已写入 Chroma 的
  向量**（`chroma_ids` 记录），消灭孤儿向量；② `delete_item` 对 image 条目
  同时删除 uploads 附件文件（`_delete_upload_file`，仅限 upload_dir 内，
  越界静默跳过）。
- **端口单点化**（修改，坑位 #11）：`config.py` 新增 `tsumugi_port`（读
  `TSUMUGI_PORT`）；`main.py`、`frontend/vite.config.js`、
  `frontend/electron/main.cjs`、`start.bat` 统一读取该环境变量。
- **app/main.py**（修改，坑位 #14）：CORS 从 `*`+credentials 收紧为白名单
  `localhost:5173 / 127.0.0.1:5173 / null`（Electron file://）。
- **app/config.py + app/rag.py + tests/test_rag.py**（修改，坑位 #15）：
  `deepseek_api_key` 改 `pydantic.SecretStr`，防明文泄露；测试注入
  `SecretStr`。
- **tests/test_ingest.py**（新增 3 用例）：commit 失败后 Chroma 无残留；
  删除图片清文件；upload_dir 外文件不被删。
- 验证：pytest **52 passed**；Electron 冒烟（自动起后端→health 200→
  SMOKE_OK→退出后端口释放）通过；CORS 实测 null/5173 放行、第三方 Origin
  拒绝；`npm run build` 通过。
- 遗留待办：入库（ingest）仍在 async 路由直调同步阻塞逻辑，需自建 session
  后方可 threadpool，列入下批优化；README 待项目整体完成后补。

### 2026-08-05 重复内容去重（方案 A：静默跳过 + force）

用户确认方案 A。详见 docs/decisions/0005-content-dedup.md。

- **app/models.py**（修改）：`Item` 新增 `content_hash`（sha256，String(64)，
  可空，带索引；历史数据为 NULL 不参与判重）。
- **app/database.py**（修改，坑位 #16 相关）：新增 `ensure_schema()`——
  `create_all` 不给已有表加列，故对旧库执行 `ALTER TABLE items ADD COLUMN
  content_hash`；`main.py` 启动时调用。
- **app/ingest.py**（修改）：新增 hash 辅助（`compute_content_hash` /
  `compute_file_hash` / `compute_bytes_hash` / `find_existing_by_hash`）；
  `ingest_text_document` / `ingest_image` 默认按指纹判重（命中直接返回已有
  Item，不重复写向量），`force=True` 强制再导入。
- **app/schemas.py**（修改）：`ItemCreate.force`、`IngestResponse.duplicated`。
- **app/api/routes.py**（修改）：`create_item` / `upload_item` 接入判重；
  图片重复时提前返回不写文件；`upload_item` 的 `force` 显式用
  `Form(False)`（坑位 #17：裸 bool 会变成 query 参数）。
- **frontend/src/App.jsx**（修改）：上传结果若 `duplicated` 提示"内容已存在，
  跳过导入"。
- **tests/**（新增 6 用例）：重复文本跳过（id 一致、不重复写向量）、force
  再导入、图片按内容判重（路径不同也算重复）、hash 辅助函数、上传重复返回
  duplicated 标记、图片重复不写多余文件。
- 关键坑位：
  - **#16**：检索/入库自建 session 后，测试必须打桩模块内 `SessionLocal`，
    否则会操作真实 `./tsumugi.db`（报了 "no such column: items.content_hash"）。
  - **#17**：multipart 传 bool 字段须写 `Form(...)`，否则被当 query。
- 验证：pytest **59 passed**；真实后端实测（重复上传 `duplicated:true` 且
  item_id 一致 → force 新建条目 → multipart force 生效）；`npm run build`
  通过；测试数据已清理。

### 2026-08-05 CI 基建（GitHub Actions）

用户选择"先做 CI 基建"。

- **.github/workflows/ci.yml**（新增，YAML 已校验）：两个 job：
  - **backend**（ubuntu + Python 3.12）：`pip install -r requirements.txt` →
    `pytest -q`。测试全 mock，无需 .env/模型下载；设置
    `NLTK_DISABLE_IMPORT_SECURITY=1` 与本机行为一致。
  - **frontend**（ubuntu + Node 20，working-directory=frontend）：
    `npm ci` → `npm run build`。electron 仅作 devDependency 装包不打包，
    设 `ELECTRON_SKIP_BINARY_DOWNLOAD=1` 跳过二进制下载加速。
  - 触发条件：push 到 main + PR。
- 关键点/坑位：
  - CI 用 `npm ci`（需锁文件与 package.json 严格同步，本机验证通过）；
    但本地跑 `npm ci` 后 electron 二进制会丢，需重跑 install.js（坑位 #18）。
- 验证：pytest 59 passed；`npm run build` 通过；electron 冒烟 SMOKE_OK、
  退出后端口释放。

### 2026-08-05 入库阻塞优化（create_item / upload_item 移入线程池）

遗留待办收尾：ingest 在 async 路由直调同步阻塞逻辑（embedding + Chroma 写入，
秒级）会阻塞事件循环，与检索对齐改为线程池执行。

- **app/api/routes.py**（修改）：
  - 新增 `_ingest_sync()`：统一处理 note/image 的判重 + 入库（自建并关闭
    session），图片上传时负责保存附件文件；返回构造好的 IngestResponse
    （session 打开时已读取 chunks/tags，避免坑位 #20 的懒加载报错）。
  - `create_item` / `upload_item` 改为 `run_in_threadpool(_ingest_sync, ...)`，
    不再依赖请求级 db；文件读取仍为 async。
- 关键坑位：
  - **#19**：conftest 的 SessionLocal patch 漏了 `app.database`，导致
    `_ingest_sync` 写进真实库、污染测试数据（已清理真实库，仅留示例）。
  - **#20**：session 关闭后访问 lazy 属性报错，改为在 session 内构造响应。
- 验证：pytest 59 passed；真实后端实测 JSON 入库 + 重复跳过（duplicated:
  true）正常；测试不再污染真实库（跑完 pytest 后条目数不变）。

### 2026-08-05 Phase 2 标签系统（结构化筛选 + 标签管理）

详见 docs/decisions/0006-tag-system.md。

- **app/retrieval.py**（修改）：`_select_item_ids_by_tags` 支持 `match_all`
  （`GROUP BY + HAVING count(distinct)>=len(tags)`）；`retrieve_chunks` 新增
  `tag_match="any"|"all"` 参数。
- **app/schemas.py**（修改）：新增 `TagOut/TagRename/TagMerge/ItemFilter/
  ItemListResponse`；`QueryRequest` 加 `tag_match`。
- **app/api/routes.py**（修改）：
  - `GET /items` → 结构化筛选（`tag` 可重复、`tag_match`、`type`、`source`），
    返回 `{total, items}`；
  - 标签管理：`GET /tags`（含 count）、`PATCH /tags/{id}` 重命名、
    `DELETE /tags/{id}`、`POST /tags/merge`；
  - search/rag 路由透传 `tag_match`。
- **frontend/src/api.js**（修改）：`fetchItems(filters)` 支持筛选参数；
  新增 `fetchTags/renameTag/deleteTag/mergeTags`。
- **frontend/src/App.jsx**（修改）：列表适配 `{total, items}`；标签筛选条
  （pill 点选 + any/all 切换 + 类型筛选）；标签管理面板（重命名/删除/合并）。
- **tests/**（新增 10 用例）：检索交集 any/all、列表筛选（type/tag any/all/
  total 不计分页）、标签重命名/冲突409/删除保条目/合并。
- 关键坑位：
  - **#21**：FastAPI `Optional[List[str]]` query 参数必须显式 `Query(None)`，
    否则绑定为 None（标量处理）。路由里 list_items 的 tag 参数因此失效，
    排查时用最小 FastAPI 复现定位。
  - 跨线程 `from module import name` 能读到 monkeypatch 的属性（之前一次
    误判是调试脚本自身的遮挡问题）。
  - 上一轮调试期间真实库被写入测试残留（文档A/B/C），已清理，仅剩示例。
- 验证：pytest **71 passed**；真实后端实测 tag=RAG 筛选（total=2）、检索
  tag_match=all 交集正确、标签列表含 count；`npm run build` 通过；
  pytest 后真实库无新增污染。

### 2026-08-05 Phase 3 Connector 插件架构 + Bangumi

详见 docs/decisions/0007-connector-architecture.md。

- **app/models.py**（修改）：Item 补 `image_url/external_id/raw_metadata/
  synced_at`（JSON 列）；新增 `Source` 表。
- **app/database.py**（修改）：`ensure_schema()` 扩展为循环补多列。
- **app/connectors/base.py**（新增）：Connector Protocol + ConnectorManifest/
  SearchResult/ItemDetail dataclass + `load_manifest()`。
- **app/connectors/registry.py**（新增）：register/unregister/get_enabled/
  discover（扫描 manifest.json + connector.py，build_connector 工厂）。
- **app/connectors/bangumi/**（新增）：manifest.json + connector.py
  （search/get_detail/normalize + 令牌桶限流 + SQLite 缓存表）。
- **app/ingest.py**（修改）：新增 `ingest_external`（收藏入库，幂等）。
- **app/api/routes.py**（修改）：`/api/search/federated`（联合检索，按来源
  分组）、`/api/items/save-external`（收藏入库）、`/api/connectors`（数据源
  列表）；`_item_out` 增加外部字段。
- **app/main.py**（修改）：启动时 `connector_registry.discover()`。
- **tests/test_connectors.py**（新增 11 用例）+ routes 新增 6 用例。
- 关键坑位：
  - **#22**：模块顶层 `from app.database import SessionLocal` 会把引用拷进
    `app.retrieval`/`app.ingest` 命名空间，只 patch `app.database.SessionLocal`
    对它们无效；必须同时 patch `app.retrieval`、`app.ingest` 的属性。
    此前检索未真正写库所以没暴露，接入 federated 后暴露。
  - 真实 Bangumi API 本机超时（bgm.tv 不可达），错误处理正确返回 502
    "Bangumi API 请求失败：timed out"（预期行为）。
- 验证：pytest **88 passed**；真实后端实测 connector 注册成功、收藏入库
  （external_ref + source 筛选）、federated 错误处理正确；测试数据已清理。

### 2026-08-05 Phase 4 声明式自定义数据源

详见 docs/decisions/0008-declarative-connector.md。

- **app/connectors/base.py**（修改）：新增 `DeclarativeConnector`（HTTP 端点 +
  字段映射，`{query}`/`{env_var}` 占位符，点号嵌套取字段），不执行任意代码
  （AGENTS.md 插件安全边界）；`_dig/_safe_float/_as_list/_urlencode` 辅助。
- **app/connectors/registry.py**（修改）：新增 `register_declarative(config)`。
- **app/connectors/persistence.py**（新增）：声明式配置持久化到 sources 表
  （config_ref 存 JSON，密钥用环境变量占位符）+ 启动恢复 + 删除。
- **app/api/routes.py**（修改）：
  - `POST /api/connectors` 创建声明式数据源、`DELETE /api/connectors/{name}`；
  - **联合检索降级**：单个 Connector 失败改为记入响应 `errors` 字典
    （HTTP 200），不再整体 502 阻断其它源与本地结果。
- **app/main.py**（修改）：启动时从 sources 表恢复声明式数据源。
- **app/schemas.py**（修改）：`DeclarativeConnectorConfig`、`FederatedSearchResponse.errors`。
- **tests/**（新增 10 用例）：声明式搜索/字段映射/嵌套路径/API 错误/占位符
  头/校验、管理 API 增删查、federated 降级。
- 关键点：
  - federated 原实现单源抛错 → 整体 502（真实 Bangumi 超时暴露）；
    改为 errors 降级后，实测 mock-api 源正常返回 + bangumi 超时进 errors，
    本地与外部结果并存。
  - 声明式配置持久化验证：重启后端后 mock-api 自动从 sources 表恢复。
- 验证：pytest **98 passed**；真实链路（mock API + 声明式源 + 联合检索 +
  降级）全通；测试数据与 mock 服务已清理。

### 2026-08-05 Phase 5 UI 自定义 + Docker + CI 打磨

详见 docs/decisions/0009-ui-theme-docker.md。

- **frontend/src/index.css**（重写）：CSS 变量主题系统（默认/书斋/春/夏/秋/冬），
  `html[data-theme]` 切换；`tsm-*` 类映射变量。
- **frontend/src/themes.js**（新增）：THEMES 列表 + loadTheme/applyTheme
  （localStorage 持久化）。
- **frontend/src/layout.js**（新增）：仪表盘面板清单 + 顺序持久化。
- **frontend/src/components/Panel.jsx**（新增）：通用面板壳（收起/移除）。
- **frontend/src/App.jsx**（重写）：主题切换器 + 仪表盘面板（增删/重置布局）；
  错误分区（uploadError/askError）；外部条目 source 角标；硬编码色改
  `tsm-*` 变量类。
- **Dockerfile / docker-compose.yml / nginx.conf**（新增）：后端单 uvicorn
  进程 + 前端 nginx 静态服务（/api 代理 + SSE 关缓冲）+ 数据卷持久化。
- **pytest.ini / requirements.txt**（修改）：pytest-cov + 覆盖率门槛 70%
  （`--cov=app --cov-fail-under=70`）。
- **tests/test_persistence.py**（新增 4 用例）+ **坑位 #22 修复**：
  - persistence.py 顶层 `from app.database import SessionLocal` 改为函数体内
    `_session()` import，使测试 autouse patch 生效（与 routes._ingest_sync
    一致），避免了"测试连真实库"的坑。
  - 调试中发现单跑某测试时 `--cov-fail-under` 会误报（只统计单个模块），
    需 `--no-cov` 或全量跑。
- 验证：pytest **102 passed**（覆盖率 82.43% 达标）；Electron 冒烟 SMOKE_OK
  退出后端口释放；`npm run build` 通过；**Docker 未安装，本机未实测构建**
  （文件按标准编写，待有 Docker 环境验证）。

### 2026-08-05 坑位 #22 补充（persistence.py 顶层 import 修复）
- 见上：persistence.py 顶层 `from app.database import SessionLocal` 在测试
  patch 下不生效（模块顶层已固化引用）。改为函数体内 import 后，测试可复用
  autouse `_patch_session_local`，无需特殊 fixture。

### 2026-08-05 README + 两个小优化（项目收尾）

- **README.md**（新增）：项目定位、特性、架构图、RAG 原理速览（面试）、
  快速开始、测试说明、目录结构、技术栈、相关文档。
- **app/rag.py**（修改）：流式重试策略改进——仅当**尚未产出任何内容**时对
  网络错误重试（重发完整请求不产生重复内容）；一旦已 yield 过片段，中途失败
  直接抛错（前端已收到部分内容，无法安全重连）。HTTP 状态错误不重试。
- **app/ingest.py**（修改）：`ingest_external` 补写 `synced_at`（新建时
  `datetime.utcnow()`；幂等命中时刷新同步时间），前端可显示"同步于 xx"。
- 验证：pytest **102 passed**（覆盖率 82.32%）。

### 2026-08-05 优化清单全部执行（前端增强 + 工具脚本 + 打包配置）

按用户要求执行全部优化建议：

**前端（api.js / App.jsx / layout.js / Panel）**
- `federatedSearch` 接入提问面板：新增"包含外部源"开关；外部结果展示
  （来源角标 + 评分 + 简介 + **收藏入库**按钮）；单源降级提示（显示各源
  errors）；本地流式回答与外部检索并行展示。
- 数据源管理面板（第 5 个仪表盘面板）：列出/删除已注册数据源；表单创建
  声明式数据源（name/base_url/search_endpoint/field_map）。
- 资料列表分页：PAGE_SIZE=20，上一页/下一页（`/api/items` 的 skip/limit）。

**后端**
- `app/images.py`（新增）：`cache_external_image`——收藏入库时把外部封面图
  缓存到 `data/thumbnails`（md5 命名，>5MB 拒收，失败返回 None 不阻塞）；
  config 加 `thumbnails_dir`；`_save_external_sync` 缓存成功后写入
  `item.file_path`（与 image_url 区分本地/外部）。
- `synced_at`：ingest_external 新建时写入、幂等命中时刷新（实测生效）。

**工具脚本**
- `scripts/dedup_history.py`：历史重复数据清理（默认 dry-run 预览，--apply
  执行，--delete-images 连带删图片附件）。
- `scripts/build_dist.bat`：一键打包（PyInstaller 后端 + vite + electron-builder）。
- `scripts/backend.spec`：PyInstaller spec（collect_all chromadb/onnxruntime/
  sentence_transformers）。

**Electron 打包**
- `frontend/electron/main.cjs`：`resolveBackendCommand()`——打包模式
  （app.isPackaged）用 resources/backend/tsumugi-backend.exe，开发模式用
  .venv python。
- `frontend/package.json`：改名 `tsumugi`，加 electron-builder 配置
  （nsis 安装包 + extraResources 附带后端 exe）+ `dist` 脚本。
- **PyInstaller 打包本机超时未完成**（onnxruntime 数据收集 >15 分钟），
  配置已就绪，标注待有资源时跑通验证。

**CI / env**
- ci.yml 加 `docker` job（main 分支构建后端镜像验证 Dockerfile）。
- .env.example 补 `TSUMUGI_PORT`/`LLM_MAX_RETRIES`/`THUMBNAILS_DIR`。

**验证**：pytest 102 passed（覆盖率 81.82%）；前端 build 通过；Electron 冒烟
通过；真实后端实测收藏入库 + synced_at 刷新 + 缩略图降级正常；测试数据已清理。
**已知限制**：PyInstaller 打包与 Docker 本机构建均未完整验证（本机资源/环境），
配置就绪待 CI 或目标机器验证。

### 2026-08-05 Glass 玻璃拟态主题（新增第 7 主题）

按用户提供的 Glassmorphism 设计描述实施，采用"新增 Glass 主题"方案（保留
原有 6 主题可随时切换）。

- **frontend/src/index.css**（修改）：
  - 新增 `html[data-theme="glass"]`：柔和渐层背景
    （indigo `#a5b4fc` → pink/peach `#fbcfe8`/`#fdba74` → mint `#99f6e4`，
    135° 渐变 + `background-attachment: fixed` + 30s 缓慢漂移动画）；
    磨砂玻璃面板（`backdrop-filter: blur(18px) saturate(150%)` + 大圆角
    `--glass-radius: 20px` + 柔影）；
  - Glass 专属覆盖：`tsm-panel`（玻璃卡）、`tsm-input`（毛玻璃输入框）、
    `tsm-accent`（indigo→pink 渐变按钮）、`tsm-tag`（半透明胶囊）、
    **`tsm-source-badge`**（粉色 Bangumi 风格来源角标）；
  - 来源角标类对所有主题生效（默认 fallback 到 tsm-tag 样式）。
- **frontend/src/themes.js**（修改）：THEMES 加 `{ key: "glass", label: "Glass 玻璃" }`。
- **frontend/src/App.jsx**（修改）：顶栏改为 `position: sticky` + backdrop-blur
  （玻璃悬浮条）；外部检索结果与库内外部条目的来源角标改用
  `tsm-source-badge`（玻璃主题下粉色高亮）。
- 关键点：
  - Glass 是"面板级"玻璃拟态（背景渐变 + 卡片 blur），保留现有面板增删/
    主题切换/分页等全部功能，零功能回归；
  - `tsm-source-badge` 独立于 `tsm-tag`，玻璃主题给外部源粉色强调，其它
    主题沿用胶囊标签（不破坏原视觉）。
- 验证：`npm run build` 通过（CSS 产物确认含 glass-bg-shift / backdrop-filter /
  tsm-source-badge）；vite dev 页面 200 无 error。

### 2026-08-05 Desktop 桌面图书馆视图（三栏布局）

按用户第二条设计 prompt（三栏桌面布局：图标导航栏 + 可折叠侧栏 + 海报网格 +
悬浮查询栏）实施，采用"新增桌面视图"方案（仪表盘视图保留，右上角切换，localStorage
记忆）。

- **frontend/src/components/DesktopView.jsx**（新增）：三栏布局
  1. **最左图标导航栏**（60px 深色磨砂）：内联 SVG 图标（图书馆/问答/导入/设置），
     选中态 Bangumi 粉高亮；
  2. **可折叠侧栏**（220px↔40px）：分组（全部/笔记/图片/外部收藏，带计数）+
     标签树（按 count 排序），半透明面板；
  3. **中央海报网格**：`grid auto-fill minmax(150px,1fr)`，3:4 封面卡（优先
     image_url → file_path → 占位），大圆角 + 细光晕边框 + hover 上浮效果；
  4. **悬浮 RAG 查询栏**（右上）：磨砂玻璃输入条，Enter/按钮提问，外部源结果
     （粉色角标 + 收藏按钮）+ 本地流式回答 + 来源标签（柔和青）。
- **frontend/src/index.css**（修改）：新增 `html[data-theme="desktop"]`
  主题——深石板底 + 玻璃面板（blur 18px）+ Bangumi 粉 `#F09199` 强调 +
  柔和青 `#7dd3fc` 点缀；desk-card hover 上浮 + 粉光晕；桌面视图玻璃覆盖。
- **frontend/src/themes.js**（修改）：THEMES 加 `Desktop 桌面`。
- **frontend/src/App.jsx**（修改）：顶栏加"视图"下拉（仪表盘/桌面图书馆），
  view 状态存 localStorage；desktop 时渲染 DesktopView，dashboard 时原有面板。
- 关键点/坑位：
  - **Tailwind v4（LightningCSS）会把自定义 CSS 的 hex 颜色压缩成小写**
    （`#F09199`→`#f09199`）、`rgba()`→8 位 hex（`#ffffff59`），校验产物时
    大小写/格式要对得上，否则误判"缺失"；
  - `desk-nav`/`desk-askbar` 用 inline style（无对应 CSS 类），Tailwind 不生成
    是正常的，不影响渲染。
- 验证：`npm run build` 通过；产物确认 desktop 主题（`#f09199`/`#7dd3fc`/
  desk-card）就位；vite 页面 200 无 error；Electron 冒烟 SMOKE_OK、退出端口释放。

### 2026-08-05 移除仪表盘视图，仅保留三栏式设计（按用户要求）

用户确认三栏式结构，要求删除之前的仪表盘面板布局，仅保留三栏桌面视图；
同时按新 prompt 精化视觉（暗石板 + 自然玻璃拟态 + 环境光晕 + 1px 细边框）。

- **frontend/src/App.jsx**（重写 600→40 行）：瘦身为壳——数据加载
  （items/tags/refresh）+ 主题状态 + 顶栏（品牌「紬」+ 主题切换）+ 渲染
  DesktopView。删除仪表盘视图、面板状态、分页逻辑等全部旧结构。
- **frontend/src/components/DesktopView.jsx**（重写 351 行）：三栏布局精化：
  1. 超薄图标导航栏（52px，内联 SVG 纤细 1.4 描边）；
  2. 半透明侧栏（分组 + 标签列表，标签按 count 排序）；
  3. 中央工作区（海报网格 + 问答 + 导入，四区块导航切换）；
  - 新增导入区块（文件 + 标签表单）；问答含外部源结果 + 收藏 + 本地流式。
- **frontend/src/layout.js / components/Panel.jsx**（删除）：仪表盘专用。
- **frontend/src/themes.js**（修改）：默认主题改为 `desktop`；THEMES 里
  desktop 排最前。
- **frontend/src/index.css**（修改）：
  - desktop 主题精化：`--panel: rgba(30,34,48,0.42)`、边框
    `rgba(255,255,255,0.10)`（1px 半透明细边框）、`--accent:#F09199`、
    `--tag-text:#a5d8f7`（柔和青）；
  - **环境光晕** `.desk-ambient`：三层径向渐变（indigo `#6366f1` / rose
    `#f48fb1` / teal `#80cbc4`，低透明度）透过磨砂面板；
  - `.desk-card`：1px 细边框 + 柔和阴影（`0 1px 12px`，非重阴影）+ hover
    上浮 + 粉光晕；
  - `.desk-askbar/.desk-answer-card` 玻璃面板。
- 关键点/坑位：
  - Tailwind v4（LightningCSS）把 `rgba()` 压缩成 8 位 hex
    （`#f48fb124`），rgb 三元组校验不匹配是正常现象，看 hex；
  - `handleImport` 里 `uploadItem(file, null, tags)` 即可（api.js 内部已建
    FormData），动态 `import()` 复用现有函数。
- 验证：`npm run build` 通过；vite 200 无 error；产物确认 desk-ambient /
  desk-card / 光晕 hex 就位；Electron 冒烟 SMOKE_OK 退出端口释放。

### 2026-08-05 Desktop 主题精化（第三次设计迭代）

按新 prompt（深黑曜石 + violet/indigo 夜间画布 + 樱粉/琥珀点缀 + 20px 圆角）
在既有三栏视图上精化 desktop 主题。

- **frontend/src/index.css**（修改）：
  - 背景升级：`--bg: #0B0F19`（深黑曜石）；body 用
    `radial-gradient(violet) + linear-gradient(#070a12→#0b0f19→#101528)`；
  - 圆角 `--glass-radius: 20px`；
  - 新增 `--amber: #ffc24b`（琥珀点缀），标签用琥珀
    （`--tag-bg` amber 0.12 / `--tag-text: #ffd88a`），替换原柔和青；
  - 环境光晕升级为 violet/indigo/amber 四层径向渐变（`#8b5cf6`/
    `#6366f1`/`#ffc24b`/`#f09199` 低透明度）；
  - `.desk-card`：20px 圆角 + `inset 高光`（`inset 0 1px 0 rgba(255,255,255,0.06)`）
    + hover 发光边框（`0 0 0 1px rgba(240,145,153,0.22)`）+ 粉色光晕悬浮；
  - `.desk-askbar/.desk-answer-card` 同款 20px 圆角 + 高光。
- **frontend/src/components/DesktopView.jsx**（修改）：
  - 图标导航栏/侧栏背景改 `rgba(11,15,25,…)`（黑曜石玻璃）；
  - 侧栏加 `backdrop-filter: blur(14px)`（无缝过渡，无生硬灰块）；
  - 侧栏标签选中态用琥珀（`--tag-bg`/`--tag-text`），分组选中态保持樱粉。
- 配色对照 prompt：黑曜石 `#0B0F19`✓、樱粉 `#F09199`✓、琥珀 `#ffc24b`✓、
  violet `#8b5cf6`/indigo `#6366f1` 光晕✓、20px 圆角✓、1px 高亮边框✓。
- 验证：`npm run build` 通过（产物确认 obsidian/amber/violet/20px 就位）；
  vite 200 无 error。

### 2026-08-05 春樱主题（Sakura）——零生硬灰块（第四次设计迭代）

按新 prompt（统一玻璃拟态 + 零 harsh grey blocks + 春樱粉/暖白/薰衣草配色）
落地，**spring 升级为默认主题**，并把 DesktopView 里硬编码深灰全部改为
CSS 变量驱动。

- **frontend/src/index.css**（修改）：
  - `spring` 主题按用户参考代码重写：`--bg:#fff5f7`（极浅樱粉底）、
    `--panel: rgba(255,255,255,0.45)`（45% 通透白玻璃）、
    `--panel-border: rgba(244,114,182,0.20)`（樱粉细边框）、
    `--text:#4a044e`（深紫红）、`--text-secondary:#9d4edd`、`--accent:#ec4899`、
    `--card-text:#4a044e`；
  - 春樱弥散光斑背景（body 三层 radial-gradient：粉/紫/浅青 + 暖白渐变）；
  - `.desk-ambient` 春樱版光斑（粉紫透过玻璃）；
  - 卡片/查询栏统一 45% 白玻璃 + `inset` 高光 + 樱粉发光 hover；
  - **新增结构玻璃变量**：`--rail-bg`（导航栏/侧栏共用 45% 玻璃）、
    `--card-bg`、`--card-thumb`（粉紫渐变占位，非水泥灰）、`--card-text`，
    并在 `:root/default`、`desktop`、`spring` 都给出值。
- **frontend/src/components/DesktopView.jsx**（修改）：消除全部硬编码深灰
  （`rgba(11,15,25,…)` / `rgba(30,34,48,…)`）→ 改用 `var(--rail-bg)` /
  `var(--card-bg)` / `var(--card-thumb)` / `var(--card-text)`；卡片圆角 20px。
- **frontend/src/themes.js**（修改）：默认主题 `spring`（春樱 Sakura 排最前）。
- 关键点/坑位：
  - "零灰块"的关键是把三栏结构色（rail/card/thumb）抽成主题变量，否则切换
    春樱时导航/侧栏仍是深灰，与 prompt 矛盾；
  - Tailwind v4 仍会把 `rgba()` 压缩成 8 位 hex（校验用 hex）。
- 验证：`npm run build` 通过（产物确认 spring `#fff5f7`/`#f472b6`/`#4a044e`/
  rail-bg/card-thumb 就位）；vite 200 无 error。

### 2026-08-05 主题精简：仅保留 夏空 / 春樱 / 默认 三个主题

按用户要求删除其余主题（desktop / glass / shosai / autumn / winter）。

- **frontend/src/themes.js**（修改）：THEMES 仅剩
  `spring(春樱 Sakura)` / `summer(夏·空)` / `default(默认)`。
- **frontend/src/index.css**（重写，386→330 行）：只保留三个主题块——
  - `default`：原始 stone 系 + 结构玻璃变量（白玻璃回退）；
  - `spring`：春樱（45% 白玻璃 + 樱粉细边框 + 弥散光斑 + 粉紫占位渐变）；
  - `summer`：夏空（青空清爽，白玻璃 60% + 天蓝光斑 + 青蓝占位渐变 + 天蓝
    来源角标）；
  - 每个主题补齐结构玻璃变量（--rail-bg/--card-bg/--card-thumb/--card-text）；
  - 删除 shosai/autumn/winter/glass/desktop 全部规则与桌面视图专用覆盖。
- 验证：`npm run build` 通过——产物 CSS 仅含 `data-theme=default/spring/
  summer`（各主题计数 0 已确认）；vite 200 无 error。

### 2026-08-05 新增「夜紫书斋」（Violet Obsidian）暗色主题

按新 prompt 添加深色暗夜主题（第 4 个，可在右上角切换）。

- **frontend/src/themes.js**（修改）：THEMES 插入 `violet-obsidian(夜紫书斋)`。
- **frontend/src/index.css**（修改）：新增 `html[data-theme="violet-obsidian"]`——
  - `--bg:#090A0F`（极深黑曜石底）；`--panel: rgba(18,14,28,0.45)`（黑紫
    毛玻璃）；`--panel-border: rgba(168,85,247,0.25)`（紫色微光边框）；
    `--text:#f3e8ff`（浅紫白）；`--text-secondary:#a855f7`（荧光紫）；
    `--accent:#c084fc`（高亮紫）；
  - 结构玻璃：`--rail-bg: rgba(18,14,28,0.25)`（导航/侧栏 25% 黑紫玻璃）、
    `--card-thumb` 深紫渐变占位、`--card-text:#f3e8ff`；
  - body 三层紫色/靛蓝弥散光斑（`#9333ea`/`#4f46e5`/`#a855f7` 低透明度）；
  - 卡片 20px 圆角 + 紫色 hover 发光（`0 0 0 1px rgba(168,85,247,0.22)`）+
    紫色光晕悬浮；来源角标高亮紫。
- 配色对照 prompt：黑曜石 `#090A0F`✓、紫色光晕 `#9333ea`/`#4f46e5`✓、
  浅紫文字 `#F3E8FF`✓、荧光紫 `#a855f7`✓、25% 黑紫玻璃✓、1px 紫光边框✓。
- 验证：`npm run build` 通过（产物确认 `#090a0f`/`#9333ea`/`#f3e8ff`/
  `#a855f7` 就位）；vite 200 无 error。

### 2026-08-05 二次纠察优化（静态文件 + 后端筛选 + 导入/数据源管理补全）

按二次纠察建议执行（4 项修复 + 1 项清理）：

**修1 静态文件服务（图片缩略图/附件可显示）**
- `app/main.py`：挂载 `StaticFiles`——`/static/uploads`（upload_dir）、
  `/static/thumbnails`（thumbnails_dir）；
- `frontend/vite.config.js`：新增 `/static` 代理；
- `frontend/src/api.js`：新增 `filePathToUrl()`——把 `./data/uploads/xxx`/
  `./data/thumbnails/xxx` 转成 `/static/...` URL（Electron 下转后端绝对路径）；
- `DesktopView.cardCover` 改用 `filePathToUrl(it.file_path)`。
- 实测：上传图片 → `/static/uploads/<name>` 返回 200；vite 代理链路正确。

**修2 资料库筛选/计数改后端（>100 条不再失真）**
- DesktopView 的 grid 改为走后端 `fetchItems({tag, type, skip, limit:200})`，
  随 `activeTag/activeGroup/items` 变化重新拉取；header 计数用后端 `total`；
- 分组计数（全部/笔记/图片/外部）单独用 3 次 `fetchItems({type, limit:1})`
  的 total 拉取，保证准确（不再基于前端 100 条子集）。
- 代价：每页最多显示 200 条（个人库足够）；gridLoading 状态提示。

**修3 导入补 JSON 创建**
- `api.js` 新增 `createItem(payload)`（POST /items）；
- DesktopView 导入页新增"直接创建笔记"表单（标题/内容/标签），与文件上传
  并列。

**修4 数据源管理 UI（设置页）**
- DesktopView 设置页改为真实的数据源管理面板：列出已注册数据源（名称/
  能力/启用状态/删除）+ 表单创建声明式数据源（base_url/search_endpoint/
  field_map）——恢复重构时丢失的 Phase 4 功能入口。

**清理**
- 移除 `handleImport` 里冗余的动态 `import("../api.js")`（顶部已静态导入）。

**验证**：pytest 102 passed（覆盖率 81.53%）；前端 build 通过；静态文件
实测 200；Electron 冒烟 SMOKE_OK 退出端口释放；测试数据已清理。

### 2026-08-05 设置页新增背景图功能（自定义背景 + 透明度/模糊）

按用户要求在设置中添加"导入背景图"能力：自主添加背景图片、调节透明度、
模糊度，本地持久化。

- **frontend/src/App.jsx**（修改）：
  - 新增 `bg` 状态 `{ image, opacity, blur }`（localStorage `tsumugi-bg`
    持久化，默认 opacity=0.5 / blur=20）；
  - `updateBg(patch)` 更新并写回 localStorage；
  - 渲染全屏背景图层 `.bg-custom-layer`（`position:fixed; inset:0; z-index:0;
    pointer-events:none`）——`background-image` 用 base64、`opacity` 透明度、
    `filter: blur()` 模糊，盖在主题背景之上、内容之下；
  - 把 `bg` / `updateBg` 传给 DesktopView。
- **frontend/src/index.css**（修改）：`.app-root` 加 `position:relative`；
  新增 `.bg-custom-layer` 样式。
- **frontend/src/components/DesktopView.jsx**（修改）：
  - props 增加 `bg` / `updateBg`；
  - 设置页新增「背景设置」面板：选择/更换背景图（FileReader 读 base64，
    超 4MB 提示拒绝）、清除背景、透明度滑块（0.05-0.95）、模糊滑块（0-40px）；
  - 背景设置与数据源管理并列。
- 关键点/坑位：
  - 背景图存 **base64 到 localStorage**（跨 Web/Electron 同步，无需后端）；
    限制 4MB 防 localStorage 超限（约 5MB 上限）；
  - 图层 `z-index:0` + 内容在 header(z30)/侧栏/网格(z10) 之上，玻璃面板
    backdrop-blur 会透出背景图，实现"自定义背景透过毛玻璃"。
- 验证：`npm run build` 通过（产物确认 bg-custom-layer 就位）；vite 200
  无 error。

### 2026-08-05 Text & Vector Storage Inspector 分析面板

按用户 prompt 实现"数据库与文本分析检测器"面板，支持数据可视化。

- **app/stats.py**（新增）：统计聚合模块——
  - `Total Characters`：所有 note/external_ref 的 content 字符总数；
  - `Token Estimate`：粗略估算（chars/1.4，中文为主场景）；
  - `Vector DB Size`：Chroma 持久化目录磁盘占用（递归统计）；
  - `Chunk Count`：chunks 表总数；
  - `Source Distribution`：按来源分类（Markdown/TXT/PDF/Web Crawl/其他——
    外部收藏与 external_ref 归为 Web Crawl，扩展名 .md/.txt/.pdf 归类，其余
    note 默认 Markdown）；
  - `Top Files`：按字符数排序的前 10 大文本条目（含 title/chars/tokens）。
- **app/api/routes.py**（修改）：新增 `GET /api/stats`（run_in_threadpool
  执行，避免阻塞）。
- **frontend/src/components/InspectorPanel.jsx**（新增）：
  - Header：标题 + 日期徽标（当前日期）+ 刷新按钮；
  - 顶行 4 指标卡（字符数/Token/向量库大小/Chunk 数，大号数值 + 角标）；
  - 左下 **SVG 环形图**（来源分布，stroke-dasharray 实现，带图例）；
  - 右下 **横向条形图**（Top 10 大文件，渐变进度条 + 标题）；
  - 数值格式化（1w/1k/MB）、加载骨架、错误提示。
- **frontend/src/api.js**（修改）：新增 `fetchStats()`。
- **frontend/src/components/DesktopView.jsx**（修改）：导航栏加"分析"图标
  （折线图 SVG）+ `inspector` 区块渲染面板。
- 关键点/坑位：
  - 环形图用 SVG `stroke-dasharray` 手写实现（不引入图表库，符合项目手写
    透明理念）；分段用 `rotate` 定位；
  - 来源分类：`external_ref`/非 local 一律归 "Web Crawl"（对应需求面板的
    Web Crawl 项），扩展名识别 .md/.txt/.pdf。
- 顺手修复：`ingest.py` 的 `datetime.utcnow()` 弃用警告 → `datetime.now(
  timezone.utc)`。
- 验证：pytest **112 passed**（+10 stats 用例，覆盖率 81.68%）；stats API
  实测返回完整数据；vite 页面 + /api/stats 代理 200；Electron 冒烟 SMOKE_OK
  退出端口释放。

### 2026-08-05 布局重构：设置左下角 + 分类设置 + 侧栏搜索框 + 导入并入图书馆 + 导航自定义

按用户 6 点布局需求重构 DesktopView（三栏布局大改）：

1. **设置功能放到左下角**：导航栏 `settings` 固定底部（`mt-auto`），不再与
   其它按钮混排。
2. **设置内分类**：设置页顶部"分类 tab"（外观 / 导航栏 / 数据源），分类
   筛选在最上方；主题切换归类入「外观」。
3. **侧栏不变、中间放大**：侧栏 200→224px 微调；中央 `p-6` + 网格
   `minmax(160px,1fr)`（卡片更大），无 max-w 限制更宽。
4. **搜索栏=问答，圆角长框，一号位，默认打开**：
   - 侧栏顶部新增**圆角长搜索框**（rounded-full，放大镜图标 + 输入框 +
     提问按钮，Enter 提问、Esc 收起导入）；
   - 导航栏 `ask` 固定一号位；`section` 默认 `"ask"`（打开即问答）；
   - 问答区改为居中窄栏（max-w-3xl），空态引导文案。
5. **导入并入图书馆**：删除独立 `import` 导航项；图书馆侧栏**左下角**新增
   "导入"按钮（点击展开：上传文件 + 标签，可收起），Esc 关闭。
6. **导航栏自定义排序**：`navOrder` 状态存 localStorage（`tsumugi-nav-order`）；
   设置「导航栏」tab 用 ↑↓ 调整顺序（ask 固定首位不可移），settings 固定
   底部不参与排序。
- 主题状态提升：App 持有 `theme`/`setTheme`，从顶栏下拉移除，改由设置页
  「外观」分区切换（顶栏只剩品牌）。
- 关键点：导航拆成 `NAV` 映射 + `navOrder` 排序数组（ask 固定、settings 底部、
  中部可排序）；`loadNavOrder` 兼容旧 localStorage 缺失项。
- 验证：`npm run build` 通过；vite 200 无 error；Electron 冒烟 SMOKE_OK
  退出端口释放。

### 2026-08-05 布局微调：搜索框居中（浏览器风格）+ 导入图标展开浮层

按用户反馈修改两点，并审查整体美观：

**修改1 搜索框居中（参考 Edge/Chrome 地址栏）**
- 搜索框从侧栏移到**中央工作区顶部居中**（`flex justify-center` + `max-w-xl
  mx-auto`），所有区块（图书馆/问答/分析/设置）共享同一搜索条；
- 圆角长条（rounded-full）+ focus 高亮（边框变 accent 色 + `box-shadow 0 0
  0 3px accent-soft`，失焦还原），参考浏览器地址栏交互；
- 侧栏恢复为纯"分组 + 标签"内容，不再挤搜索框。

**修改2 导入改为"图标 → 点击展开浮层"**
- 侧栏左下角改为**小图标按钮**（上传图标 + 文字"导入"），点击弹出**居中
  浮层 modal**（半透明遮罩 + 毛玻璃卡片）；
- 浮层内含 tab 切换：`上传文件` / `创建笔记`；Esc / 点击遮罩 / ✕ 关闭；
- 移除了原来侧栏内联展开的导入表单块（设计不佳的形态）。

**关键点**：
- 搜索框 focus/blur 用 `e.currentTarget.parentElement.style` 动态改边框与
  光晕（无额外状态）；
- 浮层 `stopPropagation` 防止点击卡片内部关闭；Esc 用 `useEffect` 监听
  `keydown`（仅浮层打开时注册）。
- 验证：`npm run build` 通过；vite 200 无 error；Electron 冒烟 SMOKE_OK
  退出端口释放。

### 2026-08-05 流式调用失败排查 + 占位符识别

用户反馈"一直出现 Responses 流式调用失败"。诊断结果：**`.env` 的
DEEPSEEK_API_KEY 仍是占位符 `your_deepseek_api_key_here`**（start.bat 从
.example 复制而来），流式问答到 DeepSeek 请求时失败。

- **app/rag.py**（修改）：`_ensure_key` 把占位符也视为"未配置"，给出明确
  提示（避免去请求后拿 401）。之前只判空，占位符会被当成真实 key 发起请求。
- **tests/test_rag.py**（新增）：占位符 key 应被拒绝的用例。
- 实测：流式接口返回 `sources`（检索正常）+ `error`（提示填 key），消息清晰。
- 用户需在 `.env` 填入真实 `DEEPSEEK_API_KEY` 后重启后端。

### 2026-08-05 session 泄漏修复（判重提前 return 路径）

- **app/ingest.py**（修改）：`ingest_text_document` / `ingest_image` 在自建
  session（`own_session`）场景下，判重命中提前 `return existing` 时未关闭
  session，会造成连接泄漏。现统一用 `try/finally` 包裹，`own_session` 时
  finally 关闭。
- 验证：pytest 59 passed。
- 用户决定：**历史重复数据清理暂不执行（方案 C）**——当前向量库数据极少，
  等积累后再考虑 `scripts/dedup_history.py` 或 API 端点。已在简报标注为
  ADR 0005 已知限制。

### 2026-08-05 搜索历史 + 侧栏拖拽调宽 + 搜索框居中放大

按用户三点反馈迭代：

**① 搜索历史记录（搜索栏侧边）**
- `searchHistory` state（localStorage `tsumugi-search-history`，最多 10 条，
  去重置顶）；
- 搜索框内右侧新增**历史按钮**（时钟图标），点击展开下拉列表；点击历史项
  回填并直接搜索；"清空"按钮；
- 提问时自动记录历史；`handleAskFromValue(q)` 供历史项点击复用（避免依赖
  setState 时序）。

**② 侧栏可拖拽调宽（最大页面 1/3）**
- `sidebarWidth` state（默认 224）；侧栏右侧加 5px 拖拽手柄（`dragRef`）；
- `mousedown` 记录起点 → `mousemove` 实时调宽 → `mouseup` 清理；
  宽度钳制 `[160, floor(innerWidth/3)]`；拖动时 `cursor:col-resize` +
  `user-select:none`。

**③ 搜索框上下居中 + 更大**
- 搜索框从 `max-w-xl` 放大到 `max-w-2xl`、`py-3`、`text-base`；
- 容器 `items-start pt-8`（顶部偏上居中，视觉重心在中间）；
- 历史下拉 `max-w-sm` 右对齐。

**验证**：`npm run build` 通过；vite 200 无 error；Electron 冒烟 health 200、
退出端口释放。

### 2026-08-05 搜索历史/居中搜索框/文字涂鸦/长按调序（四项交互增强）

按用户四点需求迭代：

**① 搜索历史 + 丝滑动画**
- 侧栏的"搜索框在顶部…"提示文字**删除**，替换为**搜索历史列表**（时钟图标 +
  最多 8 条 + 清空）；
- 搜索框右侧历史下拉加 `history-dropdown` 动画
  （`scale(0.94) translateY(-6px) → scale(1)`，cubic-bezier 0.22s，origin
  右上），替代生硬的显隐。

**② 搜索框居中逻辑**
- 无问答内容时搜索框**上下居中**（`paddingTop:18vh`，`translateY(-2vh)`）；
  有内容时**移到顶端**（`paddingTop:2rem`）；`transition-all duration-500`
  平滑过渡；
- 删除了 ask 区块重复的空态提示文案（居中搜索框本身就是引导）。

**③ 文字涂鸦（自定义文字 + 蒙版）**
- App 新增 `textOverlays` state（localStorage `tsumugi-text-overlays`），
  DesktopView 渲染可拖动的 `.text-overlay` 层（绝对定位、可设位置/字号/颜色）；
- 设置「外观」新增**文字涂鸦**卡片：点「编辑文字涂鸦」→ 全屏**蒙版**
  （深色遮罩 + 居中操作提示：拖动调整位置、点 ✕ 删除、+ 添加文字）；
- 蒙版模式下文字可 mousedown 拖动实时改 `left/top`；编辑完成后点「完成」。

**④ 长按导航功能键调整位置（设置除外）**
- 长按（550ms）可排序导航按钮进入 `navRearrangeMode`：按钮显示虚线框 +
  grab 光标 + 顶部"拖动功能键调整顺序"提示条；
- 调整模式下用 HTML5 `dragstart/dragover/drop` 拖动排序（交换顺序并持久化）；
- 点击导航仍正常（调整模式外）；「完成」退出。

**关键点**：搜索框居中用 `hasContent`（answer/sources/fedResults 非空）驱动
条件样式；文字涂鸦拖动用 mousedown/mousemove/mouseup 监听 document；导航
长按用 `setTimeout 550ms` + mouseup/mouseleave 取消。
**验证**：`npm run build` 通过；vite 200 无 error；Electron 冒烟 SMOKE_OK
退出端口释放。

### 2026-08-05 搜索框分区块 + 涂鸦修复 + 设置保存按钮（五点调整）

**① 删除"问答"标题**：ask 区块顶部的"问答"文字移除（搜索框即引导）。

**② 搜索框按用途区分**
- 搜索框**仅出现在问答区块**（不再所有页面共享）；
- 问答区搜索 → **RAG 知识库问答**（检索 + DeepSeek 回答）；
- 图书馆区新增**本地查找框**（右上角）：按标题/内容过滤当前网格
  （`libQuery` 客户端过滤），空结果显示"没有匹配的存储内容"。

**③ 搜索框不重复出现**：从中央工作区顶部移除共享搜索条，只在 ask 区块内
渲染（含历史下拉）。

**④ 文字涂鸦修复**：蒙版（`z-40`）曾盖住文字层（`z-[5]`）导致无法拖动——
文字层编辑时提升到 `z-45`（高于蒙版），`mousedown` 加 `stopPropagation`，
拖动才有效；蒙版仍可点空白关闭。

**⑤ 设置页左下角固定"保存设置"**：settings 区块渲染固定左下角按钮
（`fixed bottom-6 left-6 z-40`），点击显示"设置已保存 ✓"绿色 toast
（1.5s 自动消失）。所有设置本就实时持久化 localStorage，按钮为确认反馈。

**验证**：`npm run build` 通过；vite 200 无 error；Electron 冒烟 SMOKE_OK
退出端口释放。

### 2026-08-05 分析页去历史 + 自定义封面 + 标签折叠 + 自定义分组

四点新需求：

**① 分析页不显示搜索历史**
- 搜索历史改为仅 `section === "ask"` 显示（之前是 `!== "library"` 导致
  inspector/settings 也有）；分析页侧栏只剩空白引导。

**② 资料文档支持换封面**
- 后端新增 `POST /items/{id}/cover`（multipart 图片 → 存 thumbnails 目录，
  更新 `file_path` + `image_url` 为 `/static/thumbnails/cover_{id}_...`）；
- 前端卡片 hover 右上角「换封面」按钮 → 隐藏 file input → `uploadItemCover`
  上传后 refresh；测试 3 例（成功/坏类型 400/不存在 404）。

**③ 标签常态折叠**
- 侧栏标签区默认折叠：标题行显示"N 个 ▸"汇总，点击展开/收起（`tagsOpen`
  state）；展开后显示标签列表，可再点标题收起。

**④ 分组自定义**
- 新增 `customGroups` state（localStorage `tsumugi-custom-groups`）：
  `{id, name, tags[]}`；侧栏分组区顶部「+ 新建」展开输入框，用当前激活标签
  作为分组筛选条件保存；
- 自定义分组显示为 ◈ 前缀条目，可点击选中（`customActive`），可 ✕ 删除；
- 网格 fetch 适配：`activeGroup === "custom"` 时用分组的 tags 作
  `tag_filter` + `tag_match=all` 后端筛选。

**验证**：pytest **115 passed**（+3 cover 用例，覆盖率 81.82%）；cover 端点
实测（image_url 更新 + /static 200）；build/vite/Electron 冒烟通过。

### 2026-08-05 自定义分组 UI 重做（去突兀感）

用户反馈分组功能"过于突兀"，重做为更自然的交互：

- **新建分组改浮层弹窗**：去掉侧栏内联输入框（生硬），改为居中 modal——
  名称输入 + **标签多选胶囊**（点选组合筛选条件）+ 创建按钮；打开时预选
  当前激活标签。
- **系统/自定义分组分区**：系统分组标题改"资料库"，自定义分组标题"我的分组"
  （带 + 新建）；有自定义分组时才显示该区。
- **去生硬符号**：自定义分组去掉 ◈ 前缀；删除按钮 ✕ 改为 **hover 才显示**
  （`opacity-0 group-hover:opacity-100`），不再常驻。
- **空态引导**：无自定义分组时显示一个低调的"新建分组"入口（+ 图标行），
  点击同样打开浮层。
- 点击系统分组会清掉 `customActive`，逻辑更干净。
- 验证：`npm run build` 通过；vite 200；Electron 冒烟 SMOKE_OK 退出端口释放。

### 2026-08-06 Review 读后感/书评系统（RAG 接入）

详见 docs/decisions/0010-review-rag.md。

**核心设计：Review 复用 Chunk 机制 + `Chunk.review_id` 区分来源**
- `Chunk` 加可空 `review_id` 列：NULL=条目自身内容，非空=某条 review；
- Review 创建时复用 split_text/embed_texts → 写 Chroma（metadata 带
  `item_id`+`review_id`）+ Chunk 行（id 形如 `review{id}_chunk{i}`）；
- 检索时读 metadata 区分来源：`source_type="review"/"item"` + `review_id` +
  `review_title`；rag prompt 标注"（读后感：标题）"；
- 编辑/删除按 `embedding_ref` 反向删除向量（复用 ingest 的孤儿向量防御
  模式）；创建失败也反向清理。

**文件改动**
- `app/models.py`：Review 表（item_id FK 级联 / title / content / rating 0-10 /
  status 枚举 / spoiler 0-1 / 时间戳）；Chunk.review_id；Item.reviews 关系。
- `app/database.py`：ensure_schema 给 chunks 加 review_id 列。
- `app/reviews.py`（新增）：create/update/delete/list/get_public_rating +
  `_session()`（函数体内 import，见坑位 #22 变体）。
- `app/retrieval.py`：metadata 读 review_id → source_type/review_id/review_title。
- `app/rag.py`：prompt 标注 review 来源。
- `app/schemas.py`：ReviewCreate/Update/Out；RetrievedChunk 加来源字段。
- `app/api/routes.py`：review CRUD（run_in_threadpool + `_review_out(db)` 显式
  加载 item 避免 DetachedInstanceError）。
- `frontend/src/api.js`：fetchItemReviews/createReview/updateReview/deleteReview。
- `frontend/src/components/ReviewPanel.jsx`（新增）：浮层——列表 + 新建/编辑
  表单（内容/评分/状态/spoiler）+ spoiler 默认折叠点击展开 + 大众评分对比。
- `frontend/src/components/DesktopView.jsx`：卡片加"书评"按钮（hover 显示）。

**关键坑位（本轮命中）**
- **#22 变体**：`reviews.py` 顶层 `from app.database import SessionLocal` 绑定
  旧引用，测试 patch 不生效导致 "no such table: reviews"。已改 `_session()`
  函数体内 import。
- **#20**：review 经 run_in_threadpool 返回后 detached，`_review_out` 改为
  `db.get(Item, ...)` 显式加载，避免 DetachedInstanceError。
- **编码事故**：PowerShell `Set-Content -Encoding utf8` 把 reviews.py 中文
  注释损坏（GBK→UTF-8 错乱），整个文件重写恢复——教训：改代码用编辑工具，
  不用 Shell 重写含中文的文件。

**验证**
- pytest **139 passed**（+16 review 模块用例 + 8 routes 用例，覆盖率 82.40%）；
- 真实后端实测：创建 review → 检索命中（source_type:"review"/review_id/
  review_title 正确）→ 编辑后旧内容不命中、新内容命中 → 删除后向量清理；
- build/vite 200/Electron 冒烟 SMOKE_OK 退出端口释放。
- 已知限制：spoiler 内容仍参与检索（不过滤），仅前端折叠；status 为字符串
  校验非 DB enum；评分未做聚合。

### 2026-08-06 已知空档收尾（Docker/Electron/SSRF/Vitest/dedup）

清理"三·五、已知简化/待办"的遗留项，逐项实测验证：

**任务1 Docker 构建 — 成功（本机新装 Docker 实测）**
- 本机原先无 Docker，经 winget 安装 Docker Desktop（需配置 registry-mirrors
  docker.1ms.run/daocloud/dockerproxy 才能拉取镜像）；
- `docker-compose build` 后端镜像成功（首次 pip 装依赖 ~105s）；
- `docker compose up` 两容器 Up；后端 /health 200、前端页面 200；
- nginx 代理 `/api`、`/static`、`/api/stats` 均 200；
- **SSE 流式验证**：容器内挂载本机 HF 模型缓存后 embedding 可用；经 nginx
  (5173) 的 SSE 流式逐事件返回（sources → error 独立事件，未缓冲合并）；
  容器已加载含 `proxy_buffering off` + `Connection ''` 的配置（nginx 官方
  SSE 要求逐条核对并补齐 `proxy_set_header Connection ''` +
  `proxy_send_timeout`）；
- 结论：配置理论正确 + 容器实测通过。
- 变更：docker-compose.yml 挂载 `./data`（已有）+ `${HF_HOME_CACHE}`
  HF 缓存（.env 新增该变量）；nginx.conf 补 Connection ''。

**任务2 Electron 打包 — 部分成功（exe 可启动，embedding 未解决，按止损点停止）**
- 已解决 6 个打包问题：
  1. UPX 关闭加速（onnxruntime 被 UPX 破坏风险）；
  2. NLTK 打包版钩子（runtime hook `pyi_rth_nltk_fix.py`）；
  3. connectors 目录动态发现 → 显式 datas 收集（manifest.json 一起）；
  4. **numpy 2.x "cannot load module more than once" → 降级 numpy
     1.26.4/scipy 1.11.4**（requirements 已锁）；
  5. `.env` 被 PowerShell Add-Content 写坏 → 重写为 UTF-8；
  6. transformers 精确收集（实测清单 47 个子模块替代全量 4664 entries）。
- 当前 `tsumugi-backend.exe` **可独立启动 health 200 + connectors 发现
  正常**；
- **最终卡点（止损）**：transformers `_LazyModule` 需要文件系统真实
  `models/__init__.py`，PyInstaller 的 `module_collection_mode='pyz+py'`
  未覆盖子包，尝试 3 种收集方案（全量/collect_all/精确）均报同一
  `models/__init__.pyc 找不到`，embedding 不可用。按止损点约定停止，
  不再重打包（详见 ADR 0012）。
- 本次精确收集版打包实测 **~23 分钟**（entries 4664→2418，reclassification
  仍占大头，耗时改善不显著）。
- 坑位 #26（新增）：PyInstaller 每次全量重建 ~18-23 分钟，迭代成本高；
  坑位 #28（新增）：transformers `_LazyModule` 与 PyInstaller PYZ 归档
  的兼容问题（详情 ADR 0012）。

**任务3 SSRF 防护 — 完成**
- `app/connectors/ssrf.py`：协议白名单 + 私网/回环/链路本地/CGNAT/组播 IP
  拒绝 + localhost 拒绝 + 域名解析后校验（DNS rebinding 缓解）；
- 接入 `DeclarativeConnector._validate`（创建时 resolve=False）+ `search`
  （请求前 resolve=True）；ADR 0011 已写（如实标注局限：rebinding 不彻底、
  重定向未复查、单层防护）；
- 测试 `tests/test_ssrf.py` 17 用例（内网拒绝/公网放行/DNS 解析到内网拒绝）；
  因新增校验需 mock DNS，`test_connectors.py` 的声明式测试补 autouse fixture。
- pytest 156 passed（覆盖率 82.61%）。

**任务4 Vitest 冒烟 — 完成**
- 引入 vitest + jsdom + @testing-library/react；`npm run test` = `vitest run`；
- 9 个用例（themes.test / search-history.test / desktop-view.test.jsx）：
  主题切换改 data-theme、搜索历史 localStorage 去重置顶、DesktopView 渲染；
- CI frontend job 加 `npm run test` 步；
- 坑位 #27（新增）：vitest 测试含 JSX 的文件必须用 `.jsx` 扩展名
  （`.js` 会解析失败）。

**任务5 历史重复清理 dry-run — 完成（无需 apply）**
- `scripts/dedup_history.py` dry-run 输出：无重复；
- 核实库中 2 条：#1 RAG入门笔记 hash=NULL（历史数据未算），#2 测试文档
  hash=237d5006。无重复 → **不执行 --apply**。

### 2026-08-06 Electron 打包收尾：onedir 确认 + transformers 精确收集（止损）

按任务调整打包方式（详见 ADR 0012）：

- **onedir 确认**：当前 spec 本就是 onedir
  （`EXE(exclude_binaries=True) + COLLECT`），无需改模式；`main.cjs`
  `resolveBackendCommand` 引用 `resources/backend/tsumugi-backend.exe`
  + cwd=backend，electron-builder `extraResources` 把整个
  `dist/tsumugi-backend/`（含 `_internal/`）复制到 backend/，**路径链路
  正确，未改动**。
- **精确收集替代全量**：实测捕获 bge-small-zh-v1.5 实际触发的
  transformers 子模块清单（`transformers.models.{bert,auto,align,bark,
  encoder_decoder,mt5,t5}`，共 47 个子模块），把
  `collect_submodules("transformers")` 全量（4664 entries）换成精确清单；
  保留 `models/__init__.py` 复制为数据文件。
- **实测打包耗时对比**：
  - 全量 4664 entries：~20-24 分钟；
  - 精确 2418 entries：**~23 分钟**（reclassification 仍占大头，耗时改善
    不显著——entries 减半但该阶段瓶颈在二进制解析）。
- **最终验证（止损前）**：exe 独立启动 health 200 + connectors 发现正常；
  但 **embedding 检索仍报 `models/__init__.pyc 找不到`**（transformers
  `_LazyModule` 需文件系统真实 `__init__.py`，PyInstaller
  `module_collection_mode='pyz+py'` 未覆盖子包）。尝试 3 种收集方案均同一
  错误 → **按止损点停止**，不再重打包。
- **已知限制**：当前打包版 embedding 不可用（需要 embedding 的完整功能
  不可用）；后续建议见 ADR 0012（更新 hooks/降级 transformers/换打包器）。
- 坑位 #28（新增）：transformers `_LazyModule` 与 PyInstaller PYZ 归档
  兼容问题（embedding lazy import 失败）。
- 回归：pytest 156 passed、vitest 9 passed 均无回归。

### 2026-08-06 LLM Provider 可插拔化（AI 问答改为可选插件）

详见 docs/decisions/0013-llm-provider-architecture.md。核心：把"AI 生成
回答"从硬编码 DeepSeek 变成可开关/可自选后端的插件，其余功能（导入/检索/
标签/书评/联合搜索）不受影响。

**后端**
- `app/providers.py`（新增）：`LLMProvider` Protocol（`build_request`
  返回 url/headers/body）+ `OpenAICompatibleProvider`（DeepSeek 预设）+
  `OllamaProvider`（默认 localhost:11434/v1，无需 key）+ `provider_from_config`
  （环境变量占位符解析）+ `test_connection`。统一走 `chat.completions`
  （不用 Responses API，吸取之前"Responses 流式失败"教训）。
- `app/models.py`：新增 `LLMProviderConfig` 表（api_key_ref 占位符不落明文，
  enabled 单启用）。
- `app/provider_store.py`（新增）：复用 Phase 4 Connector 持久化模式
  （结构化列 + `_session()` 函数体内 import）。
- `app/rag.py`（修改）：`_active_provider()` 从启用的 Provider 构造请求；
  保留 `_stream_chat/_chat` 的流式解析/超时/重试/错误映射不动；无 Provider
  时抛 `AIAnswerDisabled`（LLMError 子类）。
- `app/api/routes.py`：provider CRUD + 测试连接 + ollama-status；rag 路由
  对 `AIAnswerDisabled` 返回引导提示而非 500（非流式 200+文案，流式 SSE
  error 事件）。

**前端**
- `components/ProviderSettings.jsx`（新增）+ 设置页"模型"tab：预设选择/
  表单/测试连接/Ollama 引导（检测 + 下载链接 + `ollama pull qwen2.5:3b`
  一键复制）/已保存配置（启用/停用/编辑/删除）。

**测试**：`tests/test_providers.py` 13 用例（请求构造走 chat.completions、
Ollama 无 key、占位符解析、test_connection mock、store CRUD + 单启用、
rag 降级）。

**实测验证（本机已有 Ollama + qwen2.5:7b）**
- 创建并启用 Ollama provider → ollama-status 检测到服务与模型 → 测试连接
  成功 → **流式问答逐块输出完整中文回答**（sources→chunk*→done）；
- 降级：删除 provider 后 `/api/rag/query` 返回"⚠️ AI 问答未启用…"，检索/
  标签/items 仍 200 正常；
- pytest **168 passed**（覆盖率 81.35%）、vitest 9 passed、Electron 冒烟
  通过。

**坑位/决策**
- 坑位 #29（新增）：Provider 的 base_url 不做 SSRF 拦截——Ollama 的
  localhost 是用户显式选择的本地服务，与 Connector"任意 URL 代请求"风险
  不同（ADR 0011 边界）。

### 2026-08-06 LLM Provider base_url SSRF 边界澄清与加固

**如实说明当初遗漏原因**：ADR 0013 的 Provider 最初**接入了** SSRF 检查，
但实测发现它把 Ollama 默认 localhost:11434 也拦掉（ssrf.py 拒 localhost），
导致 Ollama 无法测试连接。当时为了快速跑通 Ollama，直接删掉了整段 SSRF——
把"Ollama 需要 localhost 例外"误处理成"Provider 整体不做 SSRF"，是**过度
放宽**。Connector 与 Provider 本质同风险（用户填 URL → 后端发请求），不该
区别对待。详见 ADR 0014。

**加固**：
- `app/connectors/ssrf.py`：`check_ssrf_target` 加 `allow_loopback` 参数
  （默认 False，不影响 Connector）；放行**仅回环**，其它私网/链路本地仍拦。
- `app/providers.py`：两个 Provider 类各加 `validate_target(url, resolve)`
  ——Ollama 传 allow_loopback=True（放行 localhost/127.0.0.1，拦 10.x/169.254），
  通用 Provider 传 False（完整校验）；`provider_from_config(config,
  validate=True)` 做配置期轻校验（resolve=False）。
- `app/api/routes.py`：`save_llm_provider` 保存前校验，违规返回 400。
- 校验时机：保存时 resolve=False + 请求前 resolve=True（含 DNS rebinding）+
  测试连接不绕过。

**测试**：`tests/test_providers.py` 新增 9 用例（Ollama localhost 放行 /
Ollama 拦私网 / 通用拦 localhost+私网 / 公网放行 / provider_from_config
校验 / DNS rebinding / Ollama build_request）。

**实测**：
- Ollama localhost 配置保存 + 测试连接成功（未被误伤）；
- 内网通用 Provider（192.168.1.10 / 169.254.169.254 云元数据）保存被拒
  （HTTP 400）；
- 公网 deepseek 配置保存成功。
- pytest **177 passed**（+9 用例，覆盖率 81.37%）。
- api_key 落地流程补全说明：表单填占位符 → api_key_ref 列 → 运行时
  `_resolve_env_placeholder` 解析环境变量 → 仅内存使用，不落明文。

### 2026-08-06 api_key 明文落库漏洞修复（最小拦截）+ 体验缺口记录

用户在评审中指出两点，均已处理：

**① 安全隐患（当前行为有漏洞 → 已修复，非待办）**
- 问题：`_resolve_env_placeholder` 对非 `{...}` 格式的值"原样返回"，导致
  用户在设置页误填明文 key（如 sk-abcdef）会被**原样存进
  llm_providers.api_key_ref 列**——绕过"不落明文"约定（坑位 #15 初衷），
  且无告警。
- 修复（最小拦截，不动存储架构）：
  - `app/providers.py` 新增 `validate_api_key_ref(ref)`：非占位符（不以
    `{` 开头、`}` 结尾，或只有单边括号）直接抛 ProviderError，提示
    "请填写 {DEEPSEEK_API_KEY} 占位符格式，勿填明文 Key"；
  - `app/api/routes.py` 的 `save_llm_provider` 与 `test_llm_connection`
    临时配置分支均调用该校验，违规返回 400；
  - 空值（无需 key 的 Ollama）通过。
- 测试：`tests/test_providers.py` 新增 4 用例（占位符通过/空通过/明文拒/
  畸形拒）。
- 实测：明文 key 保存返回 400 + 明确提示；占位符正常保存。
- pytest **181 passed**（+4 用例，覆盖率 81.11%）。

**② 体验缺口（记入待办，不紧急）**
- 设置页 UI 填 api_key 不能真正"存"一个新 key：用户仍须先在 .env 手动配
  好对应环境变量，UI 只是"指向"它。大部分用户会以为"填完点保存就生效"。
- 记入下一批小优化：允许用户在 UI 直接填真实 key，后端接收后自行写入
  .env（或加密存储），实现"设置页自由配置"的完整直觉。
- 同时保留占位符方式（两者并存）。

### 2026-08-06 Connector 出站代理配置 + 萌娘百科 Connector

详见 docs/decisions/0015-connector-proxy-moegirl.md。

**① 出站代理配置（系统性方案）**
- `app/connectors/base.py`：新增共享 `http_get(url, proxy=...)`（httpx 原生
  proxy 参数，None=直连）、`TokenBucket`/`RequestCache`（从 bangumi 提升为
  共享，moegirl 复用）、`validate_proxy_url()`（SSRF 全量校验，**不放行
  回环**——判断：代理是"转发出站流量"的中转，没有 Ollama 那种本机合法
  服务理由）；Connector Protocol 加可选 `proxy_url` 属性。
- `app/connectors/registry.py`：`apply_settings(settings)`（启动时一次性
  注入所有实例的 proxy）、`set_proxy/get_proxy`。
- `app/connectors/persistence.py`：单行 `connector_settings`/type=settings
  存 `{name: {"proxy_url": ...}}`——因为 `Source.name` 是唯一键，声明式
  数据源已占各自 name 行，单行全量 dict 天然避冲突。
- `app/main.py`：启动时 `apply_settings(get_connector_settings())`。
- `app/api/routes.py`：`GET /connectors` 返回 proxy_url；
  `POST /connectors/{name}/proxy`（保存/清除，SSRF 校验失败 400）；
  `POST /connectors/{name}/test-proxy`（测 base_url 经代理连通性，
  任何 HTTP 响应=连通，连接失败/超时=不可用）。
- `app/schemas.py`：`ConnectorProxyRequest`。
- 前端 `DesktopView.jsx` 数据源面板：每个 Connector 加"设代理/改代理"
  展开（代理地址输入 + 测试连接 + 保存/清除），有代理时显示"代理"角标。

**② 萌娘百科 Connector（`app/connectors/moegirl/`）**
- **实测发现（决定实现路径，如实偏离任务预期）**：
  - 必须带**浏览器 User-Agent**，否则 401；
  - `list=search`、`action=parse`、`prop=revisions`（wikitext）、
    `index.php?action=raw` **全部被禁**（Unauthorized）；
  - 可用：`generator=search`（等效搜索）、`prop=info|pageimages|categories|
    extracts`、`action=opensearch`。
- search：用 `generator=search`（任务要求的 list=search 被禁），顺带取
  extracts+pageimages 直接带简介/封面；get_detail：`pageids` +
  `prop=info|pageimages|categories|extracts`。
- normalize 主路径：extracts 纯文本简介 + pageimages 缩略图 + categories
  分类（blocklist 过滤维护类：使用标题替换/分离袖子/无法存档的失效链接等）。
- **wikitext Infobox 解析保留为兜底**（`_parse_infobox` + `_strip_wikitext`，
  供开放 wikitext 的其它 MediaWiki/自建 wiki），用贴近《人物信息》模板的
  真实样例测试——覆盖任务要求的"模板字段提取逻辑"，但如实说明对萌娘百科
  本体不可达。

**测试**：`tests/test_proxy.py`（+26）+ `tests/test_moegirl.py`（+18），
全量 pytest **225 passed**（+44，覆盖率 83.09%）；vitest 9 passed；
`npm run build` 通过。

**实测（真实链路）**：
- 萌娘百科**真实 search**：federated 检索"初音未来"→ moegirl 返回
  `初音未来(1399)`、`初音未来(世界计划)(123)`，带真实封面图与简介；
  bangumi 超时进 errors（降级正常）。
- 萌娘百科**真实 get_detail**：`get_detail('1399')` → 初音未来、真实封面
   URL、完整简介、分类过滤后为内容标签（8月31日/M形刘海/VOCALOID角色/
   双马尾/发饰/处女座），维护类已过滤。
- **代理配置路径**：
  - 直连（空代理）→ `直连成功（…/api.php → HTTP 200）`；
  - `127.0.0.1:7890` 回环代理 → 400"代理地址不安全…已拒绝"（有意收紧：
    用户在本机用 clash 时代理地址就是 127.0.0.1，会被拒，见 ADR 0015
    权衡一节）；
  - 公网不可达代理 `8.8.8.8:3128` → `ok:false 代理连接失败：timed out`；
  - 私网代理保存 `192.168.1.10` → 400；
  - 公网代理保存/列出/清除 → 正常往返，重启后 `connector_settings` 行
    干净（`{}`）。

**关键决策**：代理 SSRF 校验**不放行回环**（与 Ollama 的 allow_loopback
形成对比，同坑位 #29 的分场景思路）；存储用单行全量 settings blob 而非
每 Connector 一行（避开 name 唯一键冲突）。

### 2026-08-06 代理配置放行回环 + Bangumi 经本地代理真实打通

用户指出：`validate_proxy_url` 拒绝回环导致 Clash/V2Ray（默认监听
127.0.0.1）这个**最主要的目标场景**被整个挡掉，等于没解决"Bangumi 需要
代理"的原始问题。

**代码改动**
- `app/connectors/base.py`：`validate_proxy_url(proxy_url, allow_loopback=True)`
  默认**放行回环**（127.0.0.1/localhost/::1），理由与坑位 #29/Ollama 一致：
  代理地址是用户在设置页主动配置的本地/受信地址，风险模型不同于 Connector
  处理不可信 URL；**其余私网段（10.x/172.16.x/192.168.x/169.254.x 云元数据）
  仍拦截**——只放行回环，不整体放宽。保留 `allow_loopback=False` 严格模式
  入口。新增 `http_post()`（Bangumi 搜索需要）。
- `app/connectors/bangumi/connector.py`：**修复代理打通后暴露的既有 bug**——
  Bangumi v0 搜索端点是 **POST** `/v0/search/subjects`（body
  `{"keyword":...}`），原来用 GET 会 404（被"直连超时进 errors"掩盖）。
  `_request` 支持 POST 分发，`search` 改用 POST body。
- `docs/decisions/0015`：修正"不放行回环"决策理由，补实测一节。

**测试**：`tests/test_proxy.py` 回环放行用例（127.0.0.1:7890 保存成功、
localhost/::1 放行、严格模式仍拒、API 层 Clash 默认端口保存）+ bangumi
POST 修正 2 用例；全量 pytest **232 passed**（覆盖率 83.07%）。

**实测（本机 Clash Verge，mixed 端口 127.0.0.1:7897）**
- 保存 `http://127.0.0.1:7897` 代理成功（此前 400）；
- federated 检索"辉夜大小姐"→ `errors: {}`，**bangumi 首次真实返回 10 条**
  （《辉夜大小姐…恋爱头脑战~》带真实简介 + lain.bgm.tv 封面），moegirl 同
  时返回 10 条，互不干扰；
- `get_detail("135218")` 经代理返回真实标题/简介/封面/标签；
- 代理已保存在 sources 表（`bangumi → http://127.0.0.1:7897`），用户可在
  设置页数据源面板查看/修改。

### 2026-08-06 角色图鉴：get_detail 接入前端 + 角色墙聚合

详见 docs/decisions/0016-character-wall.md。

**后端**
- `app/connectors/base.py`：`normalize_characters()` 统一角色字段
  `{id,name,image_url,relation,summary,actors}`（跨数据源）。
- `app/connectors/bangumi/connector.py`：get_detail **多拉一次**
  `/v0/subjects/{id}/characters`（实测 subject 详情不含角色），角色规范化
  进 `metadata.characters`；角色端点失败降级为空列表不阻塞详情；独立缓存
  key。
- `app/connectors/moegirl/connector.py`：`metadata.tags` 与 categories 同源
  （已过滤维护类），让萌娘百科分类作为标签消费。
- `app/api/routes.py`：
  - **收藏入库时机**：`_save_external_sync` 若 Connector 支持 get_detail，
    收藏时顺带拉详情，构建统一 raw_metadata
    `{source, detail:{title,description,image_url,metadata(含 characters)}}`
    写入；详情失败降级搜索级；幂等重收藏覆盖/回填；
  - 新端点：`GET /api/characters`（跨作品聚合角色，按 source+id 去重合并
    works）、`GET /api/items/{id}/detail`（读 raw_metadata）、
    `GET /api/external/detail`（live get_detail）。
- `app/reviews.py`：`get_public_rating` 兼容新结构 `detail.metadata.rating`
  + 旧结构顶层 rating。
- `app/schemas.py`：`ExternalDetailOut/ItemDetailOut/CharacterOut/CharactersResponse`。

**前端**
- `components/ItemDetailModal.jsx`（新增）：封面大图/简介/评分/标签/登场
  角色网格/收藏按钮。
- `components/CharacterWall.jsx`（新增）+ 导航项「角色」（people 图标）：
  角色卡片 → 点击看"出自作品" → 点作品回详情；空态引导。
- 入口：联合搜索行标题/详情按钮 → live detail；已收藏卡片点击 → item
  detail；角色墙点作品回详情。
- `api.js`：`fetchCharacters/fetchItemDetail/fetchExternalDetail`。

**测试**：pytest **244 passed**（+12，覆盖率 83.82%）；vitest 15 passed
（+6）；build 通过。

**实测（真实链路，经 Clash 代理）**：
- 收藏真实 Bangumi `135218`（辉夜…恋爱头脑战~）→ 标题被详情覆盖为真实标题、
  rating 7.2、**27 个角色**写入 raw_metadata；
- `GET /api/characters` 聚合 **27 个角色**各带关联作品；live external detail
  也返回 27 角色；
- 收藏萌娘百科 `1399`（初音未来）→ 详情落库、分类过滤后作 tags（8月31日/
  M形刘海/VOCALOID角色/双马尾/发饰/处女座）、角色为空（源无角色）；
- 测试数据已清理（角色墙回到空）。

**决策要点**：角色不建独立表（raw_metadata 内嵌 + 聚合实时扫描，避免过度
设计）；raw_metadata 存归一化 ItemDetail 而非原始 JSON（前端跨源统一读取）；
萌娘百科无角色字段如实展示为空。

### 2026-08-06 待办清理批次：评分聚合 + UI 直接填 Key + Spoiler 检索过滤

详见 docs/decisions/0017-todo-cleanup-batch.md（三条各自决策）。

#### ① 评分聚合（Review 系统遗留）
- `app/reviews.py`：新增 `get_my_rating(item_id)`——该 item 全部 Review 评分
  均值（忽略未打分），全部未打分返回 None；单条自然等于该条。
- `app/schemas.py`：`ItemDetailOut.my_rating`；`item_detail` 路由返回。
- 前端：`ItemDetailModal` 显示"大众 ★x / 我的平均 ★y"；`ReviewPanel` 头部
  本地计算同样均值（单条=该条，全未打分显示"暂无评分"）。
- 测试：多评分求均值、忽略未打分、单条、全部未打分、无 review、自建 session。
- **实测**：3 条 review [6, 8, 未打分] → `my_rating=7.0`；全部未打分 → null。

#### ② UI 直接填 Key（体验缺口）
- `app/providers.py`：`classify_api_key_ref` 把输入分为 占位符/明文/空；
  **明文不再拒绝**，`persist_api_key_placeholder` 把真实 key 写入 `.env`
  （`TSUMUGI_API_KEY_{NAME}`，gitignore、UTF-8、保留其它行、替换同名），
  同步 os.environ 并 load_dotenv 刷新，返回占位符引用存库——**不落库明文**；
  `SECRET_ENV_PATH` 模块级常量可注入（测试用）。
- `app/api/routes.py`：`save_llm_provider` 明文走写 .env 路径；`test_llm_connection`
  明文直接作为临时 api_key（不持久化）、占位符走环境变量。
- `app/main.py`：启动 `load_dotenv()`，让 UI 写入的 `.env` 变量在**重启后**
  仍能被占位符解析（关键：pydantic-settings 只读 Settings 字段，不进 os.environ）。
- 前端 `ProviderSettings.jsx`：提示"填 {ENV_VAR} 引用，或直接粘贴真实 Key
  （自动写入 .env，不落库明文）"。
- 测试：明文保存→占位符引用、明文不出现在响应/列表、占位符方式不受影响、
  两 Provider 不互相干扰、写 .env 替换/保留、空格值加引号、**重启后重载**。
- **实测**：保存 `sk-e2e-real-secret-999` → 存 `{TSUMUGI_API_KEY_E2ETEST}`、
  列表无明文、.env 写入；**重启后端**后测试连接 → DeepSeek 返回
  `api key: ****-999 is invalid`（masked 尾号证明 key 已从 .env 解析并
  用于请求头）。测试数据与 .env 已清理。

#### ③ Spoiler 内容仍参与检索（决策：方案A，完全过滤）
- 推荐方案A（spoiler 不参与检索）理由：用户主动勾选"含剧透"是强意图信号；
  方案B（参与+标记）防不住 LLM 复述剧透，防护不可靠且复杂。
- `app/retrieval.py`：`_retrieve` 拿到向量 hits 后批量查 `Review.spoiler==1`
  id（`_get_spoiler_review_ids`），剔除对应 review chunk，再走去重/排序；
  item 自身内容不受影响。
- 测试：spoiler+非 spoiler 同源、纯 spoiler、非 spoiler 仍在、item 内容仍在。
- **实测**（真实 embedding）：对 spoiler review"最终BOSS就是主角自己"提问
  → 结果里**没有**该 spoiler chunk；非 spoiler review"节奏紧凑"正常命中。

### 2026-08-06 安利卡生成（分享卡片）

详见 docs/decisions/0018-share-card.md。

**前端**
- `shareCard.js`（新增，纯函数）：`buildShareCardSvg` 手写 640×800 SVG
  （复用 Stats 面板 SVG 技术路线，不引入图表/Canvas 库）；主题取色读 CSS 变量
  （--bg/--accent/--text…），导出时用具体色值内联（独立 SVG 里 CSS 变量不生效）；
  `selectQuote`/`cleanQuote` 短评选取；`imageUrlToDataUrl` 封面转 data URL
  内联（防 Canvas taint）；`svgStringToPngBlob`/`downloadSvgAsPng` 导出。
- `ShareCardModal.jsx`（新增）：SVG 实时预览 + "下载图片 (PNG)"。
- `ItemDetailModal.jsx`：saved 时加"生成安利卡"入口；`DesktopView` 接入弹层。

**关键决策（短评 + Spoiler）**
- 短评 = **最新一条非 spoiler Review**（fetchItemReviews 时间倒序，取 [0]），
  清洗折叠空白、60 字截断加省略号；
- **spoiler 内容绝不进卡片**（比 RAG 更严格：卡片分享到应用外部）；
  全部 spoiler → 短评区显示"作者选择不剧透"，不退回用剧透内容；
  无 review → 短评区整块省略；
- 评分取 `my_rating`（聚合值），null 时整块省略评分区（不硬凑"暂无"）。

**后端**：`ItemDetailOut` 补 `file_path`（本地缓存封面，拼 /static 同源 URL）。
无其它后端改动。

**测试**：vitest **34 passed**（+19 安利卡：短评选取/spoiler 过滤/卡片组合/
XML 转义/导出触发）；pytest **266 passed**（file_path 字段断言）；build 通过。

**实测（真实链路）**：收藏真实 Bangumi 作品 → 加 1 spoiler + 1 非 spoiler
review → `my_rating=8.5`（8 与 9 均值）→ 用真实 `buildShareCardSvg` 生成：
- 短评正确选中**非 spoiler** 那条（剧透未出现）；
- 封面经 Clash 代理取到真实 lain.bgm.tv 封面并 data URL 内联；
- **headless Edge 把 SVG 光栅化为 PNG（640×800，签名合法）**——导出端到端
  可行。

### 2026-08-06 书架视图（第二种展示模式）

详见 docs/decisions/0019-bookshelf-view.md。

**前端**
- `bookshelf.js`（新增，纯函数）：`stringHash`/`primaryTag`/`spineSeed`/
  `hexToHsl`/`spineColor`。
- `components/Bookshelf.jsx`（新增）：窄条书脊（36×236，宽高比≈1:6.5）+
  竖排标题（`writing-mode: vertical-rl`）+ 左侧高光条 + 底部装帧带；hover
  **抽书预览**（fixed 定位，懒拉 `fetchItemDetail` 显示我的评分，带缓存，
  null 不显示）；点击书脊 → 打开详情。
- `DesktopView.jsx`：图书馆头部加 ▦网格/▤书架 分段切换，持久化到
  `localStorage["tsumugi-lib-view"]`（复用主题 localStorage 模式）；
  书架渲染现有 `libFiltered`（标签/分组/本地查找筛选天然生效）。

**关键决策（书脊配色）**
- 选**规则分配**而非封面主色提取：由主题 `--accent` 色相旋转 + **主分类
  标签哈希**决定偏移，饱和度/明度收敛到"书脊质感的低饱和中明度"；
  **同标签作品同色相**（按分类归架观感）、无标签用 id 兜底、主题切换整架
  随 accent 变化——不引入取色库/Canvas 复杂度，不脱离主题色系。

**测试**：vitest **48 passed**（+14：配色纯函数、Bookshelf 渲染/竖排/点击/
hover 评分/null、DesktopView 切换持久化恢复 + 书架下筛选联动）；pytest
**266 passed**（无后端改动）；build 通过。

**实测（真实数据 + headless Edge）**：向真实后端写入 5 个带标签条目
（恋爱×2、RAG×2、动漫/科幻×1），用真实 `spineColor` 计算配色生成书架 HTML
→ headless Edge 渲染 **800×380 PNG**：5 根书脊竖排标题，配色按主标签聚类
（恋爱 `hsl(188)`、RAG `hsl(106)`、动漫 `hsl(223)`）；视图切换/持久化/
筛选联动由集成测试验证。测试数据已清理。

### 2026-08-06 主题体系收敛：三套克制主题 + 有约束自定义框架

详见 docs/decisions/0020-theme-convergence.md。

**核心诊断**：历次主题迭代"换风格重来"导致视觉语言不统一、装饰元素过多
（渐变/多层光晕/大范围磨砂）显"AI 生成感"。本轮收敛而非加新主题。

**共享设计 token（:root 定义一次，主题不覆盖）**
- 间距 `--space-1..6`、圆角 `--radius-sm/md/lg/xl`（主表面 `--radius-lg`）、
  克制阴影 `--shadow-sm/md/lg`、信息密度 `--d-card-gap/pad/title/grid-min/font`
  （comfortable 默认 / `data-density="compact"` 紧凑）。

**三套主题（只改色彩）**
- 经典白（默认）：近白底 + 细边框分隔 + 单一蓝色强调 `#2563eb`，最克制；
- 薄荷 × 浅蓝：强调=薄荷绿 `#10b981`、次级信息=浅蓝（分区/状态区分）；
- 樱花粉：淡粉 + 粉强调 `#ec4899`，保持克制不堆砌。

**自定义框架（有约束，不做自由取色/背景上传）**
- 强调色色相 ±20° 微调（hexToHsl 旋转写 inline `--accent*`）；
- 信息密度 紧凑/舒适 两档；圆角 12~24px（inline 覆盖 `--radius-lg`）。
- **移除背景图上传**（任务点名"与调性不符"的旧遗留）；文字涂鸦保留。

**清理旧主题（真移除）**
- themes.js 只留 3 套；index.css 删全部旧主题/装饰覆盖；组件 backdropFilter
  19 处 → 0；构建产物 grep 确认 summer/violet-obsidian/spring/desk-ambient/
  bg-custom-layer/glass-radius 全部不存在，mint/sakura/--radius-lg/--d-card-gap 存在。

**测试**：vitest **53 passed**（themes 重写 + 自定义：data-theme/density、
radius 钳制、accent 写 inline、自定义 roundtrip；desktop/bookshelf props 更新）；
pytest **266 passed**；build 通过。

**实测（headless Edge，真实 index.css 变量渲染 900×560）**：经典白（9KB 扁平
克制）/薄荷（105KB）/樱花（65KB）三张截图，均为细边框+克制阴影、无光晕磨砂；
另生成"default+强调色+12°+紧凑+圆角20px"自定义预览，确认自定义层真实生效。

### 2026-08-06 交互细节打磨：批量操作 + 右键菜单 + 键盘快捷键 + Toast

详见 docs/decisions/0021-interaction-polish.md。所有新交互视觉复用 ADR 0020
的 token，不引入新风格。

**① 批量选择（网格 + 书架）**
- 选型：显式"选择"模式开关 + 点击选中（不选长按/Ctrl+点击——与"点击开详情"
  冲突且不可发现）；选中态 = accent 描边 + ✓ 角标。
- 批量操作栏（底部，"已选中 N 项 + 打标签/删除/取消"）：批量打标签弹
  TagEditModal（添加/替换/移除）；批量删除 window.confirm 二次确认。
- 后端：`ingest.set_item_tags`（add/remove/set）+ `ingest.delete_items`；
  端点 `POST /items/batch/tags`、`/items/batch/delete`、`/items/{id}/tags`
  （**batch 路由须在 {item_id} 前声明**，避免被捕获）。

**② 右键上下文菜单**
- 网格卡片/书架书脊右键 → 查看详情/编辑标签/写读后感/生成安利卡/删除；
  视口内钳制定位 + 外部点击/Esc 关闭；token 细边框 + 克制阴影。

**③ 键盘快捷键**
- `/` 或 Ctrl+K 聚焦搜索、Esc 逐层关闭（右键菜单→标签→快捷键→选择模式→
  详情→安利卡→书评→导入）、`?` 开关快捷键说明（导航栏加 "?" 帮助按钮）。
- **方向键条目导航跳过**（需焦点态+滚动+鼠标协调，复杂度不值，ADR 明示）。

**④ Toast 反馈**
- `toast.js`（success/error/info）+ `ToastHost`（右下堆叠、自动消失 2.6s/4.2s、
  token 样式）；接入导入/笔记/收藏/删除/批量/单条标签/Provider 保存与测试。

**测试**：vitest **64 passed**（+11：toast 渲染/自动消失/类型、批量计数与请求体、
右键弹出/删除、`?`/Ctrl+K/Esc）；pytest **271 passed**（+5 批量/单条标签删除）；
build 通过。

**实测（真实后端）**：3 条笔记 → 批量打标签（updated:3，均得 [批量,收藏]）→
单条 set → 批量删除（deleted:1）→ 列表正确减少。前端交互（批量栏/右键/`/`
聚焦/toast）由集成测试对真实组件验证；测试数据已清理。

### 2026-08-06 Electron 实机验证（CDP 真实点击）+ 修复客户端 API 404

用 CDP 驱动真实 Electron 客户端逐项实机点击（4 大项 12 项检查全过）：

- **A 右键菜单视口钳制**：缩窄视口 680×560 使卡片贴边角，右下角卡片右键 →
  菜单 rect 完全在视口内（left≥0 / right≤vw / bottom≤vh），未溢出。
- **B 批量选择 12 项**：批量栏"已选中 12 项"、12 个 ✓ 角标、选中卡 accent
  描边 + 光圈；选择 12 项耗时 31ms（无卡顿）。
- **C Toast 堆叠**：批量打标签 + 连续 3 条触发 → 4 个 toast 纵向堆叠互不遮挡；
  3.2s 后成功类自动消失（4→2）。
- **D 完整流程**：`/` 聚焦搜索 → 输入 → Esc → `?` 开快捷键说明 → Esc →
  右键 → 写读后感 → 书评面板打开。全过。

**顺带修复真实 bug（组件测试没抓到）**：Electron 下**所有 API 请求 404** →
图书馆空库。根因：`electron/preload.cjs` 的 `apiBase` 是
`http://127.0.0.1:8001`（缺 `/api`），而 `api.js` 拼 `` `${API_BASE}/items` ``，
客户端模式拼成 `.../items`（无 /api）→ 404。已改为
`http://127.0.0.1:8001/api`。测试 mock 了 fetch 所以拼 URL 差异不暴露。

回归：vitest 64 / pytest 271 / build 通过；实机测试数据已清理。

### 2026-08-06 规范沉淀：Electron 冒烟须发真实数据请求（防"冒烟通过"掩盖 404）

上一条 preload apiBase 缺 `/api` 的教训沉淀为硬性规则（已写入 AGENTS.md 第 7 条）：
**涉及 Electron 相关改动（preload/打包/端口/路径拼接）后，冒烟测试除 `GET /health`
外必须额外发一次真实数据请求（如 `GET /api/items` 或 `/api/connectors`），断言非
404 且含预期数据**。默认动作：改完 Electron 相关代码后在真实 Electron
（`--remote-debugging-port` 驱动）或后端进程上验证一次真实数据请求。
- 坑位 #30（新增）：只测 health 的冒烟会掩盖"客户端所有 API 404"类问题。

### 2026-08-06 Bangumi 收藏批量导入（OAuth 授权 + 批量拉取）

详见 docs/decisions/0022-bangumi-oauth-import.md。

**① OAuth 授权与 Token 存储**
- 设置页新增 "Bangumi" tab：OAuth 引导（开发者后台链接 + 回调地址 +
  client_id/secret 表单）→ 授权跳转 → 回调换 token → 轮询 status 显示已连接。
- client_id/secret 写 .env（`BANGUMI_CLIENT_ID/SECRET`，复用 write_secret_to_env，
  不落库）；**access/refresh token 存 `data/bangumi_tokens.json`**（gitignored，
  动态密钥无法用环境变量占位符表达，文件级隔离，DB 不落明文）。
- 过期自动 refresh；refresh 失败 → `NeedsReauthError` → 接口 401 + 前端明确提示
  "请重新授权"，不静默失败。SSRF：token/收藏端点过 `check_ssrf_target`。

**② 批量拉取收藏**
- `BangumiConnector.get_collections`（复用 `_request`：令牌桶限流 + 代理 + Bearer）
  分页 limit=30；`bangumi_import.py` 后台线程 + job 轮询（total/current/imported/
  skipped/failed/failures），前端进度条 + "已导入 X/Y + %"；单条失败不中断。

**③ 数据映射（核心）**
- 收藏条目 → Item：复用 `ingest_external`（幂等去重），用收藏接口内嵌 subject
  摘要建 Item，不重复调 search/detail。
- **追番状态 + 个人评分 → 自动 Review**：`rating`=Bangumi 评分（0→None），
  `status` 映射 `1想看/2看完/3在看/4搁置/5弃坑`，`content=""`（不产生向量，
  避免几百条平凡文本全量 embedding），`title="从 Bangumi 导入"`。
- **Review 加 `source` 列**（`bangumi_collection`）去重：同 item 同 source 一条，
  重复导入原地更新 rating/status。
- **角色信息分批策略**：批量导入不拉角色详情（请求量爆炸），改为**查看角色墙时
  懒加载补详情**（`backfill_bangumi_details` 单次≤20 条，令牌桶约束，失败跳过）。

**④ 测试与实测**
- pytest **291 passed**（+20：OAuth 授权/兑换/state/刷新/过期/断开、分页导入、
  状态评分映射、去重更新、单条失败、角色懒补）；vitest **67 passed**（+3
  BangumiPanel 引导/连接/导入进度）；build 通过。
- **实测（如实说明）**：真实公开用户收藏 API 抓 **508 条**确认结构；用 **12 条
  真实收藏数据**跑通导入映射（"阿基拉→看完★9"、"电影 吉伊卡哇→想看"、未评分
  →null），验证后清理；`get_collections` 无效 token → 401；**真实 OAuth 卡在凭证
  一步**（需用户在 Bangumi 开发者后台注册应用拿 client_id/secret，本机无法代办），
  授权/兑换/刷新/过期由 mock 覆盖，拿到凭证后即可走完整真实流程。

### 2026-08-06 Bangumi OAuth 真实端到端验证（用户注册真实应用 + 真实 57 条导入）

用户提供真实 App ID/Secret 并登记回调地址、勾选"收藏-READ"，完成首次真实
OAuth + 批量导入。全程抓到并修掉 **3 个组件/mock 测试没暴露的真实 bug**：

- **bug ① status 500**：Bangumi token 的 `user_id` 是**整数**，而 schema 声明
  `Optional[str]` → `/api/bangumi/oauth/status` 500。修复：route 里 `str(user_id)`。
- **bug ② 收藏 404**：`/v0/users/-/collections`（`-` 表示当前用户）**404**，真实
  API 要真实用户名。修复：新增 `_resolve_username` 调 `/v0/me`（复用代理+Bearer）
  拿 username 缓存到 token 文件，再用 `/v0/users/{username}/collections`。
- **bug ③ 重复导入计数错误（数据无重复）**：二次导入数据层无重复（57/57/0 重复
  external_id，幂等生效），但 job 把"已存在"误标 imported、skipped 恒 0。修复：
  `_import_page` 先查存在再记 skipped；实测二次导入 **imported 0 / skipped 57**。
- **角色墙懒加载初版同步阻塞 ~119s**（20rpm×每条 2 请求×20 条）：改为后台单飞
  非阻塞（`backfill_async`），角色墙 **0.23s 秒回**，后台逐步补齐（41→50 详情
  /70s）。

**真实结果**：授权一次通过（user_id 1051338）；批量导入 **57/57 成功、17.6s、
0 失败、未触发限流**；抽查映射正确（CLANNAD→看完★10、进击的巨人第三季→看完★9、
Silent Witch→想看 null）；角色墙真实基数下返回 1169 角色、无卡顿。
- 回归：pytest **293 passed**、vitest 67、build 通过。真实导入数据为用户真实收藏，
  保留在库中；角色详情随后续查看角色墙逐步补齐。

### 2026-08-06 VNDB Connector 接入（第三个数据源，POST+JSON body 风格）

详见 docs/decisions/0023-vndb-connector.md。

**实测先行（写实现前）**：`api.vndb.org` **直连可达**（无需代理，代理配置保留）；
公开 VN/角色查询**不需要 token**（不做 OAuth，避免过度设计）；API 是
**POST /kana/vn|character + JSON body**（`filters` 数组 + `fields` 逗号串），
区别于 Bangumi/萌娘百科的 GET 风格；filters 实测：搜索
`["search","=","STEINS;GATE"]`、详情 `["id","=","v2002"]`、角色按 VN
`["vn","=",["id","=","v2002"]]`（值是嵌套 VN filter）；角色字段 `vns.role`
（main/primary/side/appears，角色 API 不含声优）；rating 0-100（统一展示
0-10 → 除以 10）；描述含 BBCode 需清洗；限流文档 200 请求/5 分钟 → 令牌桶
40 rpm。

**实现**：`app/connectors/vndb/`（manifest + connector.py），HTTP 层用
`http_post(json_body=...)`（复用 base POST 助手）；角色归一化
`main→主角/primary→主要角色/side→配角/appears→登场`（与 Bangumi 中文 relation
一致）走 `normalize_characters`；`actors` 留空（角色 API 无声优）。
联合检索经 `discover()` 自动接入现有降级机制，来源角标复用 `r.source`（vndb），
前端零改动。

**顺带修复真实 bug（第三源暴露）**：所有 Connector 共用同一缓存表 + 相同 key
前缀（`search:{q}`/`detail:{id}`），前两个源没暴露，VNDB 一接入 vndb 读到了
moegirl 的 `search:命运石之门` 缓存 → external_id 变空。修复：`RequestCache`
加 `namespace` 参数，key 内部前缀 `{namespace}:{key}`，三 Connector 各传
`namespace=self.name`。

**测试与实测**：pytest **305 passed**（+12 vndb + 缓存命名空间）；实测真实
API：`search("Steins;Gate")` 10 条、`get_detail("v2002")` **17 个角色**（Makise
Kurisu 主要角色等）；**真实联合检索三源同时工作**：英文查询 bangumi 10 /
moegirl 10 / vndb 10、中文查询 bangumi 10 / moegirl 10 / vndb 4，`errors:{}`，
vndb 正确返回 v2002（无空 id）。vitest 67、build 通过。

### 2026-08-07 书评升级为"编辑器系统"（Markdown 工具栏 + 每篇可选字号）

**架构取舍（如实反馈）**：完整 WYSIWYG（contentEditable + 逐字字号）会把内容
存成 HTML，与"书评用纯文本/Markdown 存储并参与 RAG"冲突，且 `execCommand` 已
废弃、有 XSS 风险——**不做**。实现可行的"编辑器系统"：Markdown 工具栏（围绕
选区插入语法）+ 每篇书评可选的字体大小（存 Review，写/读同字号）。

- 后端：`Review.font_size` 列（ensure_schema 迁移）；ReviewCreate/Update/Out
  透传；`reviews.create_review/update_review` 接受 font_size。
- 前端 `ReviewPanel.jsx`：编辑器上方新增工具栏——**字号选择（14/15/16/18/20px）**
  + 格式按钮（加粗/斜体/删除线/小标题/列表/引用/行内代码/代码块/链接，围绕
  textarea 选区插入 Markdown）；正文/预览/已有书评都按存储字号渲染。
- 测试：pytest **309 passed**（+1 font_size 持久化）；vitest **77 passed**
  （+3：工具栏加粗包选区、字号选择改编辑器字号、书评按存储字号渲染）；build 通过。
- 实测真实链路：建书评 font_size=18 → 列表 18 → patch 20 → 清理，全部正确。

### 2026-08-07 沉浸式书评工作室（Review Studio）

按"Media Review & Note-taking Studio"设计，把书评升级为**全屏沉浸式工作室**
（Violet Obsidian 玻璃拟态），作为作用域独立的覆盖层实现，**不影响全局主题
收敛**（ADR 0020）。

- `frontend/src/components/ReviewStudio.jsx`（新增，替换 ReviewPanel 作为书评
  主入口）：
  - **模糊封面背景** + 深紫透玻璃面板 + 1px 发光紫边框（`.rs-*` 作用域变量）；
  - **左参考区**：四标签 Overview（简介/标签）/ Records（历史书评按 Markdown
    文档渲染，可编辑/删除）/ Archive（角色概念图网格，复用 raw_metadata
    characters）/ Memories（书写时间线）；
  - **右编辑器**：标题 + Markdown 工具栏（字号 14-20px + 加粗/斜体/删除线/标题/
    列表/引用/代码/链接，围绕选区插入）+ 写/预览切换 + **浮动引用卡**（从正文
    `> 引用` 实时提取展示）+ 底部评分/状态/剧透/发布。
- `index.css`：`.review-studio` 作用域样式（深紫玻璃、发光紫边框、紫罗兰高对比
  `.doc` 覆盖、`rs-quote` 引用卡）。
- DesktopView 书评入口切到 ReviewStudio。
- 测试：vitest **78 passed**（+1 引用卡提取、Archive 角色图；ReviewStudio 常驻
  编辑器/工具栏/字号/Records 字号渲染）；build 通过。
- 视觉验证：headless Edge 用真实 Bangumi 条目（空之境界·痛觉残留，封面+6 角色）
  渲染 studio 1280×760 PNG，确认玻璃拟态布局。

### 2026-08-07 Review Studio 视觉重做：排版驱动的沉浸感（回归主题收敛）

详见 docs/decisions/0024-review-studio-typography.md。

**修正背景**：上一轮"Violet Obsidian 玻璃拟态"是脱离三套收敛主题的"第四套
风格"（深紫玻璃+发光边框+模糊），直接违反 ADR 0020；用户已指出，本轮改为
**排版驱动**，保留左右对照结构（Overview/Records/Archive/Memories + 右编辑器）。

- **移除玻璃拟态**：删 `.rs-*` 硬编码紫罗兰/发光边框/backdropFilter，面板/标签/
  输入/引用卡改用全局主题 token（`--panel/--panel-border/--shadow-*/--radius-*/
  --accent/--accent-soft/--input-bg/--text*`）；切换主题工作室自然跟随。
- **排版沉浸**：正文限定 `max-width:720px` 居中 + `line-height:1.78` +
  **衬线字体**（Georgia/Songti/Noto Serif SC）——与界面无衬线形成"专门写作空间"
  质感（iA Writer/Bear 式）；进入时以主题底色不透明覆盖（chrome 自然淡出，不再
  模糊盖住）。打字机聚焦效果跳过（复杂度不值，记入未来项）。
- **一个克制点缀**：2px 纤细**强调色渐变字数进度条**（500 字软目标，用 accent）。
- **进出过渡**：纯 CSS `rs-enter`（展开淡入 0.28s）+ 关闭先 `.closing` 淡出
  0.18s 再卸载。
- 测试：vitest **81 passed**（+3：三主题渲染+token 接入、进出过渡、字数进度）；
  pytest 309（无后端改动）；build 通过。
- 实测（headless Edge，真实 CSS+真实条目数据）：三套主题各渲染 1280×760 有效
  PNG，工作室观感随主题统一变化，不再是独立风格。

### 2026-08-08 外部资料本地化下载 + 参与 RAG 检索（可区分来源）

详见 docs/decisions/0025-external-reference-rag.md。

**问题**：收藏入库只存简介摘要，完整资料不参与语义检索；且无法区分"自己写
的内容"和"外部下载的百科资料"，官方简介会挤掉自己的书评。

- **Chunk 来源标记**：新增 `source_type`（note/review/external_reference）+
  `connector` 列；`ensure_schema` 迁移加列并**回填**历史 chunk（review→review、
  external_ref 条目→external_reference+connector、其余→note，幂等只填 NULL）。
  检索层权威来源 = SQLite（Chunk 行），Chroma metadata 的 source_type 作快速
  路径，历史向量按 embedding_ref 批量回查 DB 兜底（不依赖 Chroma 迁移）。
- **检索区分来源**：加权（note/review=1.0，external_reference=0.4，配置项
  `external_reference_weight`）+ 可选 `source_types` 硬过滤。候选池提到 top_k*6
  补偿重排召回。不改 note/review 核心检索逻辑。
- **收藏即下载完整资料**：`_save_external_sync` 用现有 get_detail 响应构建
  `build_reference_text`（作品简介+基本信息+**角色小传**小节），`ingest_external`
  新增 `reference_text` 切分入库；`raw_metadata.detail.metadata.reference_text`
  存全文用于判重（文本未变不重建 chunk，避免重复 embedding）。
- **历史批量补齐**：`external_refs.backfill_external_reference(limit, source)`，
  复用各 Connector 令牌桶限流（不另起限流，避免重蹈"同步阻塞119秒"），每批
  limit=5（Bangumi get_detail 实为 2 请求/条，满速约 10 条/min，5 条留足余量），
  可重复调用推进、单条失败跳过；`backfill_async` 单飞后台线程供 API 用。
- **角色墙补详情顺带写 external_reference chunk**（同一 get_detail 响应不浪费，
  Bangumi 批量导入历史条目自动补齐 RAG 资料）。
- **手动刷新**：`POST /items/{id}/refresh-external`（单条，重新下载最新资料，
  不锁死为一次性快照）+ `POST /external/backfill-reference`（批量单飞）。
- **RAG 来源标注**：context 里 note→（我的笔记）、review→（我的书评：标题）、
  external→（百科资料：connector）；system prompt 要求主观问题优先用户书评；
  SSE sources 带全字段，前端来源角标显示"我的书评/我的笔记/百科·bangumi"。
- 测试：pytest **334 passed**（+25：迁移回填、collect 下载切分、检索加权/过滤、
  backfill 分批/限流/失败跳过、refresh 端点），覆盖率 85.6%；vitest **81**、
  build 通过。
- **实测（真实 Bangumi + 本地 Ollama）**：收藏「命运石之门」→ 22 个
  external_reference chunk（connector=bangumi）、18 角色、reference 文本 7081
  字；问"男主是谁"→ 检索命中百科资料、回答标注「百科资料」；写书评后问"我对
  命运石之门的看法"→ **top 命中 review（0.71），外部百科被压到 ~0.19**，回答
  引用「我的书评」；refresh-external 刷新成功。
- **顺带修复**：`.env` 无影响；实测发现启用的 LLM provider 指向不存在的模型
  `qwen2.5:3b`（Ollama 实际是 `qwen2.5:latest`），RAG 一直 404，已通过设置接口
  改为 `qwen2.5:latest` 并启用。

### 2026-08-08 书评工作室 Overview 改造 + Markdown 预览修复 + 多来源切换

详见 docs/decisions/0026-review-studio-overview-markdown.md。

**Markdown 预览修复（先复现后修）**：逐语法探测复现两个真实 bug——
① 行内代码被加粗/斜体误处理（`` `**x**` `` → `<code><strong>x</strong></code>`，
正则链式 replace 在结果上继续匹配的坑）；② 粗体含斜体嵌套撕裂
（`**外层*内层*内容**` → `*<em>外层</em>…*`，`[^*]+` 匹配失败）。修复：
`inline()` 改为逐字符 tokenizer（代码优先、不递归，其它标记递归处理），
并补 `***强调***` 与有序列表 `<ol>`。

**Overview 信息丰富化**：`ItemDetailOut` 新增 `reference_text`（ADR 0025 下载的
完整资料），简介优先取 `raw_metadata.detail.description`、缺失时从 reference_text
提取（不再只显示片段）；新增紧凑角色列表（名字 + relation，点击展开小传/声优）。

**多来源切换**：现有模型是一个 Item 单来源，不做合并去重，采用"同作品各来源
各收藏一份 + 规范化标题匹配"的简单方案。新增 `GET /items/{id}/related`（去空格/
全角标点/括号的 `_title_key` 匹配其它来源条目）；Overview 顶部渲染来源 Tab，
懒加载切换查看各来源简介/标签/评分/热度/角色。边界：VNDB 英文名与中文名通常
不匹配 → 返回空（预期）。

**"热门评论"可行性（先实测后实现）**：三源公开 API **均无用户评论文本接口**——
Bangumi `/v0/subjects/{id}/comments` → 404 且 OpenAPI 无此路由；萌娘百科
revisions/pageviews → 拒绝/404；VNDB `reviews` 字段 → 400。故不做评论展示，
改展示公开可查的替代数据：Bangumi 评分分布（rank/total/各分数人数）+ 收藏分布
（wish/collect/doing/on_hold/dropped）、VNDB 评分+投票数、萌娘百科仅页面信息
（如实标注"未公开评论/评分数据"）。connector 下载详情时写入（bangumi 增
rating_info/collection、vndb 增 votecount/length、moegirl 增 touched），
`extract_social_meta` 提炼为 `ItemDetailOut.social`，历史条目刷新后可补。

- 测试：pytest **345 passed**（+11：connector social 元数据、social/related 端点、
  item_detail 新字段）；vitest **88 passed**（+7：markdown 修复 5 例 + Overview
  多来源/热度块 + 无兄弟回退）；build 通过。
- **实测（真实 API + headless Chrome CDP 驱动真实 App）**：预览输入
  `## / **加粗** / *斜体* / `**代码**` / 1. 有序 / > 引用` → 预览 HTML 正确
  （`<h2>`/`<strong>`/`<em>`/`<code>**代码**</code>`/`<ol>`/`<blockquote>`）；
  真实收藏 Bangumi+萌娘百科「魔法少女小圆」→ Overview 出现双来源 Tab，切换后各
  自展示完整简介与热度分布（Bangumi"评分分布 13 人 + 收藏看过 26"，萌娘百科
  "页面长度 69035 · 最后编辑（未公开评论数据）"）；related 双向命中。
- 实测过程顺带发现并清理了多轮遗留的后端/前端进程（旧代码占 8001/5173）。

### 2026-08-08 第三方插件系统：本地文件加载 + 开源信任模型

详见 docs/decisions/0027-code-plugins.md。**有意推翻"禁止代码级插件"的旧决定**（不是
意外），权衡过程记录在 ADR：声明式配置无法覆盖 POST/OAuth/多端点等真实数据源需求、
开源项目代码可审查、内置 Connector 本来就是 Python 实现——但绑定严格信任模型。

- **信任模型（最重要的设计约束）**：允许用户把插件 .py 手动放进 `plugins/` 重启
  加载；**禁止**应用内"插件市场/一键远程安装并自动运行"（远程市场=不可审计的信任
  跳跃，本地文件=用户显式、可审查的决策点）；**不做沙盒**（个人项目做真安全沙盒
  工程量巨大且易出"看似安全实则可绕过"的假沙盒，比不做沙盒但如实告知更危险），
  插件与后端完全同权限，风险用户可见地暴露。
- **加载机制**：`plugins/` 下每插件一个子目录（manifest.json + connector.py +
  build_connector 工厂），与内置 Connector 同构；`_`/`.` 开头目录跳过
  （`plugins/_template/` 是可复制参考示例，不会被加载）；`app/plugins.load_plugins()`
  启动时用 importlib 独立模块名加载；**manifest.json 是身份/能力唯一权威**；声明
  get_detail 必须实现该方法。**单插件失败优雅跳过**（语法错误/缺接口/名字冲突等），
  记录 `[plugin] 加载 <目录> 失败：<原因>` 日志 + failures 列表，不崩应用；
  经 registry 统一注册（origin="plugin"），自动参与联合检索/收藏入库，不改编排代码。
- **风险提示**：设置页新增「插件」tab——插件列表每项醒目"⚠️ 第三方插件拥有与本应用
  相同的系统权限，请仅安装你信任的来源" + 加载失败列表可排查；**首次检测到插件时
  一次性风险确认横幅**（"我已了解，不再提示"持久化，不再打扰）；`/connectors` 返回
  origin；`plugins/README.md` 开头即有醒目安全声明 + 完整插件开发文档（接口规范/
  manifest 格式/最小示例/调试/安全注意事项）。
- `.gitignore`：`plugins/*` 忽略用户插件，保留 README 与 `_template/` 参考示例。
- 测试：pytest **358 passed**（+13 插件用例：正常加载、语法错误/缺 build_connector/
  返回 None/缺 search/声明 get_detail 未实现/名字冲突/manifest 非法优雅跳过、
  `_` 前缀跳过、端到端参与联合检索、/plugins + acknowledge 风险确认流程），
  覆盖率 86.0%；build 通过、vitest 88。
- **实测（真实 plugins/ 目录 + 真实 API）**：放入最小插件 `hello_world`（GitHub
  搜索）+ 故意写错语法的 `bad_syntax`，重启 → hello_world 加载（origin=plugin）、
  bad_syntax 优雅跳过并记录 `[plugin] 加载 bad_syntax 失败：'(' was never closed`，
  应用正常启动；`hello_world.search("rag")` 返回 5 条真实结果；`/search/federated`
  中 hello_world 与 bangumi/moegirl/vndb 并列返回；acknowledge 前 notice_needed
  true、确认后 false。实测中发现 Open Library 本机网络间歇不可达，实测插件改用更
  稳定的 GitHub API；提交的文档示例 `_template/hello_world` 仍用 Open Library。
  实测临时插件已删除（gitignored），仓库只保留 README + _template。

### 2026-08-08 修复左侧导航栏跟随内容滚动：改为标准 app shell 布局

详见 docs/decisions/0028-app-shell-layout.md。

**根因（布局层级问题）**：`.app-root` 是 `min-h-screen`、`.desktop-view` 是
`minHeight: calc(100vh - 60px)`，都不是固定高度 → 主内容比视口高时整个外壳无限
长高，`body` 成为实际滚动容器，把顶栏/导航/侧栏/主内容**一起滚走**；主内容区原有
的 `overflow-y-auto` 因 flex 子项默认 `min-height: auto` + 父级无限长高而从未生效。
顶栏当时用的 `position: sticky` 是不牢靠补丁，实测仍会滚走。

**修复**：
- `App.jsx` `.app-root` → `h-screen flex flex-col overflow-hidden`；顶栏去 sticky、
  加 `shrink-0`（外壳固定后天然钉顶，管理工具标准做法：顶栏+侧栏固定，只主内容滚）；
- `DesktopView` 根 → `flex-1 min-h-0 overflow-hidden`（填满顶栏以下，不外溢）；
- **主内容区** → `flex-1 min-h-0 overflow-y-auto`（唯一滚动容器；`min-h-0` 是关键，
  取消 flex 子项默认 `min-height:auto` 才能触发内部滚动）；
- 图标导航可排序区、侧栏内容滚动区补 `min-h-0`（侧栏标签很多时自己内部滚）；
- nav/aside 本就是 `.desktop-view` 直接子级（平级），结构上不可能被主内容滚动带走。
- **波及范围**：网格/书架/问答/角色墙/分析/设置全在同一个主内容滚动容器内，
  一处 shell 修复覆盖所有视图；书评工作室是 `fixed inset-0` 浮层不受影响。
- 已知边界：文字涂鸦层（absolute）会被外壳 overflow-hidden 裁剪（装饰性边界，
  记入 ADR 已知项）。

**测试**：vitest **92 passed**（+4 `layout.test.jsx`：① nav/aside 与主内容是兄弟
节点、主内容不含它们；② shell 固定不外溢、主内容有独立滚动、侧栏有内部滚动；
③ 模拟滚动 scrollTop=400 导航/侧栏 rect 不变；④ App 壳 h-screen flex-col + 顶栏
shrink-0）；build 通过。

**实测（真实 App + headless Chrome CDP）**：资料库网格 scrollHeight 4338>容器 747，
滚动 800px 后 `body.scrollY=0`（整页不滚）、`body.scrollHeight=innerHeight`（无
外溢）、顶栏 top=0、导航/侧栏 top=53 **滚动前后完全不变**；书架、角色墙视图同样
`body.scrollY=0`、侧栏零移动。滚动前/后各截 1280×800 PNG（layout_before.png /
layout_after.png）供直观确认侧栏固定可见、无空白。

### 2026-08-08 主从视图 + 状态分组列表 + Playnite 式信息设计 + 深色主题（第4套）

详见 docs/decisions/0029-master-detail-dark-theme.md。

**主从视图（优先级最高）**：资料库新增第3种浏览模式「分组列表」（☰，与网格▦/书架▤
三选一，localStorage 记忆）——天然内嵌主从视图：左=状态分组列表（主），右=详情面板
（从），同屏；点击任意条目右侧**立即**更新（fetchItemDetail 按需，不跳转/不弹层）。
**复用 ADR 0028 外壳**：主内容区内再分左右两栏，左列表与右详情**各自独立滚动**
（各自 flex-1 min-h-0 overflow-y-auto），库头部固定。

**状态分组列表**：按 Review 追番状态（想看/在看/看完/搁置/弃坑）+「未收藏」分组；
前端 `fetchAllReviews()`（复用既有 `/reviews`，首见即最新）构建 item_id→status 映射，
不改后端；每组标题+数量角标+可折叠展开（▾/▸）；只显示有内容的组。

**Playnite 式信息设计（四主题统一）**：新增共享组件 `ui.jsx`——`InfoTable` 属性表格
（label 右对齐/value 左对齐/行间细分隔线）+ `itemInfoRows()` 从 raw_metadata 提炼
基本信息（原名/日期/平台/开发商/评分/卷数集数等）；`TagCapsule` 统一圆角胶囊标签
（--tag-bg/--tag-text accent 系微弱变色，不随机上色）。应用到主从详情面板、
ReviewStudio Overview、ItemDetailModal（替换原先散落的两套标签样式）。

**深色主题（第4套，token 兼容红线）**：只新增 `html[data-theme="dark"]` 色彩变量，
间距/圆角/阴影/密度全复用 :root，不引入独立规则。配色学 Playnite"深色克制"原则：
深蓝灰底 #0e1116、面板 #151a22、文字高对比、强调色中亮蓝 #4a9eff。`applyAccentHue`
加深色分支：accent 明度~68%、hover 更亮、**soft 用深色染（~13%）而非浅色近白**；
色相/密度/圆角自定义与其它三套同一管线。

**测试**：vitest **103 passed**（+11：主从点击切换/切换条目/分组折叠展开与角标/
InfoTable 对齐分隔线/itemInfoRows/TagCapsule/深色 data-theme 与深色强调派生/与浅色
对比；原 themes.test 更新为四套）；build 通过；pytest 358（无后端改动）。

**实测（真实 App + headless Chrome，四主题 CDP 截图 md_*.png）**：点击分组列表条目
→ 右侧详情 h2 即时更新 + 基本信息表格 + 标签胶囊；左列表/右详情独立滚动；真实库
分组 `▾想看3 ▾看完4 ▾未收藏6`（数量角标），折叠未收藏组 6→0（▾→▸）；四主题 body
背景 rgb(250,250,250)/rgb(244,250,247)/rgb(253,246,247)/rgb(14,17,22)，深色 token
正确生效。

### 2026-08-08 修复列表/详情面板视觉缺陷（上一轮 ADR 0029 遗留问题的修复，非新功能）

用户提供深色主题"分组列表+主从详情"真实截图，逐项对照修复 5 个具体缺陷，并重新
截图逐条核对（详见下文实测数据）。涉及 `StatusGroupedList` / `ItemDetailPanel` /
`DesktopView`（主从容器） / `index.css`（surface 层级 + 行样式）。

- **① 来源标签被截断且无间距（真实 bug）**：列表行来源原本塞在固定 `w-8`（32px）
  span 里 → "bangumi" 被截成 "bangum" 且紧贴标题。改为**来源徽标用 TagCapsule 完整
  显示**（bangumi→Bangumi / moegirl→萌娘百科 / vndb→VNDB），外层 `shrink-0
  whitespace-nowrap` 保证不被裁切；行内 `gap-2`（8px）分隔徽标与标题，截断只发生在
  标题（flex-1 min-w-0 truncate）。
- **② 详情面板来源+评分拥挤**：`大众 ★6.6` 原来是裸文字紧贴来源胶囊。改为**来源 /
  大众评分 / 我的平均三者都是独立 TagCapsule**，`gap-2` 分隔。
- **③ 选中态不明显**：原来是 `accent-soft` 背景+细描边（深色下太淡）。改为 CSS 类
  `rs-list-row-active`：**accent 18% 低透明度填充**（`color-mix`）+ 强调色边框，
  扫一眼即知选中行。
- **④ 深色背景缺层次（扁平）**：新增表面层级 token **`--surface-0/1/2`**（四套主题
  都定义，深色尤其拉开）：深色下 0=#0b0f14（最外层）、1=#161d26（面板/卡片/列表/
  详情）、2=#202a37（hover/悬浮）。主从视图的列表容器与详情容器套 `--surface-1`
  面板背景+边框+圆角；同时把深色 `--bg`/`--panel` 的对比也拉大了。
- **⑤ 行内边距与 hover 态**：行 padding 由 `px-2 py-1.5` 加大到 `px-2.5 py-2`
  （8px/10px，文字不再贴边）；新增 `.rs-list-row:hover { background-color:
  var(--surface-2) }` hover 反馈。

**测试**：vitest **106 passed**（+3：来源标签完整渲染（Badge 文字=Bangumi、nowrap、
行 gap-2、标题 truncate 隔离）；选中行带 `rs-list-row-active`（非仅描边）；surface
token 与 `.rs-list-row:hover`/`-active` CSS 存在（读 index.css 断言））；build 通过。

**实测（真实 App + headless Chrome，深色主题分组列表，选中一个条目后截图
`fix_dark.png` 并逐项量测）**：
- ① 来源徽标文字 = **"Bangumi"（完整，非 "bangum"）**，`scrollWidth==clientWidth`
  （未裁切），徽标右缘到标题左缘间距 **8px**；
- ② 详情面板来源胶囊与"大众 ★…"胶囊间距 **8px**（两个独立胶囊）；
- ③ 选中行背景 = **accent 18% 透明度填充**（`color(srgb … / 0.18)`）+ 强调色边框
  rgb(115,169,232)，不再只是描边；
- ④ 层次：body 背景 rgb(11,15,20)=surface-0，列表/详情面板 rgb(22,29,38)=surface-1，
  hover 行 rgb(32,42,55)=surface-2 —— 三级表面色清晰可分；
- ⑤ 行 padding **8px 10px**，真实鼠标移入（CDP Input.dispatchMouseEvent）后行背景
  由透明变为 surface-2 rgb(32,42,55)。

### 2026-08-08 命令面板（Command Palette）：Ctrl/Cmd+K + 动作注册表

详见 docs/decisions/0031-command-palette.md。

**快捷键决策**：`Ctrl/Cmd+K` 从"聚焦问答搜索框"改为**呼出命令面板**（行业惯例
VS Code/GitHub/Figma）；`/` **保留**为聚焦问答搜索——两者用途不同（/ = 问答就地
搜索，Ctrl+K = 启动器/动作/跳转），原能力未丢失。原 interactions.test 的 Ctrl+K
断言同步更新。

**动作注册表**：新增 `frontend/src/commands.js` 的 `buildCommands(ctx)` 集中注册
命令（`{id, group, title, keywords, icon, run}`），分组 = 条目（内容搜索，动态）/
动作（预定义）/ 主题（四套，动态）/ 标签（动态）；ctx 只依赖一组可执行回调
（openItem/openImport/section/ask/setTheme/openTag/shareCard/reviewStudio）。
**后续新功能（声优图谱/年度热力图）只需在 buildCommands 追加一条命令**，面板零改动。
`matchCommand` 去空格小写子串模糊匹配，不引入 NLP。

**命令面板组件** `components/CommandPalette.jsx`：Ctrl+K 呼出居中弹层（遮罩 +
surface-1/panel-border/shadow-lg 全 token）；输入实时过滤、按分组标题展示（复用
Overview 区段标签排版）、↑↓/Enter/Esc、hover 同步选中、空状态提示；结果区独立
滚动（ADR 0028）。内容搜索选中回车 → `setSection("library") + setLibView("list") +
setDetailBrowseId` → **复用主从视图右侧详情直接切换**（最高频场景）。

**测试**：vitest **118 passed**（+12：注册表四组/主题四条/子串匹配；面板过滤/键盘
导航/openItem/主题切换 data-theme/空状态/执行后关闭；DesktopView 集成 Ctrl+K 呼出
+Esc 关闭、内容搜索跳转主从详情 h2）；build 通过；pytest 358（无后端改动）。

**实测（真实 App + headless Chrome CDP）**：Ctrl+K 呼出；输入"打开"+ArrowDown+Enter
→ 执行第二条"打开问答"、切到问答区、面板自动关闭（键盘导航流畅）；输入真实条目标题
"钢之炼金术师"+Enter → 主从详情 h2 即时更新；输入"深夜深蓝"+Enter → data-theme=dark
即时切换；Esc 关闭。截图 palette_jump.png / palette_dark.png。

### 2026-08-08 声优/Staff 关系图谱（作品-角色-声优 三层关系网络）

详见 docs/decisions/0032-voice-relations-graph.md。

**数据实测（先实测后设计）**：真实库 1840 角色中 87% 带声优（1597 个，243 个缺失，
**不保证完整**）；actors 是归一化声优名串列表；唯一声优 1020 个、分布极偏斜（只配
1 作 663 个 / ≥2 作 357 / ≥3 作 159 / ≥5 作 41）。**Staff 范围**：Bangumi get_detail
目前只取 subject+characters（含声优 actors），**未拉作品级 staff（persons）**——本轮
**只做声优关系**，staff 列为未来扩展。

**后端聚合 `/api/voice-relations`**：复用角色墙"内嵌+实时聚合、不建表"决策，从
raw_metadata 扫描聚合 声优→作品→roles 三层；缺失声优计入 stats.missing_actor_chars；
(声优,作品,角色) 去重、空白声优名跳过；阈值由前端过滤。

**图谱可视化技术选型**：**不引入力导向/图框架**（打包体积红线），手写**轻量 SVG +
确定性布局**——作品绕外圈、声优放其配音作品质心 + 70 轮重叠松弛（力导向的确定性
一次算完近似，无实时 O(n²) 帧循环），角色放声优→作品线段中点偏移；节点色全走
token。**默认阈值 ≥2**（实测剔除 663 个只配一作孤立声优，剩 357 声优/56 作品/
~1160 角色形成跨作品网络，正是本功能价值），提供 2/3/5/8 四档调节。

**交互与入口**：点声优 → 完整配音列表 + 子图高亮；点作品/角色 → 复用主从视图跳详情
（setSection(library)+setLibView(list)+setDetailBrowseId）；入口 = 新导航"声优图谱"
+ 角色墙"声优关系"按钮 + 角色面板声优名一键跳 + **命令面板"打开声优图谱"动作项**。

**测试**：pytest **363 passed**（+5：空数据/三层聚合/角色去重/空白声优名/缺失计数）；
vitest **124 passed**（+6：默认阈值过滤/点声优配音列表/点作品 onOpenWork/focusActor
自动选中/阈值调节/命令面板动作项）；build 通过。

**实测（真实 App + headless Chrome CDP）**：阈值 2 图谱 **1575 圆（357 声优 + 56
作品 + ~1162 角色）+ 2324 边**；阈值 3→927 圆、5→359 圆（调节即时生效）；点击真实
跨作品声优 **坂本真綾（12 部作品）** → 详情面板列出 12 部作品完整配音（空之境界
系列/伤物语系列/星际牛仔/死亡笔记等）；点详情里"剧场版 空之境界 第三部 痛觉残留"
  → **主从视图 h2 即时更新**。截图 voice_graph.png（阈值2全量）/ voice_graph_t5.png
  （阈值5稀疏版）。

### 2026-08-08 活跃度热力图（年度总结）

详见 docs/decisions/0033-activity-heatmap.md。

**活跃度权重（决策）**：`按天 = 书评数×2 + 新收藏数×1`。书评是主动创作（投入/信号
高），收藏是轻量保存动作；权重拉开让"深度投入"的天更突出。数据来源：书评 =
Review.created_at，新收藏 = 外部条目 Item.synced_at；本地笔记不计入（任务范围外，
未来扩展）。

**后端聚合 `GET /api/activity?year=`**：按天分组返回 `{date, reviews, collections,
score}`（只含活跃天）+ 统计摘要（总数/活跃天数/最活跃月份/最长连续活跃）；跨年边界
只按 `year-` 前缀计入。

**热力图（手写 SVG，复用 Stats 面板路线，不引图表库）**：GitHub 贡献图风格——一格
一天、按周排列、对齐周日；五档深浅 = `color-mix(accent X%)`（22/42/62/85，基于当前
主题 accent，四主题统一）；零分天 surface-2、未来格透明；每格 `<title>` 悬停显示
"X 条书评 · Y 个收藏"。

**页面 + 入口**：`YearlySummary.jsx` 热力图 + 统计摘要卡；入口 = 新导航"年度总结"+
命令面板"打开年度总结"动作项。

**测试**：pytest **369 passed**（+6：空数据/按天加权分组/跨年边界双向/最长连续活跃/
本地笔记不计收藏/默认当前年）；vitest **128 passed**（+4：热力图+摘要渲染/悬停
title/档位配色/命令动作项）；build 通过。

**实测（真实 App + headless Chrome，真实数据）**：年度网格 224 格（2026-01-01→
08-08）；活跃 2 天——**2026-08-07 为醒目 accent 85% 色块**（57 书评+57 收藏=171 分，
即本轮高强度开发/导入产生的活跃记录），08-08 较浅（4 收藏）；悬停显示"2026 年
8 月 7 日：57 条书评 · 57 个收藏"。截图 yearly_summary.png。

### 2026-08-08 封面动态取色氛围（Ambient Color from Cover Art）

详见 docs/decisions/0034-ambient-cover-color.md。

**取色位置（决策）**：前端 Canvas 实时提取 + 按封面 URL 缓存（模块级 Map，上限
200）。理由：主从详情面板的封面已被后端收藏时缓存到本地（/static 同源可读），无需
后端预计算字段；查看详情才取色、频率低。**算法**：轻量直方图主导色（不引图像库）——
32×32 缩小 → RGB 高4位量化（4096 桶）→ 主导色 + 第二主色（hue 差>30° 才保留）；
**可读性降级**：主色过灰/过黑/过白 → null → 回退主题表面色。

**防 taint（复用安利卡经验并改进）**：优先直接加载原 URL 绘制（同源 /static 含
扩展名被猜成 .img/octet-stream 的浏览器 img 按内容嗅探可读）；跨域污染 canvas → 回退
`imageUrlToDataUrl`（CORS fetch→data URL）；非 CORS 远程 → null 降级。

**克制参数**：详情面板右上角柔和圆形光晕（双层 box-shadow，可 0.5s 平滑过渡切换
条目）；透明度走 `--ambient-alpha` token——浅色 **0.16-0.18**、深色 **0.30**；
光晕 pointer-events:none、在内容之下，文字/按钮仍是主题 token 色，不被覆盖。

**测试**：vitest **137 passed**（+9：纯红/纯蓝主色、纯白/黑/灰→null 降级、透明像素
跳过、红+绿主次色、isVivid 边界；详情面板光晕用取色主色+标题仍 token 色、切换条目
更新+0.5s 过渡、无封面→透明不触发取色）；build 通过；pytest 369（无后端改动）。

**实测（真实 App + headless Chrome CDP）**：纯色演示封面端到端——红→rgb(210,40,40)、
蓝→rgb(40,90,210)、绿→rgb(40,170,80)，**不同条目氛围色真实反映各自封面主色**；
真实收藏条目（本地缓存封面）——《命运石之门系列》→ rgb(53,54,9)（深橄榄调）、
《魔法少女小圆》→ rgb(183,197,188)（浅灰绿调）；**深浅主题克制对比**：浅色 alpha
0.16 / 深色 alpha 0.30，文字按钮不受影响。截图 ambient_dark_red.png。
（说明：批量导入条目封面因当时网络波动未缓存本地——仅 2/61 有本地封面，远程
lain.bgm.tv 本机当前不可达；真实条目演示用了 2 个有本地封面 + 3 个纯色演示封面做
端到端验证，演示数据已清理。）

### 2026-08-08 补齐历史收藏封面本地缓存（回填）+ .img 扩展名根因修复

**bug 修复范围确认**：上一轮对 ".img 扩展名" 的修复**只是取色场景的局部绕过**
（ambient.js 优先直接加载同源封面让浏览器按内容嗅探）。根因在 `app/images.py` 的
`cache_external_image`——`_guess_ext` 对无扩展名 URL（Bangumi 封面
`/pic/cover/l/xx/yy`）兜底写成 `.img`，/static 以 octet-stream 提供，**任何
"fetch + blob.type 校验"的消费方都读不到**：除取色回退外，**安利卡 ShareCard 的
`imageUrlToDataUrl` 同样受影响**（blob.type 非 image/* → 封面内联失败退化为占位图）。
**本轮从根源修复**：新增 `download_and_cache_cover`——下载后按 **Content-Type / 魔数**
识别正确扩展名再落盘，清理旧 `.img` 残留，支持出站代理；`cache_external_image`
保持原契约（失败返回 None，不阻塞收藏入库）。

**批量回填**：`scripts/backfill_covers.py`（顺序执行 + 0.3s 间隔限速，复用 bangumi
connector 的出站代理；单条超时 2.5s，避免不可达时长时间挂起；输出 flush 实时落盘）。
结果：
- 待回填 **60 条**（59 缺本地封面 + 1 扩展名错误；另一条 .img 在上一轮中断运行中已
  被重缓存，故剩余 1 条）；
- **成功 2（累计）/ 本次成功 1**：命运石之门系列、魔法少女小圆的封面重下载并以正确
  `.jpg` 缓存（魔数验证为有效 JPEG），**`still .img = 0`**；
- **失败 59**：全部"连接超时（网络不可达）"——lain.bgm.tv 本机当前不可达且未配置
  出站代理。**待网络恢复后重跑本脚本即可补齐**（脚本幂等：已有正确缓存自动跳过）。

**测试**：pytest **379 passed**（+10 `test_images.py`：Content-Type/魔数扩展名识别、
非图片回退 URL、缓存命中复用、旧 .img 清理、失败原因（网络不可达/404/非图片）、
wrapper 契约）；vitest 137、build 通过（本轮无前端改动）。

### 2026-08-08 开启代理后重跑封面回填（Clash Verge 127.0.0.1:7897）

**确认代理**：本机 Clash Verge 运行中，HTTP 代理 **127.0.0.1:7897** 在监听；经代理
访问 lain.bgm.tv 封面 **HTTP 200 / image/jpeg（1.6s）**；且 connector 设置里已持久化
bangumi 代理 `http://127.0.0.1:7897`（上一轮配置过）。

**发现并修复脚本缺陷**：`backfill_covers.py` 此前没调用 `registry.discover()` +
`apply_settings(持久化设置)`——registry 为空，`get_connector` 返回 None、代理读不到，
所以直接连接全部超时。已修复：与 main.py 启动一致（先 discover 注册内置 Connector，
再应用持久化代理），并按条目数据源取代理（bangumi 用 bangumi 的代理）。

**回填结果（经代理）**：
- 第一轮：**成功 58，失败 1**——唯一失败为 [41] 空之境界 矛盾螺旋"连接超时"；
  诊断确认该封面经代理可正常访问（两次均 HTTP 200 image/jpeg 57KB），**非死链/
  非 DNS/非限流**，是一次瞬时连接超时；
- 第二轮（脚本幂等，自动跳过 58 条已缓存）：**成功 1，失败 0**（[41] 补上）；
- **最终 59/59 补齐**：外部收藏 61 条**全部有本地封面（61/61）**，`still .img = 0`，
  新增封面魔数验证为有效 JPEG。
- 若仍有失败且原因不同（404=图片本身失效 / DNS 解析失败 / 429=限流），脚本会逐条
  输出具体原因；本次实际只出现"连接超时（网络不可达）"一类，且为瞬时、重跑即过。

### 2026-08-08 修复声优图谱默认视图可读性（阈值+标签分级+碰撞避让+布局增强）

详见 docs/decisions/0035-voice-graph-readability.md。用户截图确认默认阈值2时
357 声优挤压成"实心圆饼"、标签大面积重叠不可读。

- **默认阈值 2 → 3**：实测 ≥3 = 159 声优（保留较完整网络），≥5 = 41 更稀疏；
  档位 2/3/5/8 保留可调。默认 3 + 标签分级后 927 圆以圆点为主、可读。
- **标签分级（根治糊字）**：只有配音作品数 ≥ `LABEL_ACTOR_MIN`(4) 的声优与全部作品
  才进入标签候选；其余声优/角色只显示圆点，**hover 显示名称**（title）。
- **碰撞避让**（pickLabels）：按优先级（声优=作品数、作品=连接数）贪心放置，重叠则
  隐藏低优先级；选中声优标签强制显示；包围盒宽度系数 0.9（中文全角保守估宽）。
- **布局增强（根治实心）**：迭代 70→160、最小间距 55→74、排斥力增强；**确定性抖动
  初始化打破对称坍缩**（多个声优同质心时斥力向量抵消会原地不动）；节点钳制画布内；
  角色小圆点 r=3.2→2.6、低透明度。
- 测试：vitest **145 passed**（+8：标签候选/碰撞避让/forcedKeys/包围盒/布局钳制与
  分散；voice-graph 更新为默认阈值3+标签分级断言，SVG text 精确查询避开 title 误匹配）；
  build 通过。
- **实测（真实 App + headless Chrome CDP，默认视图阈值3）**：**0 标签重叠**（101 个
  可见标签两两包围盒零重叠）；**不再挤压成实心**——927 圆（159 声优+56 作品+~712
  角色），声优位置 x 标准差 **144px**（明显铺开）；阈值5 = 359 圆/79 标签/0 重叠。
  截图 voice_default3.png（默认）/ voice_t5_read.png（阈值5）。

### 2026-08-08 声优图谱改为搜索驱动的"以人为中心"邻域视图（ego network）

详见 docs/decisions/0036-voice-ego-view.md。用户反馈确认：上一轮只解决了标签重叠，
**问题的根本不是标签，是全局图本身的边密度**（357 声优/2324 边交织成毛线球，
高密度下无解）——故从"渲染整张全局图"改为**"以搜索选中的单个声优为中心的邻域
视图"**。

- **搜索优先入口（默认态）**：顶部搜索框（复用命令面板 matchCommand 模糊匹配）；
  未选中时不渲染全局大图，展示引导文案 + "配音作品最多的声优"快捷入口（前 8 位）。
- **单声优邻域（核心）**：`buildEgo` + `layoutEgoGraph`——中心=选中声优，内环(R150)=
  TA 配过的全部作品 + 角色小圆点，外环(R270)=共同出演声优（只连共享作品、不展开其
  它作品，避免滚雪球）；标签复用 pickLabels 碰撞避让，实测 0 重叠。
- **全局概览降级为可选**：保留"查看全部声优网络"入口（非默认），进入时明确提示
  "数据量大、边密集，建议用搜索聚焦单个声优"；阈值 2/3/5/8 保留。
- **入口联动**：角色墙点声优名 → 直接进该声优邻域；命令面板"打开声优图谱" →
  搜索入口页；离开 voice 区清空 voiceFocus（避免重进重新聚焦旧声优）。
- 测试：vitest **147 passed**（+2 buildEgo/coActorCap/layoutEgoGraph；voice-graph
  重写为搜索驱动：默认引导态/搜索过滤空状态/邻域只含选中声优的作品/焦点直进邻域/
  概览入口提示）；build 通过。
- **实测（真实 App + headless Chrome CDP）**：默认入口显示 坂本真綾·12部/
  子安武人·12部/花泽香菜·9部/能登麻美子·8部 快捷入口；搜索"坂本真綾"→ 邻域
  **65 圆（1 中心 + 12 作品 + ~12 角色 + 40 共同出演）、103 边、0 标签重叠**——
  对比全局 927 圆/2324 边，规模小约 14 倍，清晰可读不再是毛线球。
  截图 voice_search_entry.png / voice_ego_sakamoto.png。

### 2026-08-08 封面氛围色延伸到网格卡片 hover 与书架书脊高光

详见 docs/decisions/0037-ambient-card-shelf-hover.md。完全复用 ADR 0034 的取色逻辑
（extractPalette 缓存），不重新实现；书脊主色仍按 ADR 0019 标签哈希决定，取色高光
只是 hover 的额外点缀层。

- **组件 `CoverAmbient`**：包裹卡片/书脊，hover 才懒触发取色（页面加载不预计算），
  box-shadow 光晕层 `inset:0` + 同圆角 + pointer-events:none，进出 `box-shadow 0.3s`
  过渡；
- **强度（明显弱于详情面板）**：alpha = `--ambient-alpha × factor`，卡片 0.6
  （浅色≈0.10）、书脊 0.7（≈0.11）vs 详情 0.16/0.30；光晕卡片 18px / 书脊 12px；
  无封面/取色失败 → 主题 accent 降级高光（保留 hover 反馈）。
- **顺带修正**：`cardCover` 此前优先 remote image_url（浏览器可能加载失败/CORS →
  取色降级），改为与详情面板一致优先本地缓存 file_path（同源可靠），remote 兜底。
- **性能**：取色仅单卡片 hover 触发 + URL 缓存（同一 URL 返回同一 Promise）；为可测
  性把 extractPalette 拆缓存包装 + `_computePalette`，并改为非 async（缓存引用可观察）。
- 测试：vitest **156 passed**（+9：缓存复用（同一 URL 同 Promise/只算一次/不同 URL
  各算/无 src null）、hover 懒触发+封面主色光晕、离开恢复+0.3s 过渡、无封面 accent
  降级、书脊 hover 高光且不覆盖标签主色、书脊无封面降级）；build 通过。
- **实测（真实 App + headless Chrome CDP）**：网格 hover 魔法少女小圆 →
  rgb(183,197,188) a=0.094、另一小圆条目 → rgb(249,168,168)（不同封面不同色调）、
  命运石之门系列 → rgb(53,54,9)——与 ADR 0034 详情面板实测同封面色调一致；书脊 hover
  同调（a=0.114）且书脊主体仍为标签哈希色；强度层级 卡片 0.094 < 书脊 0.114 < 详情
  0.16。截图 ambient_grid_hover.png / ambient_shelf_hover.png。

### 2026-08-08 图书馆数据备份/导出/导入（兑现长期技术债）

详见 docs/decisions/0038-backup-export-import.md。兑现 AGENTS.md"未来扩展点"里一直
搁置的"本地库数据导出/备份"。

- **导出格式**：单个 JSON 文档（`format=tsumugi-library` + version 字段），内容 =
  items（含 raw_metadata 下载资料）/ reviews / tags / sources / llm_providers；
  密钥均为环境变量占位符（config_ref/api_key_ref，不落明文）；文件带时间戳。
  **理由**：可审计、可版本化迁移、可控范围；不选 SQLite 直接拷贝（chunks 会指向
  丢失向量、受 schema 演进影响）。
- **向量库（Chroma）不导出**：可从原始内容重建（导入时 ingest 重建 chunk +
  embedding），且体积大、与库版本强耦合。
- **导入冲突策略（幂等合并）**：外部按 (source, external_id)、本地笔记按 content_hash
  去重——命中刷新元数据（不重建向量）、未命中新建并重建向量；Review 按
  (item_id, title, content) 去重；标签/数据源/Provider 按 name 幂等。导入到空实例 =
  完整恢复，导入到已有实例 = 合并不重复，重复导入幂等。
- **触达**：设置页新增「备份」tab（导出下载 / 导入上传）；导入走后台线程 +
  job 轮询（复用 bangumi_import 模式），前端进度条 + 结果 toast，大数据量不阻塞。
- 测试：pytest **386 passed**（+7：导出完整性/导入空实例恢复（含向量重建）/幂等合并/
  非法格式/路由与任务状态）；build 通过。
- **实测（真实数据闭环）**：真实导出 646KB JSON（63 items/57 reviews/289 tags/2
  sources/2 providers，0.1s）；导入到全新临时 SQLite+Chroma 实例 **22s（含向量重建）**
  恢复 63/57/289、chunks 重建 89 条；「命运石之门」reference_text 7081 字 + 22 条
  external_reference chunk 完整保留，本地笔记 note chunk 重建。

### 2026-08-09 图书馆首页：神殿空间氛围（Layer 1，不含角色台词系统）

详见 docs/decisions/0039-home-shrine.md。首页（默认 ask 空态）重做为"神殿空间氛围"，
**不含角色台词系统（Layer 2）**、无角色插画、无新图片资源，严格遵循 ADR 0020 token
体系（红线：不引入独立风格，不装饰堆砌）。

- **空间隐喻**：纵轴构图 + 大留白；SVG **鸟居轮廓线**（4 条 path 的极简几何符号，
  点到为止非场景插画）；搜索栏作焦点"祭坛"；"最近供奉"——最近收藏封面以**纵深/退级**
  排布（中心最低最实、两侧逐级升高变淡），非平铺网格。
- **氛围色环境光**：复用 ADR 0034 取色，以"最近收藏代表封面"主色生成顶部 radial
  环境光（`--ambient-alpha × 0.7` 更收敛）；首页随最近关注的作品变化色调；无数据/
  取色失败回退主题表面色。
- **仪式过渡（控制频率）**：每天**首次打开**播放"暗幕 0.55→渐亮 + 内容渐次呈现"
  （纯 CSS transition ~1.2s）；同日后续进入简化版；点击/按键可跳过。**理由**：每次都
  播会从仪式感退化为烦人加载动画，按天切分符合节奏。**坑位**：React StrictMode 双调
  useState 初始器会提前消耗"当天已播放"标记 → `shouldPlayRitual` 保持纯查询，在
  仪式 effect 播放时才 markRitualShown。
- 测试：vitest **160 passed**（+4：渲染/氛围色环境光与降级/每日首次播放+同日不重复/
  点击跳过）；build 通过；pytest 386（无后端改动）。
- **实测（真实 App + headless Chrome CDP）**：环境光 = 最新收藏（魔法少女小圆）封面
  主色 rgba(183,197,188,0.114)；仪式时间线 t≈0-70ms 暗幕 veil 0.55 → 揭幕 → 1s 内
  渐亮；同日再进无完整仪式。截图 shrine_home.png / shrine_ritual_dim.png。

### 2026-08-09 图书馆看守猫娘：人设文档 + 首页固定台词库（Layer 2 第一步）

详见 docs/decisions/0040-mascot-lines.md 与 docs/mascot-persona.md。在人设上**不重新
设计**（用户已确认），把口头规则量化为可测试的硬约束。

- **人设圣经 docs/mascot-persona.md**：身份（住在这里的猫，不是客服）/ 性格（软萌、
  黏人、情绪直给）/ 称呼（固定"主人"）/ 口癖（"喵～"句尾、克制：同库占比≤50%，担心、
  低落句不加）/ 三种态度红线（新收藏开心、想念不抱怨、为写书评开心且不评判深浅）/
  各场景基调表 / 语言红线 / 创作自检清单。
- **台词库 frontend/src/mascot.js**（独立数据文件，改台词不动组件）：分场景 5-8 句——
  时段问候（晨/午/晚/深夜）、新收藏、想念、写书评、里程碑（收藏 1/10/50/100/200/500、
  书评 1/10/50/100）、兜底。全部遵守人设，测试把规则写成断言（每句含"主人"、句长≤40、
  喵必须"喵～"且占比 42.3%、无客服/数据腔）。
- **展示位置**：首页纵轴中搜索栏（祭坛）与"最近供奉"之间一行小字，text-secondary
  12px，参与既有 shrine-item 渐次揭幕，不打断、不弹窗、无视觉装饰。
- **触发匹配 matchScene（纯函数）**，优先级：想念（>3 天）> 里程碑（恰好等于节点）>
  新收藏（6h 内）> 写书评（6h 内）> 时段问候 > 兜底。窗口/节点集中在 mascot.js 顶部可调。
- **会话内稳定**：HomeShrine 用 ref 保证挂载期间只匹配一次，数据刷新不换台词；切换
  页面再回来才重新选。
- **坑位 1（StrictMode 双跑）**：computeLastVisitGap 若在 useState 初始器读写
  localStorage 会被双跑写两次→想念永不触发；用**模块级缓存**每次页面会话只算一次。
- **坑位 2（数据异步竞态）**：newestCollectionAt 原依赖 App 的 items（异步晚到），
  会在数据就绪判定之前是 null → 台词选成问候而非新收藏；改为与 collectionCount
  同一批请求（groupCounts 的 external_ref limit:1 即最新外部收藏）一起返回，消除竞态。
- **实测（真实 App + headless Chrome CDP，3 场景）**：想念=「主人不在的这些天，我把
  每件收藏都检查过一遍，一件都没少。」（localStorage 模拟 4 天未打开）；新收藏=「主人
  收的这件，我一眼就喜欢上了。」（临时把最新收藏 created_at 改为现在，测后已还原到
  2026-08-08 03:53:25，integrity_check ok）；问候=「主人午安喵～要不要休息一下？」
  （还原后自然时段）。截图 mascot_missing_you/new_collection/greeting.png。
- 测试：vitest **177 passed**（+17：matchScene 六类场景与优先级/窗口边界/时段分界、
  selectLine 兜底、lastVisitGap 缓存、台词库结构与三大人设硬规则、首页展示+rerender
  稳定、里程碑、数据未就绪不展示）；build 通过；pytest 386（无后端改动）。

### 2026-08-09 AGENT_UI.md：反模板审查（v1 → v2）

对 AGENT_UI 设计纲领做 16 项反模板审查（Dashboard/Notion/Bangumi/Chat/二次元皮肤/卡片/
渐变/发光/圆角/装饰/猫娘/神社/书架/AI/我的vs外部/长期疲劳），发现核心风险是**信息架构
仍站在"工具"一侧**，而非颜色问题。修复落回 AGENT_UI.md v2：

- **新增【应用外壳：图书馆的空间结构】**：三栏壳的导航重构为"图书馆各馆"（检索台/书库/
  人物档案馆/关系厅/记忆回廊/管理室），侧栏=馆内导览，统计数字重新定位为馆藏目录与阅读史，
  每屏可回答"我在图书馆哪个空间"。
- **新增【排版与视觉层级系统】**：衬线标题+现代无衬线正文、基线网格、2:3 封面；**圆角层级**
  （封面可圆/控制小圆/正文面板近直角）；**卡片治理**（仅物理馆藏对象可用卡）；渐变只用于氛围光、
  发光全屏仅一个焦点、层级用细描边+柔和阴影。
- **书架**：从"书脊列表"升级为真书架（层板线/按系列分架/书脊厚度来自数据/hover 取书/点击滑出开书）；
  **Archive Grid 弱化追番状态徽章**（Bangumi 语言），重心移到"我记录了什么"。
- **作品详情**：强化为"打开一本书"双页结构——左页=外部世界档案、右页=我的理解，用页面结构
  区分而非只靠角标。
- **AI**：明确为"检索记录文献形态"（无气泡/头像/线程；回答=带馆藏脚注引用的文献；每次会话
  自动写入记忆回廊成为检索记录）。
- **记忆回廊**：提升为反 Dashboard 的核心证据，猫娘台词/详情时间线/年度总结统一从它取数。
- **猫娘**：从问候者升级为"记忆的记录者"（记忆之声，引用真实馆藏状态）。
- **动画节奏**：默认安静、进入/状态变化才动、≤200ms（仪式按天限流）、尊重 reduced-motion、
  禁止循环动画。
- **新增【反模板自检清单】12 条**：新页面/改动必须先过清单。

### 2026-08-09 AGENTS.md：补入"开发简报"永久约定（用户要求保留的习惯）

读取 prompts/ 全部文件与 `项目简报\Tsumugi\` 的讨论简报后，确认：仓库根目录没有
AGENTS.md，真正的 agent 指令文件是 `prompts/AGENTS.md`，且其中**没有**任何"开发简报"
书写提示词，同时项目定位仍停留在早期"个人文章/资料语义搜索工具"。

按用户明确要求（"无论如何修改 agent.md，开发简报书写这一习惯必须保留；如需修改
agent.md，必须保留书写开发简报这一提示词"）：

- 在 `prompts/AGENTS.md` 协作模式之后新增【开发简报（永久约定，重写本文件时不得
  删除本节）】：① 每次修改文件都要在 `开发流程简报.md` 追加变更记录；② 产品方向/
  讨论简报存于 `E:\program\MyProject\项目简报\Tsumugi\`（v1.0 / v0.2 / 现状校验附录
  等），是后续开发提示词的基准，与逐条变更记录是两类东西；③ 含中文的简报/文档一律
  用编辑工具操作，禁止 Shell 重写；④ 本节为红线，今后重写 AGENTS.md 不得删除或弱化。
- 顺带修复 `开发流程简报.md` 一处历史编码损坏（"newestCollectionAt" 丢了首字母 n）。

另：`prompts/AGENTS.md` 的项目定位与 `AGENTS.md` 规则（协作模式 2）提到的"开发流程
简报"均已被本期确认；项目定位部分与 v1.1 的对齐属 00 号提示词的任务，本轮未执行。

### 2026-08-09 产品定位重构：项目基准变更为 v1.1（00 号提示词执行）

执行 prompts/00_基准文档校准.md，本轮不写功能代码，只做基准合并与文档校准：

- **产出 `docs/product-brief-v1.1.md`**：合并《最终项目开发简报 v1.0》与《v1.0 现状
  校验附录》的四处修正——① 第二十节 Connector 现状补齐（Bangumi OAuth 批量导入 /
  萌娘百科 / VNDB / 声明式自定义源 / 每源可选出站代理）+ Backend/Frontend/Engineering
  遗漏项；② 第九节检索台架构图标注"世界检索与我的检索仍是两套独立入口，统一检索台是
  目标状态"；③ 第十五节真书架标注与 ADR 0019（书脊哈希配色）/ADR 0037（hover 取色）
  的关系，作为增量升级；④ 第二十五节修正"插件沙箱"歧义（插件系统已完成，仅不做沙箱
  隔离）。其余章节原样沿用。路径放仓库 `docs/` 而非 `项目简报\Tsumugi\`：因为 01/02/03
  号提示词与 AGENTS.md 直接引用它作为可执行基准；原始讨论稿保留在 `项目简报\Tsumugi\`。
- **`prompts/AGENTS.md` 项目定位对齐 v1.1**：新定位"开源、可自定义的二次元私人图书馆：
  收藏作品，也收藏与作品相遇时留下的自己"；写入核心原则（Work 是核心对象、Book 是 UI
  隐喻；世界轴/我的轴；Memory 独立容器、Experience 语义聚合、Timeline 是查看方式；
  AI/RAG 为可选扩展、Core 无 AI 也完整；UI 为图书馆空间隐喻；开发阶段规划以 v1.1 的
  Phase A→G 为准）；并写明基准文档与开发提示词来源（docs/product-brief-v1.1.md +
  prompts/ 文件夹 + 项目简报）。**保留**了上一轮新增的【开发简报】永久约定节（红线）。
- **一句话确认**：后续 01/02/03 号提示词执行时，一律以 v1.1 + 更新后的 AGENTS.md 为
  基准，不再引用 v1.0 原文。

### 2026-08-09 Phase A：Memory Core 记忆核心数据结构（01 号提示词）

详见 docs/decisions/0041-memory-core.md。目标：让"作品与我的联系"真正成为可存储的
数据。本轮只做数据层 + 两个接口，**不做时间轴/记忆回廊/新素材类型**（Phase B/C/D）。

- **models.py 新增 `Memory` 表**：id / item_id(FK) / source_type（本轮=review，预留
  text/image/collection/milestone，**不做白名单硬校验**）/ source_ref（多态引用，
  **不设外键**，review 时=Review.id）/ occurred_at（记忆发生时间，review=Review.created_at，
  与入库时间分离）/ summary（冗余展示摘要）/ created_at；`Item.memories` 关系带
  cascade="all, delete-orphan"（删作品级联清 Memory）。新表由启动时 `create_all` 自动
  创建（旧库无需 ALTER）。
- **app/memories.py（新增）**：`_review_summary`（标题优先，否则内容首行截断 40 字+…）、
  `ensure_review_memory`（幂等）、`sync_review_memory`（仅更新已存在的记录）、
  `delete_review_memory`、`list_item_memories`（occurred_at 倒序）、`query_memories`
  （item/时间范围/分页；支持 ISO 时间/纯日期/**年份**——年份作 end 按次年 1 月 1 日
  exclusive，便于"按 2023/2024/2025"筛选，Phase C 用）。
- **reviews.py 挂接（同一事务）**：创建 flush 后 `ensure_review_memory`；编辑提交前
  `sync_review_memory`（摘要随标题/内容变化同步，occurred_at 不变）；删除先删 Memory
  再删 Review（不留孤儿）。embedding 失败回滚时 Memory 一并回滚。
- **schemas/routes**：`MemoryOut`；`GET /items/{item_id}/memories`（Phase B 时间轴）、
  `GET /memories`（全局筛选，Phase C 记忆回廊）。
- **关键决策**：① Memory 不参与独立 RAG（v1.1：语义容器，检索仍走底层 Review 的
  source_type=review，检索层不感知 Memory）；② 加冗余 summary 字段（避免列表 N+1
  实时关联长正文）；③ 不走 create_review 的 Review（Bangumi 导入的状态/评分记录）编辑
  时不凭空造 Memory——那是收藏同步，Collection Memory 属 Phase D；④ **存量 Review 不
  回填 Memory**（本轮数据层，回填留待 Phase B 前单独任务，避免误以为漏实现）。
- 测试：pytest **403 passed**（+17 test_memories.py：迁移建表/自动生成/摘要标题与截断/
  编辑同步/meta-only 不变/无 Memory 编辑不造/删除级联/embedding 失败同事务回滚/按
  item/时间范围/年份/分页/API CRUD 流程），覆盖率 86.87%。
- **实测（真实后端）**：对 item 63 建书评（title=【PhaseA实测】）→ Memory 自动生成
  （summary=标题、occurred_at=书评创建时间）；改内容→摘要保持标题（标题优先，正确）；
  改标题→摘要同步、occurred_at 不变；删书评→Memory 级联删除，库回 0、integrity ok。
  测试数据已清理。

### 2026-08-09 Phase B：作品记忆时间轴 + 详情页"世界/我的"双栏（02 号提示词）

详见 docs/decisions/0042-memory-timeline.md。目标（v1.1）：第一次真正让用户看到
"这部作品在我这里留下了什么"。本轮只做详情页时间轴，**不做记忆回廊页面（Phase C）**。

- **双栏结构**：ItemDetailPanel（主从视图"从"面板）改为上下分区——上半"外部世界 ·
  世界如何描述它"（基本信息/简介/标签/角色，原内容原样），下半"我的记录 · 我如何理解它"
  （细分隔线 + Memory 时间轴）。选上下分区而非左右分栏：右栏宽度有限，左右分栏会挤压
  InfoTable/标签/时间轴可读性；两区各用 11px 小节标题 + `--accent`/`--text-secondary`，
  复用 InfoTable/TagCapsule/四套主题 token，无新视觉风格（ADR 0020）。
- **MemoryTimeline 组件（新增）**：调 Phase A 的 `GET /items/{id}/memories`；每条显示
  发生日期 + 摘要（Phase A 的 summary 字段）；时间轴**正序（旧→新）**——"回望"这段关系
  的编年史，最能回答"我的观点有没有变化"（API 返回倒序，组件内排序）；`--panel-border`
  细竖线 + `--accent` 圆点，编辑感克制。
- **点击跳转**：`source_type=review` 的记忆 → 轻量**只读弹层**展示完整书评（标题/评分/
  状态/日期 + `renderMarkdown` + `.doc` 样式）；不用 Review Studio 全屏编辑器（主从浏览
  是快速翻阅场景，避免打断）；未来 text/image/collection 来源默认不可点（组件已按
  source_type 分支）。
- **空状态**：无记忆时引导"还没有值得被记住的时刻。写下第一篇书评，这里会出现属于
  你们的记忆。"
- **存量 Review → Memory 回填（本轮补做）**：`memories.backfill_reviews(engine)` 启动时
  调用，幂等（只为无 Memory 的 Review 补建）；理由——若不回填，用户已有 57 篇书评在时间轴
  全是空，"这部作品在我这里留下了什么"对存量数据不成立，与 Phase B 目标直接冲突
  （ADR 0041 已注明"Phase B 前单独任务"）。
- 测试：vitest **185 passed**（+8 memory-timeline：正序渲染/点击弹层+markdown/空状态/
  非 review 不可点/itemId 切换/双栏结构/空库详情空状态/四主题兼容）；build 通过；
  pytest **406 passed**（+3 回填），覆盖率 86.87%。
- **实测（真实 App + headless Chrome CDP，主从视图）**：真实作品（空之境界·痛觉残留）
  详情页渲染双栏——外部世界 + 我的记录；时间轴正序 3 条（从 Bangumi 导入→初见的感想→
  重看之后的笔记）；点击"初见的感想"弹出只读书评全文（markdown 已渲染："第一次看空之
  境界，很喜欢这种安静的氛围…"）；命运石之门（无记忆）显示空状态。截图
  phaseb_timeline/modal/empty.png。实测用的 2 篇临时书评已删除（Memory 级联清理），
  库回 57 条真实 Memory（回填产物，幂等保留），integrity ok。

### 2026-08-09 Phase C：记忆回廊基础版（03 号提示词，仅自由查询）

详见 docs/decisions/0043-memory-gallery.md。全局记忆浏览页 + 入口，只做自由查询，
**不做**意外重逢（Phase E）/轻量记录（Phase D）/作品泛化（Phase F）/RAG 语义检索。

- **展示形式选型**：按年份分组的**编年列表**（不用活跃度热力图网格）——热力图表达
  "活跃度频率"，回廊要表达"内容/记忆"（日期+作品+摘要+点击跳转）；空间隐喻 = 回廊从
  "现在"往回走，整体时间倒序、按年份分组（年标签如书档，`--accent` 字+细分隔线+计数），
  组内倒序；`--panel-border` 细竖线 + `--accent` 圆点，与 Phase B 作品时间轴同一视觉语言。
- **筛选**：年份（数据中实际存在的年份 chip，点选/再点取消）+ 作品（下拉，全部作品或
  出现记忆的作品）。**本轮不做**标签/角色/时间段/记录类型：Memory 无标签字段、按标签筛
  需后端 join（属后端改动），时间段被年份覆盖，留待后续按需扩展 `GET /memories` 参数。
- **数据与性能**：一次拉全量（`GET /memories?limit=500`）后客户端筛选，个人库体量下
  零请求延迟；后端已支持 server 侧 item_id/start/end 分页，量大后迁移。
- **入口**：主导航新增"记忆回廊"图标键（`SORTABLE_KEYS` 可排序，旧 localStorage
  导航序自动补到末尾）+ 命令面板「打开记忆回廊」（keywords：记忆/回廊/memory/回忆/时间轴）。
- **点击条目** → 跳到对应作品的主从详情（`setSection("library"); setLibView("list")`）。
- **空状态**：完全无记忆给引导；有数据但筛选空分别提示"还没有 N 年的记忆 / 这个作品
  还没有留下记忆"。
- 测试：vitest **193 passed**（+8 memory-gallery：年份分组渲染/点击跳转/年份筛选与取消/
  作品筛选/无记忆空状态/命令面板动作项/主导航入口切换）；build 通过。
- **实测（真实 App + headless Chrome CDP）**：临时插入 2 条带【回廊实测·YYYY】标签的
  旧年份记忆（测后已删除）——总览分 2026:57 / 2024:1 / 2022:1 三个年头组；点 2024 chip
  → 只剩该年 1 条（2026/2022 全部消失）；作品下拉选 item 3 → 只剩该作品 2 条（从
  Bangumi 导入 + 2024 实测条）。截图 phasec_overview/year2024/work.png。库已回 57 条
  真实 Memory，integrity ok。

### 2026-08-09 【里程碑·特别记录】产品定位重构 + Memory 三阶段（00→01→02→03）整体复盘

> 本轮不是一次普通功能迭代，而是 Tsumugi 的**产品定位重构**：从"二次元数据库 +
> RAG 工具"转向"二次元私人图书馆：**收藏作品，也收藏与作品相遇时留下的自己**"。
> 以下把 00~03 四个阶段作为一个整体记录，方便未来回看这一次转折。

**一、为什么重构（背景）**

此前多轮迭代（视觉打磨/交互细节/命令面板/声优图谱/氛围色/猫娘人格）都是在给一个
"没有情感内核的骨架"做表层修饰。v0.1/v0.2 讨论确认了根本问题：Tsumugi 真正的差异化
不在"世界轴"（作品本身资料，做得再精致也只是"更好的二次元数据库"），而在**"我的轴"**
（作品与我之间发生过什么）。最终由《最终项目开发简报 v1.0》+《现状校验附录》定稿，
本次 00 号提示词将其校准为可执行的 **v1.1 基准**。

**二、新的定位与核心概念（v1.1 基准）**

- 一句话定位：**开源、可自定义的二次元私人图书馆：收藏作品，也收藏与作品相遇时留下的自己**；
- **Work 是核心馆藏对象，Book/Shelf 是 UI 隐喻**（数据模型不叫"书"）；
- **世界轴 + 我的轴**：世界轴描述作品本身，我的轴描述"作品在我这里留下了什么"；
  Core 价值 = **作品 × 我的经历**；
- **Memory 是独立容器**（不是 Review 的视图）、**Experience 是语义聚合不建表**、
  **Timeline 是查看方式不是数据实体**；
- **AI/RAG 是可选扩展，不是 Core**（Core 无 AI 也完整），AI 不得虚构记忆、必须区分
  "我的资料/外部资料"；
- UI 是图书馆的空间隐喻：检索台/书库/人物档案馆/关系厅/记忆回廊/管理室。

**三、本次落地的四个阶段与产物**

| 阶段 | 提示词 | 交付 | 测试 |
| --- | --- | --- | --- |
| 00 基准校准 | prompts/00 | `docs/product-brief-v1.1.md`（v1.0+四处修正）、AGENTS.md 定位对齐（保留开发简报永久约定红线） | — |
| Phase A | prompts/01 | `Memory` 表 + Review 生命周期自动同步 Memory + `/items/{id}/memories` + `/memories` 全局查询 | pytest 406 |
| Phase B | prompts/02 | 详情页"外部世界/我的记录"双栏 + 作品记忆时间轴（正序、只读弹层、空状态）+ 存量 Review→Memory 回填 | vitest 185 |
| Phase C | prompts/03 | 记忆回廊页面（年组编年列表、年份/作品筛选、空状态）+ 主导航/命令面板入口 | vitest 193 |

**四、关键决策（跨阶段值得记住的取舍）**

1. **Memory 不参与独立 RAG**（v1.1：语义容器，检索仍走底层 Review 的 source_type=review）；
2. **summary 冗余展示字段**：避免时间轴/回廊列表逐条关联长正文的 N+1；
3. **occurred_at（记忆发生时间）与 created_at 分离**：记忆按"发生时刻"排序，是"记忆"
   而非"日志"的本质；
4. **存量 Review→Memory 回填**：不填则旧书评在时间轴全是空，"留下了什么"对存量数据
   不成立——Phase B 前补做（幂等，启动时调用）；
5. **时间轴/回廊排序**：作品时间轴正序（编年史式回望），回廊整体倒序（从"现在"往回走）；
6. **回廊展示选编年列表而非热力图**：热力图表达"活跃度"，回廊表达"内容"；
7. **筛选取舍**：本轮只做年份+作品，标签/角色筛需后端 join（Memory 无标签字段），留待扩展。

**五、当前真实数据状态（实测后）**

- 库内 **57 条真实 Memory**（= 57 篇存量书评，回填产物，幂等保留）；全部 2026 年、
  各作品 1 条；
- 测试过程产生的临时书评/临时旧年份记忆**全部已清理**，`integrity_check = ok`；
- 全部实测均用**真实 App + headless Chrome CDP** 截图留档（phaseb/phasec 系列）。

**六、测试基线（重构后）**

- pytest **406 passed**（覆盖率 86.87%）；vitest **193 passed**；`npm run build` 通过。

**七、本轮之后（尚未排期的后续批次）**

按 03 号提示词，Phase D（轻量记录/截图/收藏时刻等新 Memory 素材）、Phase E（意外重逢/
"往年今日"由猫娘唤起，需先确定与 ADR 0040 触发优先级的整合）、Phase F（作品模型泛化
anime/manga/game…，需先审查现有 Item 模型）、Phase G（Personal RAG）**均需用户实际
使用体验反馈后再单独展开**，不属于本次范围。

**八、一句话总结**

> Tsumugi 从"能搜能存资料的二次元数据库"，第一次变成了"**会记得你和每部作品之间
> 发生过什么**的私人图书馆"——作品时间轴能回望，记忆回廊能按年找回当时的自己。

### 2026-08-09 AGENT_UI.md 版本核对与合并：两条修订线统一为 v2.2

**核实结果（六处逐条）**：

| # | v2.1 修正点 | 现状 |
| --- | --- | --- |
| ① | "记忆长廊"统一改名"记忆回廊" | **已存在**（v2.1 全文已用"记忆回廊"，改名说明保留） |
| ② | 新增"定位与执行范围"说明 | **已存在**（文首【重要：本文档的定位与执行范围】） |
| ③ | 真书架标注为对 ADR 0019 的有意推翻 | **已存在**（【图书馆】节 v2.1 重要标注） |
| ④ | 区分"追番状态徽章"与"数据来源徽标 TagCapsule" | **已存在**（Archive Grid 节 v2.1 澄清） |
| ⑤ | AI 问答改版拆分（文献式呈现 / 检索会话写入记忆回廊单独排期） | **已存在**（【AI】节 v2.1 重要标注） |
| ⑥ | 字体 token `--font-heading`/`--font-body` 纳入 ADR 0020 | **已存在**（排版系统节 v2.1 补充） |

结论：仓库现存 UI 文档（`prompts/AGENT_UI_v2.1.md`）**已包含全部六处 v2.1 修正**，
无需补写缺失项；本次任务是确认 + 合并为唯一权威版本。

**合并动作**：
- 以 v2.1 文档为基础（v1→v2 审查内容原样保留，未删减重写），版本号升为 **v2.2**，
  文首写明"两条修订线合并后的唯一权威版本"；
- 消除"记忆长廊"旧译名残留：文档内三处改名说明改写为不直接出现该词（grep 后 0 命中）；
  顺带修正"开发流程简报.md"早期 v2 条目里 2 处"记忆长廊"→"记忆回廊"；
- 文末追加【v2.2 合并说明】（注明 v1→v2 agent 审查 + v2→v2.1 一致性修正两条线合并、
  v2.1 六处已全部落地、无删减）；
- 文件名统一为 `prompts/AGENT_UI.md`（删除 `AGENT_UI_v2.1.md`，避免版本号在文件名与
  文首不一致），并更新 `prompts/AGENTS.md` 第 23 行引用为 `prompts/AGENT_UI.md`。

### 2026-08-09 Phase E：往年今日（意外重逢，猫娘唤起）

详见 docs/decisions/0044-on-this-day.md。打开应用（首页神殿）时，若"去年今天"或更早
某年的今天真实存在一条 Memory，猫娘用一句话具体唤起；未命中保持日常问候。

- **后端**：`memories.query_on_this_day(db, month, day, max_year)`——`strftime('%m-%d')`
  严格同月同日 + 年份过滤；`/memories` 路由新增 `month/day/max_year` 参数（有 month+day
  才切往年今日查询，否则走 Phase C 全局查询，兼容）。
- **坑位（SQLite）**：`strftime('%Y', occurred_at) < 2026` 恒为 false——strftime 返回
  TEXT，SQLite 比较规则数字 < 文本；必须 `cast(strftime('%Y',...) AS INTEGER) < max_year`
  （substr 同理）。已修 + 有针对性测试。
- **选取规则**：后端按年份倒序、同日 occurred_at 倒序返回，前端取首条 = **最近年份
  （去年优先）+ 该日最新一条**。理由：v1.1 默认画面就是"去年的今天"，最近年份最可感；
  否决"内容更完整"（summary 短、难定义完整）。
- **触发优先级**：`matchScene` 顶部新增 `on_this_day`（`ctx.onThisDay` 命中即返回），
  插入**最高位**（高于想念）——触发条件天然稀疏（一年大多日子不命中），一旦命中即当天
  最有分量；未命中 `null` 走原逻辑零影响。
- **台词**：`MASCOT_LINES.on_this_day` 5 句模板（喵～ 1/5），占位符
  `{yearsLabel}/{title}/{summary}` 由真实 Memory 填充（"2年前的今天，你为《XX》写下了
  「……」"具体唤起）；`yearsAgoLabel` 输出"去年的今天 / N年前的今天"。
- **交互**：命中时台词上方加 `◇ 往年今日` 小标签（`--accent`），台词可点击；把 Phase B
  只读弹层**提取为共享组件 `MemoryReviewModal`**（MemoryTimeline + HomeShrine 复用），
  点击打开对应书评全文。
- **闰年边界**：严格同月同日——2-29 记忆只在 2-29 命中（每 4 年自然重逢），2-28 不
  前移匹配（避免"前天/昨天"式模糊）。
- 测试：pytest **410 passed**（+4 严格月日+年份过滤排序/max_year 缺省含今年/闰日严格/
  API 与 Phase C 兼容），覆盖率 86.91%；vitest **198 passed**（+5 matchScene 最高优先级
  与未命中走原逻辑/selectLine 填充年份标题摘要与兜底/yearsAgoLabel/HomeShrine 往年今日
  可点击+弹层/未命中日常问候）；build 通过。
- **实测（真实 App + headless Chrome CDP）**：真实库无往年同日记录（全 2026），按套路
  临时插入 2024-08-10 记忆（指向 item62 魔法少女小圆 + 真实书评 58）→ 首页显示
  `◇ 往年今日` + 台词"2年前的今天，主人为《魔法少女小圆》留下了「…」。"；点击弹出该书评
  markdown 全文。  截图 phasee_line/modal.png。测试数据已删除（库回 58 条真实 Memory），
  integrity ok。

### 2026-08-10 P8 实现：工程收尾（ADR 0052，venv 瘦身 / 文档归档 / Electron 评估）

详见 docs/decisions/0052-engineering-wrapup.md。

- **venv 瘦身（完成）**：卸载 chromadb 传递依赖 `kubernetes`（约 75MB）——已验证
  `import chromadb` 不加载它（仅分布式集群模式用）。卸载后 chromadb/后端/pytest 420
  全通过；venv 1281MB→**1205MB**。**取舍**：`pip check` 会报 chromadb 缺 kubernetes
  （pip 视角），Tsumugi 不用集群模式，风险可控，可 `pip install kubernetes` 恢复。
- **文档归档（完成）**：`prompts/`（00-26 设计文档 + AGENTS/AGENT_UI，29 个文件）已
  整体归档到 `E:\program\MyProject\项目简报\Tsumugi\prompts-archive\`（仓库外，防
  gitignore 后丢失）；后续 prompt 更新建议定期刷新。
- **Electron 打包 embedding（评估，暂不修）**：ADR 0012 止损点仍成立（transformers
  `_LazyModule` vs PyInstaller PYZ）；不重打包（高不确定性+长时间构建），记三条可选
  修复路径：①升级 transformers 重打包（改动小需实测）；②换 ONNX embedding（同时去掉
  torch ~500MB，根治）；③换打包器（最后考虑）。Docker 仍是主分发路径。
- 测试：pytest **420 passed**（卸载后全量回归），无前后端代码改动。

### 2026-08-10 默认主题暖橙化 + 排版自信升级 + 档案编号系统（ADR 0053）

详见 docs/decisions/0053-warm-accent-typography-archive.md。在 ADR 0020 token 体系内
完成（**不新增第五套主题**），方向借鉴 Hermes 的"排版自信 + 编号档案感"两点优点，
**不照搬**其黑白工程语言。

- **默认主题 accent 暖橙化**：`--accent: #b25b36`（暖棕橙/赤陶土调，hsl(18°,53%,45%)，
  略带灰调不鲜艳；浅底对比 ≈4.7:1 达 AA）、`--accent-hover: #9c4c2c`、
  `--accent-soft: #f9ede5`。只改颜色变量，间距/圆角/阴影/密度结构 token 不动；
  依赖 accent 的 进度条渐变 / 书脊色相旋转基准（蓝 221°→暖橙 18°）随之暖色化；
  `--ambient-alpha` 与封面取色不依赖 accent，自动适配。其余三套主题**不动**（测试锁定色相）。
- **排版自信升级**：新增 `--font-heading`（衬线栈）token（对齐 AGENT_UI v2.2 ⑥）；
  只提升关键标题——详情页标题 16→22px 衬线 semibold、记忆回廊页标题 15→20px 衬线、
  回廊年份 11px 小字 → **ArchiveNo lg 30px 衬线大数字**、首页猫娘问候 12→13px 衬线。
  正文层级不动。
- **档案编号系统**：`ui.jsx` 新增 `ArchiveNo`（`§` 标记 + 等宽数字，sm/lg，token 色，
  可复用组件）；应用到 记忆回廊年份（`§ 2026`）、Memory 时间轴条目（`§01 §02…`）、
  Review Studio Records（`§01…`）——"被认真编目的私人档案"感。
- 测试：vitest **216 passed**（+4：默认暖橙且三套色相锁定、index.css :root hex +
  --font-heading + 氛围 alpha 不变、书脊色相基准暖调、ArchiveNo 渲染/lg、gallery 年份
  ArchiveNo、timeline §01/§02）；build 通过。
- **实测（真实 App + CDP）**：accent=`hsl(18,53%,45%)` 暖橙；详情标题 22px 衬线
  （Georgia/Songti）；回廊年份 `§2026` 大号档案编号。截图 accent_home/detail/gallery.png。
  （坑位：Windows Hyper-V 动态端口排除 9142-9866，CDP 调试端口需避开，本次用 9900。）

### 2026-08-10 图书馆网格视图结构性重设计（Archive Grid 范本，ADR 0054）

详见 docs/decisions/0054-archive-grid-card.md。先把一个页面做成"重构到底长什么样"的
范本：网格卡片从"模板式卡片"改为"档案编目卡"（**非配色任务，是结构改动**）。

- **偏差核实**：任务前提"上一轮新增了第四套主题"——经核实 themes.js `THEMES` 仍是 4 套、
  `ACCENTS.default` 已是暖橙 #b25b36，即上一轮就是**替换默认主题强调色**（有测试锁色相），
  第一步无额外动作；本轮核心是网格结构重构。
- **来源徽标**：封面左上角 accent 色块胶囊 → 信息区**编目行文字标注**（`NO. 0003   BANGUMI`，
  大写字距）。论证：档案馆/博物馆给藏品贴标签是文字标注不覆盖藏品本身；解决截图"徽章
  压在封面上"。
- **档案编号**：每卡片 `NO. {id补零4位}`（等宽、accent、字距），标题上方编目行 + 细分隔线
  ——"被认真编目的私人收藏"直接证据。
- **比例排布**：容器圆角 16→6px（近直角，遵循"面板近直角"），封面保留 `--radius-sm` 中等
  圆角（馆藏实物）；标题 13px 无衬线 → 15px 衬线 semibold + 留白；网格保持对齐（秩序感），
  "非机械均匀"落在卡片内部层级节奏 + 编目分隔。
- **空态占位**：灰渐变方块 → **档案占位卡**（书脊轮廓线 + 内细虚线框 + `藏 · xxxx` 编目号），
  不再是无内容灰块。
- 实现：新增 `ArchiveCard.jsx`（组件 + `catalogNo` + `ArchivePlaceholder`），grid 换用；
  CSS `.archive-card*/.archive-ph*` 走 token；不改书架/分组/配色体系。
- 测试：vitest **225 passed**（+9 archive-card：编目号补零/编目行且封面无徽章/衬线标题/
  占位非灰块/有封面/交互/选择模式/本地不显来源）；build 通过。
- **实测（真机前后对比，git stash 同一 app 实拍）**：BEFORE=旧 `.desk-card` 网格（来源徽章
  盖封面、  无编号、大圆角、灰块占位）；AFTER=`.archive-card`×63 + 档案占位×1（编目号/来源
  文字标注/近直角/衬线标题/书脊轮廓占位）。截图 grid_BEFORE.png / grid_AFTER.png。

### 2026-08-10 网格三个探索性视觉方向（ADR 0055，探索性任务，非正式交付）

不做渐进优化，做三个"看起来像三个不同产品"的独立方向，供用户真实浏览器对比挑选。
允许偏离 ADR 0020 token（探索阶段）；选中后再做 token 融入。

- **A 深夜书房**：近黑背景，封面取色氛围光为主视觉（径向光晕大胆铺开，不是 hover 微光），
  封面像被台灯照亮的书（强明暗、深阴影、冷光标题）。
- **B 编目抽屉**：纸感索书卡版式（暖纸底 + 档卡横线 + 等宽/衬线强对比），粗重
  `NO.xxxx` 编目号，密集元数据行（编目号/来源/记录数）。
- **C 展览橱窗**：美术馆式大留白，不规则展示单元（部分卡片横跨两列、横版封面），
  `№ xxxx` 编号，少而精、非对称。
- **开关/路径**：网格工具栏"视觉方向"切换（经典档案卡 / A / B / C）+ URL
  `?concept=a|b|c|classic` 直达 + localStorage 记忆；非正式主题。
- 实现：`GridConcepts.jsx`（三组件 + 开关 + parseConcept + 封面取色钩子），CSS 各自
  作用域（.concept-a/b/c）；经典仍为默认。
- 测试：vitest **231 passed**（+6 grid-concepts：三方向各渲染真实数据/占位、开关 4 项
  与 onChange、parseConcept 校验）；build 通过。
- **实测（真实数据）**：三方向各 63 卡片 / 62 封面真实渲染，截图
  concept_A.png（848KB，暗色光晕）/ concept_B.png（461KB，纸感）/ concept_C.png
  （548KB，留白）——文件大小差异即三者视觉差异的佐证。

### 2026-08-10 编目抽屉正式落地为默认设计系统（ADR 0056，全站）

方向 B"编目抽屉"（探索版 0055）转正为 Tsumugi 默认设计系统，覆盖全站。

- **主题范围决策**：**版式与配色分离**——编目号/索书卡字段/近直角/字体是版式，可独立于
  配色；故结构 token 全站统一（四套主题共享新版式），**默认主题色板替换为米黄纸感**
  （原经典白被取代），薄荷浅蓝/樱花粉/深色三套**保留各自色板**仅共享版式（深色用纸感
  会失去夜间可读性）。
- **纸感色板**（token 框架内）：--bg #f4efe3 / --panel #faf5ea / --panel-border #dcd0b6 /
  surface-0 #efe8d8 / 1 #faf5ea / 2 #e9dfca / text #2b241a + #7a6d57 / accent 暖橙延续；
  radius 近直角（lg 10 / sm 4）；新增 --font-mono（编目号等宽）。
- **全站推广**：网格 ArchiveCard 转正（编目行 NO.xxxx · 来源）；书架层板纸感 + **抽书预览
  加 NO. 编号**（书脊 10-28px 太窄不放编号的取舍）；详情页标题下 **NO. 编号** + InfoTable
  本就是索书卡字段罗列；记忆回廊/时间轴 § 编号继承；**首页神殿不硬加编号**（仪式空间
  隐喻，避免破坏参拜感，只继承纸感+暖橙+衬线）；命令面板/Toast/批量栏跟随 token 不加
  内容型编号。
- **保留机制**：封面取色氛围/猫娘/真书架逻辑不变，视觉随 token。
- 测试：vitest **233 passed**（+2 纸感色板/分层/--font-mono/近直角 token 断言、详情页
  编目号、书架预览编目号）；build 通过。
- **实测（真实 App + CDP，全站 5 页）**：首页神殿/网格/书架/详情/记忆回廊均纸感渲染，
  GRID_BG=#f4efe3、详情 NO. 0043。截图 ds_home/grid/shelf/detail/gallery.png。
- **待办（简报记录）**：薄荷/樱花/深色三套的"编目化配色打磨"（当前共享版式+各自色板；
  若需更贴合编目抽屉的色板微调，作为后续任务）。

### 2026-08-10 应用外壳三个空间结构探索（ADR 0057，探索性任务，非正式交付）

沿用编目抽屉配色/字体/编号（0056），探索**骨架结构本身**（用户反馈三栏骨架仍是 SaaS
工具形状，换皮肤没换骨架）。三方向真实数据渲染，右下角开关切换。

- **A 卡片抽屉导航**：左侧=一排卡片目录柜抽屉（顶部编号 tab + 拉出式选中态）+ 抽屉
  内容索引卡；导航即"拉开哪一格目录柜"。
- **B 书脊索引栏**：左导航=一排立着的书脊（竖排衬线 + 编号），整条是"馆藏索引书脊"，
  选中立起；宽度不同于窄图标条。
- **C 非对称档案室**：不同页面进入不同"房间"（书库宽偏移/回廊居中窄廊/人物馆右留白/
  关系厅左偏移/设置居中窄卡），打破全站统一侧栏宽。
- **实现**：`AppShells.jsx`（三外壳 + ShellSwitcher + parseShell + 共用真实内容渲染器：
  书库 ArchiveCard 真实网格、检索台真实 searchMy、回廊/人物馆/关系厅复用现有组件）；
  DesktopView 顶部按 shell 分支渲染 + 右下角浮动开关；?shell=a|b|c 直达 + localStorage。
- 测试：vitest **238 passed**（+5 app-shells：三外壳 data-shell 不同结构、抽屉/书脊/房间
  布局、开关 4 项、parseShell 校验）；build 通过。
- **实测（真实数据）**：三外壳各 48 真实卡片渲染，截图 shell_A.png（862KB）/
  shell_B.png（1061KB）/ shell_C.png（964KB）——体积差异佐证结构差异。

### 2026-08-10 "Catalog Drawer" 全量需求对照审计 + 两处规格修正

外部以"专家前端工程师"视角提出的完整界面需求（索书卡系统：字体/圆角/米黄纸色板/四视图/
交互）——逐条对照发现**项目已全部实现**（ADR 0054-0056 + 各组件），故不重建，只做审计
与两处与需求精确规格不符的修正：

**需求 → 实现映射（审计）**
- 字体：衬线标题(--font-heading)+等宽编号(--font-mono)+无衬线正文 ✓
- 近直角：radius sm4/md8/lg10/xl14；主表面 6px（本轮）、内容对象 4px（本轮）✓
- 色板：#f4efe3/#faf5ea/#dcd0b6/surface 分层/#2b241a/#7a6d57/#b25b36 全套与需求一致 ✓
- 外壳：`h-screen flex flex-col overflow-hidden`（App.jsx）、header 固定、左栏 224px 可拖、
  中央 `flex-1 min-h-0 overflow-y-auto p-6` ✓
- View A 网格：ArchiveCard（NO.+来源元数据行/6px 主表面/15px 衬线标题/书脊轮廓占位 藏·编号）✓
- View B 书架：层板线/按主标签分架+层左标签/vertical-rl/厚度 10-28px/hover 上浮+抽书预览 ✓
- View C 神殿：鸟居 SVG/祭坛搜索/最近供奉纵深/猫娘台词/暗幕 0.55→0 仪式 1.2s ✓
- View D 工作室：不透明纸感底/720px+1.78+衬线/左参考面板(Overview/Records/Archive/Memories)/
  右编辑器+Markdown 工具栏+字号 14-20/`>`引用卡/rs-enter .28s+.closing .18s ✓
- 交互：批量选择(选中描边+等宽✓+底部栏)/右键视口钳制菜单/Ctrl+K 命令面板/Toast 3.2s ✓
- 无 backdrop-filter 模糊 ✓

**本轮两处修正（对照需求精确规格）**
1. 圆角方向：主表面(archive-card) 4px→**6px**、内容对象(封面 cover) 6px→**4px**（需求：
   主表面 6px、内容对象 4px）；
2. 书脊取书过渡：`.shelf-book` 0.18s→**0.3s**（需求：0.3s）。

- 测试：vitest **238 passed**、build 通过（纯 token/过渡微调，无行为回归）。

### 2026-08-10 Bookshelf 需求对照 + 五处规格补齐（ADR 0058）

外部"Bookshelf 组件"需求逐条对照：结构（层板/分组/标签/厚度/取书）已实现（ADR 0019/
0049）。与需求精确规格不一致/缺失的五处补齐：

1. **暖粉彩书脊**（需求：饱和 30-40%、明度 55-65%）：`spineColor` 收敛区间改为
   sat [0.30,0.40]、light [0.55,0.65]（原 0.32-0.5 / 0.4-0.58）；书脊文字改深色
   （var(--text)）、高光条/底带调整为适配浅粉彩（白字在 55-65% 明度上不可读）。
2. **目录卡标签**：分类标签从文字行改为**左侧目录卡标签**（`.shelf-label`：accent 左边条
   + 衬线标签 + 等宽册数），对应需求"catalog label capsule on the left edge"。
3. **取书浮起**：-5px → **-6px**，0.3s ease-out（需求精确规格）。
4. **层板线**：渐变方向改为 surface-1→surface-2（需求 to bottom 顶层更亮）。
5. **抽书预览加书评摘录**：悬停索书卡显示 NO. + 封面 + 标题 + 评分 + **最新非剧透书评
   内容首行 56 字摘录**（需求"review snippet"）。

- 未用 lucide-react：项目用自定义内联 SVG（手作图标是既有决策，不引入通用图标库）。
- 测试：vitest **240 passed**（+2 粉彩区间断言、预览书评摘录；书架标签/浮起类断言更新）；
  build 通过。
- 实测（真实数据）：书架目录卡标签"TV 22 册/剧场版 11 册…"、书脊 hsl(290,32%,65%) 粉彩
  命中带宽。  截图 shelf_pastel.png（预览卡由 vitest 覆盖，CDP 原生事件不触发 React 合成
  mouseenter）。

### 2026-08-10 主题/外壳定案：仅默认主题 + 经典三栏/非对称档案室（ADR 0059）

用户定案，将探索产物收敛为正式形态：

- **主题仅保留默认（编目抽屉纸感）**：`THEMES` 仅 `[{default,"编目抽屉"}]`、`ACCENTS`
  仅 default、移除 index.css 的 mint/sakura/dark 三块；旧 localStorage 主题名优雅回退
  default；命令面板「主题」组剩 1 项。
- **外壳保留 经典三栏 + C 非对称档案室**：移除探索方向 A（卡片抽屉）、B（书脊索引）
  组件与 CSS；开关仅两项（?shell=classic|c）；DesktopView 外壳分支只渲染 ShellC。
- 测试：vitest **234 passed**（主题改单套断言+断言无 mint/sakura/dark 块；外壳仅
  ShellC+2 项开关；命令面板主题组 1 项；master-detail 表面 token 适配纸感 :root；
  review-doc 渲染改默认）；build 通过。
-   实测：ShellC 正常渲染、右下角开关显示「经典三栏 / C 非对称档案室」。截图
  shellC_final.png。

### 2026-08-10 Home Shrine 需求对照 + 四处规格补齐

外部"神殿首屏"需求逐条对照：空间布局（居中/鸟居/祭坛/纵深供奉）、每日仪式（localStorage
日期门控/1.2s 暗幕/点击跳过）、猫娘台词均已实现（ADR 0039/0040）。与需求精确规格有出入的
四处补齐：

1. **帷幕**：颜色 `rgba(8,10,18,0.55)` → **`rgba(30,20,15,0.55)`**（暖暗贴合纸感）、
   过渡 `1s ease` → **`1.2s cubic-bezier(0.22,1,0.36,1)`**（需求精确规格）。
2. **供奉两侧淡出**：`opacity 1 - dist*0.16` → **`1 - dist*0.25`**（最外侧 ≈0.5，
   需求"flanking covers at 0.5 opacity"）。
3. **鸟居对比**：accent 0.4 → **`--text-secondary` 0.3**（更低对比，贴合纸感底色）。
4. **祭坛搜索栏衬线占位**：新增 `.shrine input::placeholder { font-family: var(--font-heading) }`
   （需求"serif placeholder"）。

- 保留：每日首次完整仪式 + 同日跳过 + 点击/按键跳过逻辑不变；猫娘 13px serif（0053
  "排版自信"定案，保持，未退回 12px）。
- 测试：vitest **234 passed**（home-shrine 9 例全兼容，无行为回归）；build 通过。

### 2026-08-10 Ambient 取色需求对照 + 三处规格补齐

外部"canvas 主色提取 + 详情面板氛围光"需求逐条对照：32×32 Canvas 取色、鲜明度校验
（近灰/黑白降级）、模块级 Map 缓存、详情面板双层 box-shadow 氛围光 + 0.5s 过渡、卡片
hover 取色均已实现（ADR 0034/0037，ambient.js / ItemDetailPanel / CoverAmbient）。与
需求精确规格有出入的三处补齐：

1. **缓存上限** 200 → **100**（需求精确；超出丢最早，防无限增长）。
2. **新增 `extractDominantColor(src)` 包装**：解析为 `"rgb(r,g,b)"` 字符串（需求 API），
   复用底层 extractPalette 缓存与鲜明度校验；对现有组件零影响（纯增量）。
3. **氛围色强度**：默认 `--ambient-alpha 0.16 → **0.14**`（需求：浅色主题 0.14 极柔和；
   深色 0.28 因 0059 已无深色主题，分支由 ambient-alpha token 将来扩展）。

- 已具备且保留：双层 radial box-shadow 光晕、`transition: box-shadow 0.5s ease-in-out`、
  CoverAmbient hover 取色、`isVivid` 近灰/黑白降级回退主题表面色。
- 测试：vitest **235 passed**（+1 extractDominantColor 包装：底层缓存复用不受影响、jsdom
  解析 null 不抛错）；build 通过。

### 2026-08-10 On This Day 猫娘对话框需求对照 + 两处补齐

外部"On This Day 往年今日 Mascot Dialog"需求逐条对照：日期匹配引擎（历史同月同日、排除当年、
取最近年）后端 `query_on_this_day`（ADR 0044）+ pytest 闰年 2/29 用例已覆盖；`◇ 往年今日`
赤陶色小签 + `{yearsLabel}前的今天，你为《{itemTitle}》写下了「{summary}」` 模板
（mascot.js on_this_day[0]，含主人称呼）已实现；点击弹层 MemoryReviewModal（Georgia/宋体
doc 排版 + 纵向留白 + 关闭钮）已实现；无命中回落标准时区问候已实现。两处补齐：

1. **高字距**：重逢行 `letterSpacing: 0.02em`（tsm-heading 0.01em 之上，贴合"高字距"要求）。
2. **hover 可交互**：`.shrine-mascot-reunion`（index.css）——hover 时 `--accent` 45% 细描边
   + outline-offset 6px + 手型光标。

- 不适用项：外部需求要求"模拟日期开关（含闰年）"——真实产品走真实日期（后端查询），
  闰年/多匹配逻辑由 pytest（`test_on_this_day_recent_year` / `test_leap_day_strict`）覆盖，
  不引入 mock 开关污染运行时。
- 测试：vitest **235 passed**；build 通过。

### 2026-08-10 视觉方向收敛：仅保留编目抽屉（适配经典三栏 / 非对称档案室）【ADR 0060】

用户定案：**视觉方向仅保留「编目抽屉」**（ADR 0056 默认设计系统），并同时适配经典三栏与
C 非对称档案室两个外壳。将 ADR 0055 探索产物正式收敛：

1. **移除网格探索**：删 `GridConcepts.jsx`（A 深夜书房 / B 编目卡片抽屉 / C 展览橱窗 +
   `GridConceptSwitcher`/`parseConcept`）与 `grid-concepts.test.jsx`；DesktopView 移除工具栏
   「视觉方向」开关、`gridConcept` 状态、`?concept=` URL 直通、localStorage
   `tsumugi-grid-concept`；网格分支仅保留经典档案卡（ArchiveCard 编目行，即编目抽屉正式形态）。
2. **CSS 清理**：index.css 移除 ADR 0055 探索块（`.grid-concept-switch`、`.concept-a/b/c*`）。
   保留 ADR 0057 外壳块（ShellC 仍用，注释更新为"0059 起仅 classic + C"）。
3. **两外壳同向**：经典三栏书库 = 编目抽屉档案卡；ShellC 导航/房间沿用编目抽屉 token +
   ArchiveCard 卡片。无第二套视觉语言。
- 测试：vitest **229 passed**（删 6 例 grid-concepts，其余不依赖概念开关）；build 通过。
- ADR 0060 记录。

### 2026-08-10 编目抽屉·索书卡正式化（ADR 0060 勘误）

用户反馈：网格变成"初始风格"（经典档案卡）。定案时用户选中的编目抽屉是 0055 探索版 B
**索书卡**（档卡横线 + 密集元数据行 + 暖纸内晕），ADR 0060 只留了经典档案卡、把 B 删了。
勘误并正式化：

- **ArchiveCard 升级为索书卡版式**：顶行等宽编目（NO.xxxx + 来源文字标注，非封面色块）+
  档卡横线（meta 下暖橙细分隔）→ 衬线标题 → 3/4 封面/档案占位 → **密集元数据行**
  （`─ 编目号 NNNN / ─ 来源 x / ─ 记录 N 条`，等宽 10px）→ 书评/删除。暖纸内晕
  （inset 0 0 46px 暖橙 4%）营造真实档案卡纸感。功能保留：选中态/换封面/右键/书评/删除。
- **两外壳同用**：经典三栏 + ShellC（非对称档案室）书库都渲染这张索书卡（ArchiveCard 是
  唯一网格卡，自动生效）；`--d-grid-min` 160→190px、compact 140→170px、`.shell-grid`
  minmax(150→190px,1fr) 为密集元数据行让出列宽。
- 测试：vitest **230 passed**（+1 索书卡元数据行断言；原编目号/来源/占位/交互断言全兼容，
  来源仍为文字标注非封面徽章）；build 通过。ADR 0060 已勘误（§四）。

### 2026-08-11 大风格主题层：编目抽屉·索书卡注册为首项（ADR 0061，设置可切换）

把「编目抽屉·索书卡」提升为**大风格主题**（视觉语言层）注册项，放入设置，建立后续切换
机制（与色彩主题分离，延续 ADR 0056"版式与配色分离"）：

- **themes.js**：新增 `STYLE_THEMES`（首项 `catalog-drawer`「编目抽屉·索书卡」+
  description）+ `loadStyleTheme()`（无持久化/未知值回退首项）+ `applyStyleTheme(key)`
  （写 `html[data-style-theme]` + 持久化 `tsumugi-style-theme`）。
- **App.jsx**：`styleTheme` 状态 + effect 应用 + 下发 props。
- **DesktopView 设置 → 外观**：新增「大风格主题」组（在色彩「主题」之上），当前仅一项，
  选中高亮；后续其它大风格主题在此切换。
- **index.css**：索书卡基础样式 = 首项大风格主题默认样式；注释标明后续以
  `html[data-style-theme="..."]` 作用域覆盖。
- 测试：vitest **232 passed**（+2 大风格主题注册/applyStyleTheme/loadStyleTheme 回退）；
  build 通过。ADR 0061 记录。

### 2026-08-11 索书卡样式正式收进大风格主题作用域（ADR 0061 补完）

用户定案「正式将探索版适配」= 把索书卡样式从"全局基础样式"正式收进大风格主题作用域，
成为真正可切换的大风格主题：

- **index.css**：`.archive-card*` / `.catalog-no` / `.archive-ph*` 全部索书卡规则前缀
  `html[data-style-theme="catalog-drawer"] `；注释标明后续大风格主题以各自作用域覆盖。
- **main.jsx**：首屏渲染前**同步** `applyStyleTheme(loadStyleTheme())` 写 `data-style-theme`，
  避免作用域未命中的首帧闪样（此前 App.jsx effect 在首帧后才应用）。
- 机制闭环：注册项（themes.js STYLE_THEMES）+ 首屏同步应用（main.jsx）+ 作用域样式
  （index.css）+ 设置切换（DesktopView 外观）+ 状态下发（App.jsx）。
- 测试：vitest **233 passed**（+1 作用域断言：索书卡规则已带作用域前缀、无裸
  `.archive-card {`）；build 通过。

### 2026-08-11 Phase D：完成时刻 milestone Memory 自动采集（ADR 0062）

经"当前阶段校准"（简报记录 + 文档/代码交叉验证 + 只读测试基线：pytest 420 / vitest 233）
确认 Core 健康、Memory 结构独立但真实库 58 条记忆 100% 来自 Review——下一步推进 Phase D
最小范围："状态迁移到看完"自动生成 milestone Memory，让 Memory 独立于 Review 在真实
使用中成立。

- **app/collections.py**：`set_collection` 检测真实状态迁移 `未看完 → 看完`（同一次看完
  重存幂等；看完→其它→再次看完 = 新完成事件可再生成）；新增 `create_completion_memory`：
  `source_type=milestone`、`source_ref=NULL`、`occurred_at=实际完成时间`、
  `summary="这一天，我把《{title}》看完了"`。**轻量创建不写向量**（沿用收藏时刻 0046
  模式，完成时刻不被可选 embedding 阻塞；语义检索由既有 `backfill_memory_vectors`
  ADR 0051 启动时幂等补，不新增 RAG 代码）。
- **触发边界**：仅用户 `PATCH /items/{id}/collection`；导入（on_collect）与启动回填
  （backfill_collections）不走 set_collection，历史 34 条"看完"不批量造记忆。
- **前端零改动**（时间轴已渲染里程碑徽标，P3）。无表变更、无既有数据修改。
- 测试：pytest **426 passed**（+6：看完迁移生成摘要/occurred_at、同状态重存幂等、
  看完↔其它↔看完=两条、非看完不触发、回填不触发、API 层验证）；vitest 233 全量回归；
  build 通过。
- **明确不做**（按用户约束）：历史回填、重新打开作品自动采集、往年今日对 milestone
  语义、Phase F / RAG / AI / Ollama、新增表或改既有数据。
- ADR 0062 记录。

### 2026-08-11 Memory Rediscovery · 小步①：Memory 类型情感权重呈现（ADR 0063）

本阶段目标 = Memory Rediscovery & Emotional Presentation。小步①完成"完成/收藏时刻的
情感权重呈现"，分三个文件、纯前端：

- **ui.jsx 新增共享 `MemoryTypeTag`**（克制视觉层级，不新增字段/评分/AI）：
  `书评` accent-soft pill（保持原样）/ `记录` neutral pill / `✓ 完成` accent pill 加粗
  （暖，里程碑）/ `＋ 收藏` muted 无 pill（入藏注记）。
- **MemoryTimeline.jsx**：内联徽标换用 `MemoryTypeTag`；时间轴圆点按类型——收藏时刻
  暖石 muted（`color-mix(accent 40%, panel-border)`），其余可点类型保持 accent。
- **MemoryGallery.jsx**：行 meta 加类型标记，**仅非 review 类型**（书评默认形态保持
  干净）——回廊里收藏/完成时刻被自然识别。
- 未改后端、未改数据模型、未改点击/删除/弹层行为。
- 测试：vitest **235 passed**（+2：Timeline milestone `✓ 完成`、collection `＋ 收藏` 且
  不可点击；Gallery 收藏/完成标记渲染、书评行无标记）；build 通过。
- ADR 0063 记录。

### 2026-08-11 Memory Rediscovery · 小步②：记忆回廊文本筛选

Memory Gallery 前端文本筛（复用既有后端能力，不改后端/数据模型）：

- **MemoryGallery.jsx**：`q` state + 搜索输入框（占位「搜索记忆内容…」+ 清除 ✕ 按钮，
  与作品下拉同排）；过滤并入既有 `filtered` useMemo——`summary` 子串、大小写不敏感，
  **与后端 `GET /memories?search=` 的字段一致**（不创造新搜索协议）；空状态区分
  "没有找到包含「q」的记忆"；副标题更新为「按年份 / 作品 / 文本筛选」。
- **纯客户端**：复用已拉取的全量记忆（个人库体量小，与既有年份/作品客户端筛选同架构）；
  后端 `?search=` 已存在，数据量增大后再迁移服务端分页，不新增协议。
- 测试：vitest **237 passed**（+2：文本筛命中/清除恢复、无命中提示）；build 通过。
- 无新架构决策，不新增 ADR（复用既有接口字段）。

### 2026-08-11 非对称档案室交互化 + 外壳切换入口移入设置（ADR 0064）

ShellC（C 非对称档案室）此前是"探索版"外壳、无法交互；完成实际功能并把切换入口放入
设置：

- **AppShells.jsx（ShellC 交互化）**：
  - 书库：点 ArchiveCard → 打开「作品档案」详情弹层（`ItemDetailPanel` 自取数据：
    外部世界 + 我的记录 + 时间轴），✕/点蒙层可关闭；
  - 检索台：作品 → 详情弹层；书评/记忆 → `MemoryReviewModal` 只读弹层（复用现有）；
  - 记忆回廊 `onOpenWork`、人物馆 `onOpenWork`/`onOpenVoice`（跳关系厅聚焦）、关系厅
    `onOpenWork` 全部接线；
  - 管理室：从"探索版仅示意"改为真实设置入口（应用外壳切换 + 数据管理引导）。
- **DesktopView.jsx**：移除右下角浮动 `ShellSwitcher`（探索期产物）；设置「外观」新增
  「应用外壳」块；`<ShellC shellValue onShellChange>` 传 props。
- 后端零改动；全部复用既有接口（/search/my、/memories、/items/{id}/detail 等）。
- 测试：vitest **240 passed**（+3：点卡开详情弹层可关闭、检索台命中可点开记忆弹层、
  管理室外壳切换回调）；build 通过。
- ADR 0064 记录。

### 2026-08-11 ShellC 整合既有功能：书库三视图 + 管理室真实化 + 命令面板（ADR 0065）

用户反馈"其他正常功能也要整合进非对称档案馆"——复用既有资产整合进 ShellC，后端零改动：

- **书库三视图 + 筛选（新 ShellLibrary）**：网格（ArchiveCard+CoverAmbient）/ 书架
  （复用 Bookshelf）/ 分组列表（复用 StatusGroupedList，点行开详情弹层）；work_type
  类型筛选 chips + 库内搜索（标题/内容）；视图选择持久化 localStorage。
- **管理室真实化**：应用外壳切换 + **AI Provider**（复用 ProviderSettings）+ **数据备份**
  （导出/导入 JSON，复用 exportBackup/importBackup/fetchImportStatus，后台向量重建）。
- **命令面板 Ctrl/Cmd+K**：`CommandPalette` 新增 `commands` prop（直接外部命令集）；
  ShellC 作用域命令集（六房间切换 + 切换主题 + 条目搜索开详情弹层）。
- **ToastHost 移到外壳分支外**（两外壳共用，ShellC 内导出/导入 toast 正常显示）。
- **明确不做**：完整检索台抽取（大重构，单独排期）、批量操作/右键/涂鸦等 DesktopView
  深度绑定能力。
- 测试：vitest **243 passed**（+3：书库三种浏览切换、管理室真实化三块、Ctrl+K 命令面板
  切房间）；build 通过。
- ADR 0065 记录。

### 2026-08-11 夜书房 · Visual Novel Archive：Phase1 Design Tokens + Application Shell（ADR 0066）

UI/UX 再设计第一阶段（视觉方向「夜书房 · Visual Novel Archive」），分五个小步提交：
小步A/B（tokens+PageHeader）→ 小步C（命令面板索书卡化）→ 小步D（Shell 馆内导览）→
小步E（页面头统一）。

- **Design Tokens**（index.css :root）：字阶 --fs-*、4px 基网格 --sp-*、hairline 边框、
  --ink-0/1/2、--scene-surface、--shadow-float、Motion（180/240/400ms + 统一 easing）、
  --focus-ring + 全局 :focus-visible + prefers-reduced-motion 全局退化。
- **Application Shell**：左侧导航改为**馆内导览**（serif 馆室 + mono 编号；馆室 ROOMS=
  书库/记忆回廊/时光轴/人物档案，工具 TOOLS=分析/声优图谱；检索台固定顶、管理室固定底；
  保留拖拽排序）；**顶栏**（mono 面包屑 + 全局检索 + Ctrl+K + 移动端汉堡）；**统一页面头**
  `PageHeader`（serif+hairline+mono 路径）应用于 7 个区（MemoryGallery 加 showHeader）；
  **命令面板**改为索书卡式检索浮层（Ctrl+K 行为不变）；**响应式**（<1024 抽屉，≤768
  底部房间导航 + 内容下边距）。
- UI 房间→内部 section 键映射 `ROOM_META`，**不改名内部键/路由/数据结构**；时光轴当前
  承载 summary（年度总结），全局轨迹后续阶段新建。
- 测试：vitest **246 passed**（+3 馆内导览/顶栏/抽屉；旧导航 title 用例合理更新）；
  build 通过。ADR 0066 记录。
- **遗留问题（下一阶段）**：时光轴=年度总结的临时映射；MemoryGallery 内部 h2 由
  showHeader 隐藏（classic）；Library 工具栏/页面正文待 Phase2；分析/声优图谱在工具组。

### 2026-08-11 Phase 2-0：ShellC 视觉语言统一（ADR 0067）

Phase 1 验收 PASS WITH FIXES，进入 Phase 2 前先做最小前置修正——ShellC rail 从 sans
房间标签统一到「馆内导览」Design System（解决验收次要问题 2 的双重设计语言）：

- **index.css**：`.shell-c-rail` 宽 148→176px、`--surface-1`→`--rail-bg`、padding/gap 对齐；
  `.shell-c-nav` baseline→center、去边框、`transition:none`→motion token、active 层级=
  accent+accent-soft（与 Classic 相同）；`.shell-c-no`（mono 编号）10→9px、16px 居中、
  `--ink-2`；新增 `.shell-c-label`（serif 12px / 0.06em / w500）。
- **AppShells.jsx**：标签 span 加 `shell-c-label` class。
- 不改变功能/房间数/section key/状态管理/数据/API/房间布局概念。
- 测试：vitest **247 passed**（+1 rail 结构断言）；build 通过。
- 真实浏览器验证：railWidth 176、railBg --rail-bg、标签 Georgia/Songti 12px/0.06em、
  编号 mono 9px、激活态 accent+accent-soft——与 Classic 一致。
- ADR 0067 记录。下一步：Phase 2 · Library 正式重设计。

### 2026-08-11 Phase 2-1：Library 空间与页面头（ADR 0068）

Phase 2 · Library 开始，第一步只做"空间"（禁止动 ArchiveCard/工具栏/分组/圆角）：

- **书架空间列 `.lib-space`**：书库内容约束 `max-width:1600px` 居中（超宽屏防拉伸），
  1920 以下自然填满；只做空间不改内部。
- **移动端基础结构**：`≤768px` 隐藏左侧侧栏 → 书库获得全宽（实测 390 库列
  105px→329px），消除"桌面缩小"式挤压；配合底部房间导航 + 64px 下边距。
- 页面头沿用 Phase 1 PageHeader（书库 · LIBRARY）。
- 测试：vitest **247 passed**；build 通过。
- 真实浏览器验证：1920/1440 书库列填满无溢出；390 rail 隐藏 + 底部导航 + 侧栏隐藏
  全宽 + 无横向溢出。截图 lib21_1920/1440/390.png。
- ADR 0068 记录。下一步：Phase 2-2 ArchiveCard（待确认）。

### 2026-08-11 Phase 2-2：ArchiveCard →「书 / 索书卡」视觉物件（ADR 0069）

Phase 2-2 只处理 ArchiveCard（不改工具栏/分组/圆角全面迁移/后端）：

- **视觉层级**：Cover → Title → 编目行（NO.·来源）→ 索书卡元数据行 → 次级操作；
  封面成为第一锚点；全部信息与交互保留。
- **纸与墨**：去掉 inset 暖色内晕 + 浮起阴影 → `0 1px 2px rgba(43,36,26,0.05)` 轻微
  环境层次；hairline 边框；Surface 6px / Cover 4px；无渐变/玻璃/发光。
- **Hover 安静**：边框微染 accent 35% + 封面 `scale(1.02)` + 次级操作淡入（不再上浮）；
  motion 用 `--dur-enter` + `--ease-standard`。
- **键盘焦点**：`role=button` + `tabIndex=0` + Enter/Space 打开；`:focus-visible` 用
  `--focus-ring`；Selected 保持 accent 边框 + 环。
- 测试：vitest **248 passed**（+1 封面首锚点 + 键盘打开）；build 通过。
- 真实浏览器：封面 3/4、卡圆角 6px、无 inset、标题 serif+clamp2、meta/lines 保留、
  Ambient 在卡外、390 无横向溢出。离屏无法模拟真实 Tab，焦点环留待实际浏览器 Tab 验证。
- ADR 0069 记录。下一步：Phase 2-3 工具栏/筛选（待确认）。

### 2026-08-11 Phase 2-3：Library 工具栏/筛选 →「整理书架」（ADR 0070）

Phase 2-3 只处理工具栏/筛选层级（不动状态分组/圆角全面迁移/ArchiveCard 核心/后端）：

- **三级操作层级**：L1 搜索（置左主入口，纸感 hairline + focus-ring + ✕ 清除）；
  L2 视图切换（compact，active 由白字实心降为 accent-soft+accent）；L3 批量选择
  （默认安静 ink-2，进入 Selection Mode 才 accent+accent-soft、底部批量栏出现）。
- **书架范围行**：常显 `共 N 册`（mono 微字）；作品类型并入该行（mono 微字，无彩色
  chips 墙）；激活的标签/分组/搜索词以可清除小标展示 +「清除筛选」；移除原
  "全部资料+计数 pill"主标题。
- 排序：现有 Library 无内容排序能力，不新增逻辑。
- Accent：Normal 模式实测 0 个实心 accent 元素；Motion 仅用 token，无按钮堆动画。
- Responsive：搜索 `w-full sm:w-72`；1920/1440/1024/390 均无横向溢出。
- 测试：vitest **248 passed**（搜索占位/选择/全部类型/视图 title 全保留）；build 通过。
- 真实浏览器：搜索为首控件、Normal 无实心 accent、范围行「共 63 册」、Selection Mode
  批量栏出现且可见、390 全宽无溢出。截图 tool23_1920/1440/1024/390.png。
- ADR 0070 记录。下一步：Phase 2-4 状态分组→书架分区（待确认）。

### 2026-08-11 Phase 2-4：状态分组 → 书架分区（ADR 0071）

Phase 2-4 只处理状态分组/侧栏视觉层级（不动 ArchiveCard/工具栏/圆角全面迁移/后端）：

- **书架章节标题**（StatusGroupedList）：分组标题改为 `▾ [状态名 serif 14px] ─hairline─
  [N 册 mono]`；数量降权（--ink-2 mono 微字，非 pill/非焦点）；保留折叠/状态值/行点击。
- **分区节奏**：分组间 gap-5 书架层；不套 Card（无 Card 套 Card）。
- **侧栏一并审视**：分组/标签数量改为 mono 微字 --ink-2；当前选择仍 accent；功能不变。
- **空状态/颜色**：空分组不渲染（无"0 items"）；无状态专属彩色；实测实心 accent 0。
- 测试：vitest **248 passed**（分组标题/折叠/行点击/主从 title 全保留）；build 通过。
- 真实浏览器：章节标题 serif 14px、`N 册` mono、hairline、3 分区、实心 accent 0、
  四视口无溢出。截图 shelf24_1920/1440/1024/390.png。
- ADR 0071 记录。下一步：Phase 2-5 圆角语义迁移（待确认）。

### 2026-08-11 Phase 2-5：圆角语义迁移（六级 Radius System，ADR 0072）

建立全站可复用六级圆角语义，并按语义迁移（非机械替换）：

- **Tokens**：`--radius-surface 4 / card 6 / cover 4 / control 8 / chip 999 / floating 14`
  ——数值沿用既有语言，为"为什么不同圆角"建立语义与单一来源。
- **迁移**：ArchiveCard 卡面→card(6)、封面→cover(4)；Library 搜索框（去胶囊化 8）、
  视图切换组/按钮、批量选择→control(8)；主从视图面板→surface(4)；CommandPalette /
  MemoryReviewModal / ItemDetailModal / ShareCardModal 容器 / Bookshelf 抽书预览→floating(14)；
  类型/筛选标签保持胶囊（chip）。
- **保留**（遗留，原因见 ADR）：检索台答案卡/设置卡/批量栏/导入浮层（ask/settings 区）、
  CharacterWall/VoiceGraph/Inspector/YearlySummary/ProviderSettings 等非 Library 页面、
  大量 rounded-xl/lg 在 ReviewStudio/ItemDetailPanel 等（rounded-lg 数值=control）。
- 测试：vitest **248 passed**；build 通过。
- 真实浏览器：card 6 / cover 4 / search 8 / view 8 / batch 8 / chip≈999 / palette 14；
  1920/1440/390 无溢出。截图 radius25_1920/1440/390.png、radius25_grid1920.png。
- ADR 0072 记录。下一步：Phase 2-6 Responsive（待确认）。

### 2026-08-11 Phase 2-6：Library Responsive 收口（Phase 2 收官，ADR 0073）

全 7 viewport（1920/1440/1024/768/430/390/375）真实浏览器审计，全部 hOverflow=false，
修复三处真实问题（最小改动、无重设计）：

- **移动端单列巨卡** → `@media(max-width:560px){:root{--d-grid-min:140px}}`：390 双列
  149px / 375 双列 141px / 430 双列 169px；桌面 7 列不变。
- **移动端主从视图挤压** → 桌面保留左右主从；窄屏列表全宽 + **全屏 Detail Scene**
  （复用 ItemDetailPanel + detailBrowseId，`window.innerWidth<1024` 守卫 + CSS `lg:hidden`，
  不新增第二套详情系统）。
- **触控目标** → 视图切换按钮 25→29px。
- 未改 ArchiveCard/工具栏/状态分区/Radius/Shell/PageHeader/底部导航；侧栏沿用 ≤768 隐藏；
  未加 Filter Drawer/Bottom Sheet；后端零改动。
- 测试：vitest **248 passed**（主从窄屏浮层用 JS 视口守卫，jsdom 无 CSS 不重复渲染）；
  build 通过。真实浏览器 gridCols 7/4/2/2/2/2/2、hOverflow 全 false。截图
  resp26_1920/1024/390.png。
- ADR 0073 记录。**Phase 2（Library）全部小步完成。**

### 2026-08-11 Work Detail 阶段约束登记（ADR 0074，用户钦定限制，仅记录未实现）

进入 Work Detail 重设计前，用户钦定 15 条限制，逐条登记为绑定约束（ADR 0074）：

1. 禁止修改后端 API
2. 禁止修改数据模型
3. 禁止 Item → Work 重命名
4. 禁止新增 Experience / Timeline 实体
5. 禁止修改 Collection 状态语义
6. 禁止重新设计 Review Studio
7. 禁止重新设计 MemoryReviewModal
8. 禁止修改 MemoryTypeTag 的既有语义
9. 禁止新增 Dashboard/统计卡
10. 禁止为了视觉效果增加虚假数据
11. 禁止顺手修改 Library
12. 禁止顺手修改 Gallery / Timeline
13. Classic 与 ShellC 必须共享同一 Work Detail 设计语言
14. 每个小步必须 vitest → build → 浏览器验证 → 报告 → 停止
15. 未经下一步确认不得继续

本条目只登记约束，未开始 Work Detail，等待确认后按小步执行。

### 2026-08-11 Phase 3-0 → 3-1：Work Detail 现状审计 + 统一详情内容系统（ADR 0074/0075）

Phase 3-0 只读审计：确认 ItemDetailPanel/ItemDetailModal 重复、Classic 三入口两套观感、
ShellC 与 Classic 详情分叉、milestone 可从 Memory(source_type=milestone) 直接获得、
review/collection/memory 均含时间字段、"相遇纪事"可不新增模型构造。ADR 0074 登记 15 条
用户钦定约束（仅记录未实现）。

Phase 3-1 实施（统一 Work Detail 内容系统）：

- **ItemDetailPanel 为唯一内容基底**；新增最小 props：`externalDetail`（外部未收藏模式，
  仅作品本身+这个世界+外部操作）、`refreshKey`、`onSaveDetail/onShareDetail/onRefreshDetail`
  （回调由调用方传入，Panel 不承担新后端业务）。
- **入口统一**：`openItemDetail`（网格/书架/HomeShrine/CharacterWall/右键）→ 浮层渲染
  ItemDetailPanel；`openExternalDetail` → externalDetail 模式；`detailSave` 收藏入库后切换
  itemId 模式；`handleRefreshExternal` + detailRefreshKey 重取；MemoryGallery/VoiceGraph/列表/
  移动 Detail Scene 走 detailBrowseId（已统一）。
- **ItemDetailModal 降级**：无 App 运行时引用（仅 characters.test 单元测试），保留文件，
  具备删除条件但本阶段不删。
- 未改后端/API/数据模型/Collection/Memory/Review；未做 3-2 叙事重组；未动 Library 等。
- 测试：vitest **250 passed**（+2 外部未收藏/已收藏操作按钮）；build 通过。
- 真实浏览器：Classic 已收藏=统一容器+我的记录+安利卡/刷新（无收藏入库）；ShellC=同一
  面板；Mobile 详情无溢出；外部未收藏由组件测试覆盖（联邦检索依赖网络未实跑）。
- ADR 0075 记录。下一步：Phase 3-2 叙事结构重组（待确认）。

### 2026-08-11 Phase 3-1 清理 + Phase 3-2-A：统一详情收尾 + 作品本身/这个世界叙事重组

**Phase 3-1 遗留清理**：
- 迁移 characters.test.jsx 的 ItemDetailModal 用例到统一 ItemDetailPanel（外部未收藏模式
  收藏入库/已收藏模式无收藏入库/点击触发 onSaveDetail）；
- **删除 ItemDetailModal.jsx**（迁移后无任何运行时/测试引用残留；git grep 确认）。

**Phase 3-2-A（作品本身 + 这个世界）**：
- **作品本身**：封面升级为第一视觉锚点（128px 书本封面，radius-cover + 细边 + 轻微投影，
  非后台数据卡）；标题 serif 24px；NO./来源/评分收敛为 **mono 编目行**（不再来源/评分/
  状态胶囊堆叠）。
- **这个世界**：serif 章节标题「这个世界」+ mono WORLD + hairline；**简介优先**（阅读）；
  原「基本信息 InfoTable」→ **quiet 编目**（mono 标签 + hairline 行，非表格中心）；
  标签轻量索引；角色作作品世界一部分。work_type 内联修正保留（章节标题右）。
- 外部操作区（收藏入库/安利卡/刷新）保留在「这个世界」结束处。
- 未动 我的记录/composer/MemoryTimeline/Review Studio/MemoryTypeTag/后端；未加 ADR
  （纯视觉布局变化，按约束不机械新增 ADR）。
- 测试：vitest **250 passed**（旧"外部世界/基本信息/评分胶囊"断言按新语义合理更新）；
  build 通过。真实浏览器：封面 128px、serif「这个世界」16px、简介优先、编目 3 行、
  mono 编目行、1920/1024/390 无横向溢出。截图 wd32a_1920/1024/390.png。
- 下一步：Phase 3-2-B「我与它」重组（待确认）。

### 2026-08-11 Phase 3-2-B：Work Detail「我与它」章节重组

把原"我的记录（收藏 select + 喜欢 pill + 收藏于）"后台控制区重组为安静私人档案章节
「我与它 · MY RELATION」（serif + mono + hairline）：

- **章节结构**：`我与它` serif 章节标题 + mono `MY RELATION` + hairline；内部按关系信息层
  （数据优先、关系其次、操作最后）：
  - **当前状态**：状态本身作为主线叙事（serif 14px `未收藏/想看/看完…`），状态切换 select
    降为轻量控制（视觉权重低于状态）；行为不变（看完→milestone 依 ADR 0062，不改生成逻辑）
  - **我的态度**：喜欢 → 克制 toggle（accent 仅 active，非大红心/独立卡）
  - **我的评价**：`我的平均 ★x`（来自书评），安静展示，不改评分逻辑
  - **我的书评**：书评入口按钮（`onOpenReview`，进入 Review Studio；Review Studio 不重构）
  - **带回书架**：`收藏于 {date}`（collection.added_at，私人档案语言）
- 本地笔记：显示"本地笔记没有收藏关系"。
- composer + MemoryTimeline 保留原位（3-2-D 归入「我的记忆」叙事标题，本阶段未动）。
- **边界遵守**：本阶段未实现「相遇纪事」（未把 added_at/milestone/review.created_at 做成
  时间线节点、未做首次遇见）；未改后端/API/Collection/Memory/Review/ReviewStudio/
  MemoryTimeline/MemoryTypeTag；未新增 ADR（纯视觉重组，状态/喜欢/评分语义未变）。
- 测试：vitest **250 passed**（「我的记录」断言按新语义更新为「我与它」+ 书评入口）；
  build 通过。真实浏览器：1920/1440/1024/768/390 全部 `我与它` serif 章节 + 当前状态 +
  书评入口 + 带回书架 + 无横向溢出；书评入口点击 → ReviewStudio 打开且详情浮层关闭。
  截图 wd32b_1920/1440/1024/768/390.png。
- 下一步：Phase 3-2-C「相遇纪事」（待确认；首次遇见推导规则将先明确并写入 ADR）。

### 2026-08-11 Phase 3-2-C-0：相遇纪事数据规则审计 + ADR 0076（只读/纯规则，未实现 UI）

先回答"Tsumugi 到底知道什么"，再定未来 UI 的数据规则（本阶段无任何 UI 变化）：

- **代码审计确认**：Collection `added_at`（收藏入库时间，nullable）、Memory `occurred_at`
  （"记忆发生的时间"，赋值语义：review 记忆=review.created_at、collection/text/milestone=
  now）、Review `created_at`/`updated_at`；**milestone 是 Memory(source_type=milestone)**，
  **完成 vs 重新打开无结构化子类型**，仅能由 summary 文本区分（诚实处理，不伪造类型）。
- **纯前端 View Model**：`frontend/src/encounter.js` `buildEncounterEvents({collection,reviews,
  memories})` → `EncounterEvent[]{id,type,occurredAt,title,source,metadata}`：
  带回书架(added_at)/写下书评(created_at，禁用 updated_at)/留下记忆(text.occurred_at)/
  里程碑(milestone.occurred_at)；review/collection 类型记忆排除（与主事件重复）；
  时间升序；同日事件全保留不聚合；无事件返回 []。
- **「第一次遇见」结论：不支持**。最早用户痕迹 ≠ 第一次接触作品（用户可能更早接触未留痕）；
  未来 UI 用更诚实表达（"最早记录/最初留下的痕迹"，文案 UI 阶段定）；禁止 min(最早)=遇见。
- 未改：后端/API/数据库/schema/字段/Collection/Memory/Review 语义/Review Studio/
  MemoryTimeline/MemoryTypeTag/ItemDetailPanel 视觉；未实现 UI；未新增 Timeline 组件。
- 测试：vitest **261 passed**（+11 encounter 规则测试：各来源/混合升序/冗余记忆排除/
  缺失字段/同日多事件/空数组/不推导首次遇见）；build 通过。
- 越界检查：无 first_seen_at / 后端 encounter / 新 API / 新 schema 残留；`buildEncounterEvents`
  仅 encounter.js + 测试引用。
- ADR 0076 已创建（含 Data Sources/Event Mapping/Timestamp Rules/First Encounter Policy/
  Fact vs Inference/Missing Data/Duplicate/Ordering/View Model/Non-Goals/Consequences）。
- 下一步：Phase 3-2-C-1「相遇纪事 UI 实现」（待确认）。

### 2026-08-11 Phase 3-2-C-1：Work Detail「相遇纪事」UI 实现

把 ADR 0076 的 View Model 接入统一 Work Detail：

- **ItemDetailPanel**：新增 `reviews`/`memories` 取数（fetchItemReviews/fetchItemMemories，
  一次请求，同一份 memories 同时供给 MemoryTimeline，避免重复请求）；`encounterEvents =
  buildEncounterEvents(...)`（唯一事件构造来源，UI 不重复推导/去重/排序）；渲染「相遇纪事
  · ENCOUNTER CHRONICLE」章节（serif 标题 + mono + hairline + 开放档案行：mono 日期
  YYYY.MM.DD + serif 事件标题 + metadata 弱文字），位于「我与它」与「我的记忆」之间；
  无事件整章隐藏；不显示「第一次遇见」；milestone 用既有 summary。
- **MemoryTimeline**：新增可选 `memories` prop（提供时不重复请求），面板传 memories。
- **encounter.js**：输入防御性归一化（reviews/memories 非数组 → []，修复 fetchItemReviews
  对非数组响应的暴露）。
- 边界：未改后端/API/数据模型/Collection/Memory/Review 语义/Review Studio/MemoryTimeline
  视觉/MemoryTypeTag；未做 3-2-D；未新增 Timeline 组件。
- 测试：vitest **266 passed**（+5 encounter-chronicle UI：四类事件/顺序/同日全保留/无事件
  隐藏/不显示第一次遇见；修复 ambient 3 例因非数组 reviews 引发的回归 + memory-timeline
  「神作」多处匹配按新语义更新）；build 通过。
- 真实浏览器：5 视口「相遇纪事」章节 + mono 日期 + serif 标题 + 无横向溢出；未收藏条目
  章节自然隐藏。截图 wd32c_1920/1440/1024/768/390.png。
- 下一步：Phase 3-2-D「我的记忆」叙事归位（待确认）。

### 2026-08-11 Phase 3-2-D：Work Detail「我的记忆」叙事归位

Work Detail 五个章节全部归位（作品本身 → 这个世界 → 我与它 → 相遇纪事 → 我的记忆）：

- **ItemDetailPanel**：composer + MemoryTimeline 正式归入「**我的记忆 · MY MEMORY**」章节
  （serif 章节标题 + mono `MY MEMORY` + hairline）；composer 与 MemoryTimeline 原 DOM/行为
  保留（未重写）；MemoryTimeline 继续复用同一份 memories（不重复请求）。
- **审计结论**：composer 承担 text/milestone 创建（createDirectMemory：source_type
  text/milestone、occurred_at=now、summary=文本/固定字符串、emotion、file→media）；
  MemoryTimeline 已具备查看/删除/类型（MemoryTypeTag）/情绪/媒体；无重复记忆入口；
  composer 与 MemoryTimeline 相邻即成一章——故只做最小结构归位（章节标题+hairline）。
- 边界：未改后端/API/数据模型/Collection/Memory/Review 语义/Review Studio/
  MemoryReviewModal/MemoryTypeTag/encounter.js/ADR 0076；未新增 Memory 类型；未做 3-3。
- 测试：vitest **269 passed**（+3：我的记忆章节存在且 composer/MemoryTimeline 归位、
  五章节顺序、composer 提交仍 POST /items/{id}/memories）；build 通过。
- 真实浏览器：5 视口章节顺序正确、serif「我的记忆」+ composer 归位 + 无横向溢出。
  截图 wd32d_1920/1440/1024/768/390.png。
- 下一步：Phase 3-3（待确认；不自动进入）。

### 2026-08-11 Phase 3-3：Work Detail 收尾审计与视觉打磨

收尾审计 + 最小修正（不重做五段叙事，能不改就不改）：

- **审计结果**：
  - 五段结构：真实 DOM 顺序确认 作品本身(头) → 这个世界 → 我与它 → 相遇纪事 → 我的记忆；
  - 重复内容：**数据源/大众评分/我的评分 在「作品本身」身份行、这个世界 quiet catalog、
    「我与它」之间重复出现**；
  - accent：仅主要操作（记录/书评/喜欢激活）用 accent，非按钮墙；
  - radius：cover 4px(radius-cover)、控件 radius-control，无历史冲突（composer 内 `rounded-lg`
    已被 inline radius-control 覆盖，无视觉冲突，按"能不改就不改"未动）；
  - typography/hairline/Ambient：章节 serif+mono+hairline 稳定，Ambient 为场景层不压正文。
- **实际修改（最小）**：ItemDetailPanel 的这个世界 quiet catalog 过滤掉 数据源/大众评分/
  我的评分（三者已由身份行与「我与它」承担，信息不丢失，消除三处重复）。
- 未改 encounter.js/ADR 0076/Review Studio/MemoryReviewModal/MemoryTypeTag/MemoryTimeline
  业务逻辑/后端/API/数据模型/Collection/Memory/Review 语义；未新增 ADR（纯视觉去重）。
- 测试：vitest **269 passed**（基线 269 → 修改后 269，无回归）；build 通过。
- 真实浏览器（Electron 离屏，1920/1440/1024/768/390）：五章节顺序、catalog 去重生效
  （无 数据源/大众评分/我的评分 标签）、实心 accent 按钮 1（记录）、cover 4px、
  全视口无横向溢出。截图 wd33_1920/1440/1024/768/390.png。
- externalDetail 模式：网络/Connector 依赖无法在本环境实跑，由 vitest（外部模式收藏入库/
  无我与它/无纪事）覆盖，如实说明。
- 下一步：Phase 3-3 完成后，等待确认下一阶段（不自动进入 Phase 4）。

### 2026-08-11 Phase 4-1：Memory Composer「留下这一刻」结构 / 叙事语言

把 Composer 从"一行工具栏 + 两枚 accent pill"重组为「留下这一刻」（只改 ItemDetailPanel 内
Composer + 新增 `.mc-*` CSS，其余不动）：

- **主书写区**：input → **textarea**（`mc-write`，min-height 64px / resize vertical / paper
  surface `--surface-1` + hairline / focus-ring / serif 占位），成为第一视觉焦点。
- **辅助操作层**：情绪 select + 附图按钮从主输入同行拆出为 `mc-aux` 安静层（ink-1、透明底、
  附图 hover 微 accent）；附图反馈 `已附图 ✓`。
- **主动作**：唯一实心 accent「**记录这一刻**」（原「记录」，handler/参数/API 不变，
  仍 `submitDirect("text", draft)`，disabled/recording 保留）。
- **完成 / 重新打开**：从 accent-soft pill 降为**安静文字操作**（`mc-milestone-btn`，
  ink-2 + hover 微 accent + hairline 下划线；文案「✓ 完成了」「↺ 重新打开」即 milestone
  summary 业务语义，不变）。
- Motion：仅 focus/hover/disabled 过渡（`--dur-enter` + `--ease-standard`），尊重全局
  reduced-motion；无新增 token（全部复用 ink/surface/radius-control/focus-ring/accent）。
- 未改 createDirectMemory / Memory API / Memory 字段语义 / MemoryTimeline / MemoryTypeTag /
  MemoryReviewModal / Work Detail 五章节 / 后端 / 数据模型。
- 测试：vitest **269 passed**（baseline 269 → final 269；更新 3 处「记录」→「记录这一刻」
  断言，未删/未弱化）；build 通过。
- 真实浏览器（Electron 离屏，1920/1440/1024/768/390）：textarea 74px、占位保留、
  `记录这一刻` 唯一实心 accent（实心按钮=1）、里程碑文字操作、MemoryTimeline 正常、
  全视口无横向溢出。截图 mc41_1920/1440/1024/768/390.png。
- 下一步：Phase 4-2（待确认；不自动进入）。

### 2026-08-11 Phase 4-2：Memory Composer 输入区 / 情绪 / 附图视觉层级收口

在 Phase 4-1 结构上收口三者层级（只改 Composer JSX + `.mc-*` CSS）：

- **输入区**：`.mc-write` 书写感增强——padding `sp-3/sp-4`、行高 1.8、focus 边框微 accent、
  focus-visible ring、serif 占位（ink-2）不变；默认 76px 高（第一视觉焦点）。
- **情绪**：`.mc-select` 降权——10px、ink-2、透明底、更轻边框；仍原生 select。
- **附图**：`.mc-attach` 辅助动作——透明底 + hairline 边框、hover 微 accent、min-height 36px；
  附图反馈「已附图 ✓」；fileInputRef/隐藏 file input 不变。
- **主操作**：`.mc-submit` **独立成行**（`mc-actions`，右对齐）——唯一实心 accent「记录
  这一刻」，min-height 40px，disabled/recording 保留；与辅助层分离，不形成一行工具栏。
- **完成/重新打开**：文字式（mc-milestone-btn）保持不变。
- Motion：仅 focus/hover/disabled 过渡（--dur-enter/--ease-standard）；无新 token。
- 未改 createDirectMemory/Memory API/Memory 字段语义/MemoryTimeline/MemoryTypeTag/
  MemoryReviewModal/Work Detail 五章节/后端/数据模型。
- 测试：vitest **269 passed**（baseline 269 → final 269，无新增行为故未加测试，未删/未弱化）；
  build 通过。
- 真实浏览器（Electron 离屏，1920/1440/1024/768/390）：textarea 76px、情绪 10px/ink-2、
  附图/情绪 36px、主按钮 40px「记录这一刻」唯一实心 accent（实心=1）、milestone 2、
  全视口无横向溢出。截图 mc42_1920/1440/1024/768/390.png。
- 注：中途审计脚本曾因 Shell 编码写坏中文选择器（书库→??）导致误判"无 Composer"，
  改回编辑工具写脚本后确认一切正常——非代码问题。
- 下一步：Phase 4-3（提交状态/完成与重新打开/feedback，待确认）。

### 2026-08-11 Phase 4-3：Memory Composer 提交状态 / Feedback 收口

Composer 交互收口（只改 ItemDetailPanel + `.mc-*` CSS + 测试）：

- **提交状态**：`submitDirect` 加 `submittingRef` 防重复提交（含渲染前双击）；`recording` 使
  所有提交按钮（文字/完成/重新打开）disabled，杜绝并发提交。
- **安静 feedback 一行**：新增 `feedback {kind: busy|ok|error, text}` + `.mc-feedback`
  （常驻 min-height 20px，aria-live="polite"，防布局跳动）：
  - busy：「正在记录…」（ink-2）；
  - ok：「已留下这一刻」（ink-2，2.5s 自动消失，不永久占位）；
  - error：`err.message || "记录失败，请重试"`（--danger 小字，安静可读），**输入/情绪/附件
    全部保留**（catch 不清空，便于重试）。
  - 成功路径不变：清空 draft/emotion/file → timelineRefresh → MemoryTimeline 更新。
- **完成/重新打开**：仍为安静文字操作 + 复用 submitDirect（milestone）；提交中 disabled +
  反馈「正在记录…」。
- 布局稳定：feedback 常驻槽位 20px，按钮文案恒定（不翻转），无跳动。
- 未改 createDirectMemory/Memory API/字段语义/MemoryTimeline/MemoryTypeTag/Review Studio/
  Work Detail 五章节/后端/数据模型；未新增 token/组件/Toast/Alert。
- 测试：vitest **272 passed**（baseline 269 → final 272，+3：成功反馈+清空、失败反馈+输入
  保留、完成仍 POST milestone）；build 通过。
- 真实浏览器（Electron 离屏）：idle 5 视口 feedback 槽 20px 稳定 + 无溢出；真实提交（原生
  setter 注入草稿）→ busy「正在记录…」+ 按钮 disabled → 成功后 draft 清空 + MemoryTimeline
  出现新记忆 + 反馈自动消失；端到端后经 UI ✕ 删除并 API 兜底清理，库回 58 条（无残留）。
  截图 mc43_1920/1440/1024/768/390.png。
- 注：浏览器端脚本曾两次因 Shell 编码写坏中文选择器误报，改用编辑工具 + 原生 value setter
  后验证通过——均为脚本问题，非代码问题。
- 下一步：Phase 4-4（Responsive + Accessibility + Motion 收口，待确认）。

### 2026-08-11 Phase 5-1：Memory Gallery「记忆回廊」工具栏视觉语言统一

Phase 4 收口（Phase 4-4 审计：P0/P1/P2=0，仅 P3 触控偏好，不修改）→ 进入 Phase 5（记忆回廊）。
Phase 5-0 审计确认 3 个 P2 视觉问题，Phase 5-1 只改 MemoryGallery 工具栏视觉表达（不动行为/数据/API）：

- **年份筛选**：active 由「白字实心 accent」改为 **accent-soft + accent 文字**；inactive 由
  accent-soft 底色改为**透明降权**（text-secondary）——去 SaaS pill 感，与 Library 类型筛选
  克制语言一致；仍是 button、年份/全部/排序/active 判断逻辑不变。
- **搜索 input / 作品 select**：`rounded-xl` → **`--radius-control`(8px)**；原生 input/select、
  aria-label="搜索记忆"、placeholder、筛选逻辑不变。
- **年份计数「N 条」**：改用 **mono 字体 + ink-2 低权重**（tabular + tracking），非 badge/pill；
  count 数值与分组逻辑不变。
- 未改数据/API/分组/排序/空态/onOpenWork/MemoryTypeTag/条目结构；未做 Phase 5-2（标本室/条目
  重构）；未改其它页面。
- 测试：vitest **272 passed**（baseline 272 → final 272，无删/弱化；既有断言按 role/label 不依赖
  class，无需更新）；build 通过。
- 真实浏览器（Electron 离屏，1920/1440/1024/768/390）：active chip bg=accent-soft(248,244,242)+
  accent(176,90,54)、inactive 透明、search/select radius 8px、count mono+ink-2、hOverflow=false。
  截图 mg51_1920/1440/1024/768/390.png。
- 下一步：Phase 5-2（待确认；不自动进入）。

### 2026-08-11 Phase 5-2-1：Memory Gallery「记忆标本行」实现

Phase 5-2-0 审计确认条目区为"时间轴列表"（竖线 rail + accent 圆点），Phase 5-2-1 重排为
克制「记忆标本行」（只改条目区，不动年份书档头/工具栏/数据/API）：

- **条目结构**：去 `.relative.pl-5` rail 与 accent 圆点；每条为 `button.mg-specimen`
  （padding、radius-control、hover = surface-2 安静高亮）：
  - meta 行：mono 日期(MM-DD, ink-2) + MemoryTypeTag(非 review) + `· 作品名`(ink-1)
  - 摘要（body 13px）
  - **情绪**（`emotion` 存在时，mono 10px ink-2"情绪 · x"）——复用已有字段
  - **附图缩略图**（text 记忆 `media` 存在时，44px `radius-cover`）——复用已有字段
- 年份书档分组头（ArchiveNo §年 + hairline + mono 条数）保留；点击/onOpenWork/筛选/排序/
  空态/MemoryTypeTag 不变。
- 未改数据/API/Memory 字段/其它页面；未做 5-2-2（更多标本形态/结构重构）。
- 测试：vitest **273 passed**（baseline 272 → final 273，+1：标本行渲染情绪+附图且无 rail）；
  build 通过。
- 真实浏览器（Electron 离屏，1920/1440/1024/768/390）：58 标本行、rail=false、dots=0、
  mono 日期+作品+摘要、年份书档头在、hOverflow=false。截图 mg52_1920/1440/1024/768/390.png。
- 下一步：Phase 5-2-2（待确认；不自动进入）。

### 2026-08-11 Phase 5-2-2：Memory Gallery「记忆标本室」密度与档案层级收口

Phase 5-2-2 只做年份档案分组与标本行之间的密度/层级/节奏收口（不改条目结构/数据/API）：

- **年份组之间**：`space-y-6`(24px) → **`space-y-8`(32px)**——年份分区之间的"档案呼吸"。
- **年份标题与首条标本**：`mb-2`(8px) → **`mb-3`(12px)**——§ YYYY + hairline + mono「N 条」
  形成更清晰的档案分区边界。
- 标本行间距（gap-1 + 内距 8/12px）保持"克制编目密度"；年份标题仍为 ArchiveNo(serif) +
  hairline + mono 计数，非 badge/卡片/实心 accent 块。
- 未改条目结构/情绪/媒体/筛选/排序/分组/onOpenWork/MemoryTypeTag/其它页面/数据/API。
- 测试：vitest **273 passed**（baseline 273 → final 273，纯间距调整无行为变化，未删/未弱化）；
  build 通过。
- 真实浏览器（Electron 离屏，1920/1440/1024/768/390）：年分区下距 12px、`space-y-8` 生效、
  标本内距 8/12px、58 标本、年份书档头在、hOverflow=false。截图 mg52b_1920/1440/1024/768/390.png。
- 注：真实库仅 1 个年份（2026），组间 32px 间距为类生效（真实多组时生效）。
- 下一步：Phase 5-2-3（待确认；不自动进入）。

### 2026-08-11 Phase 6-1：时光轴房间 / YearlySummary 视觉语言收敛

Phase 6-0 选候选 1（时光轴房间 = YearlySummary 年度总结；5-2-3 审计确认 Gallery 无需修改、
6-0 只读盘点确认无独立 Timeline）。Phase 6-1 只收敛 YearlySummary 视觉语言（去统计仪表/
Dashboard 感，转向「年度档案 / 私人阅读时间记录」）：

- **年度档案头**：`2026 年度` accent pill → **ArchiveNo §年份 + mono `ANNUAL ARCHIVE` +
  hairline**；传说（少→多）改 `ys-legend-cell` 克制小格（ink-2，无 pill）；「活跃度=书评×2+
  收藏×1」降为 mono 注脚。
- **热力图容器**：`rounded-2xl border surface-0`（大圆角深卡）→ **纸面 `--surface-1` +
  hairline + `--radius-card`(6px)**；SVG 本身与月份/星期标签/aria-label 不变。
- **统计摘要**：4 张数字卡（text-xl font-bold accent）→ **quiet catalog 行**（`ys-stat-row`：
  mono label ink-2 + serif 值 ink-0 + mono sub，hairline 分隔，2 列）；最活跃月份行保留。
- **accent 色阶**：LEVEL_PCT 上限 85% → **70%**（克制降噪，P3）。
- 数据/API 冻结：`/activity`、fetchActivity、统计计算、年份选择、每日活动、svg rect/
  aria-label 全不变；未新建 Timeline 组件/API；未改其它页面。
- 测试：vitest **273 passed**（baseline 273 → final 273；1 处 level4 85%→70% 断言按新视觉
  语义合理更新，未删/未弱化）；build 通过。
- 真实浏览器（Electron 离屏，1920/1440/1024/768/390）：§年份+ANNUAL ARCHIVE、热力图纸面
  容器 6px/surface-1、统计 4 行 quiet catalog（serif 值 61/58/3/2）、无数字卡片、hOverflow=false
  （热力图内部 overflow-x-auto 不破页）。截图 ys61_1920/1440/1024/768/390.png。
- 下一步：Phase 6-2（待确认；不自动进入）。

### 2026-08-11 Phase 7-1：Ask / 检索台「文献式结果区」视觉收敛

Phase 7-0 审计确认 Ask 结果区为「SaaS 回答卡 + 徽章墙」（P1），Phase 7-1 只改 Classic Ask
结果区视觉呈现（搜索/联邦/我的/AI 逻辑与 HomeShrine/ShellC 全冻结）：

- **搜索栏**：`rounded-full` pill → `--radius-control`（容器/历史/提问按钮）。
- **结果区**：三块 `desk-answer-card rounded-2xl p-4` 回答卡 + tag-bg 徽章墙 → **文献式
  `ask-section`**（serif 章节标题 `我的检索/外部检索/来源/综合回答` + mono + hairline）：
  - 我的检索：类型（作品/书评/记忆）由 accent-soft pill → **mono `ask-type` 编目标签**（ink-2）；
    行 = 类型 + 标题 + 弱 meta；hover surface-2
  - 外部检索：来源 accent-soft pill → mono 来源标签；详情/收藏为 `ask-action` 弱文字
  - **来源**：tag-bg rounded-full pill + score → **脚注式 `ask-source-row`**（mono ink-2 +
    hairline，score 保留但降权）
  - **综合回答**：`<pre>` 聊天卡 → `ask-answer` 文献阅读块（14px / line-height 1.8 /
    pre-wrap，保留原文）
  - 错误：红色 rounded-2xl 横幅 → `ask-error`（hairline 左边条 + danger 小字 + aria-live）
- 数据/API 冻结：searchMy/federatedSearch/streamRag/handleAskFromValue/历史/跳转/收藏/弹层
  全不变；HomeShrine 空态与 ShellC shell-ask 未动。
- 测试：vitest **273 passed**（baseline 273 → final 273，无删/弱化；断言按文本/role 兼容）；
  build 通过。
- 真实浏览器（Electron 离屏，真实提交查询）：1920/1440/1024/768/390 hOverflow=false；
  1024/768/390 内容态呈现 我的检索(作品 mono 标签)+外部检索(bangumi)+来源脚注 5 行；1440
  10s 流式后完整呈现 我的检索/外部检索/来源/**综合回答**（真实 AI 答案，line-height 25.2px）
  ——全程无 desk-answer-card / rounded-2xl / tag-bg 徽章墙。截图 ask71_1920/1440/1024/768/390.png。
- 遗留：真实错误态未触发（本轮无错误），`ask-error` 为代码验证 + aria-live 已加；
  1920/1440 首轮 4s 内外部/AI 未就绪属时序（10s 后齐全）。
- 下一步：Phase 7-2（待确认；不自动进入）。

### 2026-08-11 Phase 7-1B：移除非对称档案室外壳（ShellC 全量下线）

产品收敛为"一个图书馆"：删除 C 非对称档案室外壳与全部外壳切换机制，Classic 三栏
成为唯一外壳（ADR 0077）。

- 删除 `AppShells.jsx`（ShellC/ShellContent/ShellLibrary/ShellBackup/ShellSwitcher/
  SHELL_CONCEPTS/parseShell）与 `app-shells.test.jsx`（9 用例）。
- `DesktopView.jsx`：移除 shellConcept 状态、`?shell=` URL + localStorage 读取、
  changeShellConcept、三元分支（classic 恒渲染）、设置页「应用外壳」块、import。
- `index.css`：删除 `.shell-switcher` / `.shell-grid` / `.shell-c*`；注释清理
  （ItemDetailPanel/CommandPalette）。
- 数据/API 冻结：房间内容本就是 Classic 组件 + ADR 0068 书库空间的组合，删除的只是
  外壳壳层，Library/Gallery/Ask/Settings/Work Detail/收藏等全部不受影响。
- 测试：vitest **264 passed**（273 − 9）；build 通过。
- 真实浏览器（dev server）：默认与 `?shell=c` 均渲染 `[data-testid="app-shell"]`
  （classic），`.shell-c` / `.shell-switcher` 不存在，shell 参数已失效无害。
- 下一步：Phase 7-2（待确认；不自动进入）。

### 2026-08-12 Phase 7-2：书架视觉结构重做（文献式真书架，ADR 0078）

只重做 `libView === "shelf"` 的视觉结构；数据/交互/API 全冻结（spineColor/spineSeed/
spineThickness/groupBookshelf/点击/selectMode/右键/hover 数据请求全保留）。

- **审计结论（旧结构为什么丑）**：书脊均值 11px（10–28px）→ 63 本 = 彩色细柱墙；
  高/宽 ≈19:1 读不出实体；层板 = 7px 渐变条 + border（"画了条线"）；分类标题 =
  accent 左边条目录卡标签（badge 比书抢眼）；页面 = 一叠横条无"一格书架"；hover 大浮卡
  远离书本；390px 仍是 11px 细柱。
- **新视觉结构**：每分类 = 书架单元（`.shelf-index` serif 分类名 + mono 册数 +
  hairline 延伸线 + `.shelf-case` hairline 围合 + 层板 13px 实体板）；书 = 实体对象
  （bookWidth 映射数据厚度 9..28 → **22..46px**，圆柱明暗 + 顶部头带 + 底脚 + 接触影，
  书名 vertical-rl）；hover = 抽书（上浮 + 阴影加深 + CoverAmbient，预览贴近书本且克制）；
  选中 = 微 accent 外框 + 13px 小圆标记（非 checkbox）；移动端书脊最小 24px。
- **改**：`Bookshelf.jsx`（JSX 结构重排 + bookWidth 视觉映射）+ `index.css`
  （`.shelf-*` 重写）。未新增依赖/token；`.shelf-label*` 类移除（data-testid 保留）。
- **测试**：vitest **264 passed**（bookshelf 20 例全兼容）；build 通过。
- **真实浏览器（真实库 63 册）**：1920/1440/1024/768/390 全 `hOverflow=false`；书脊
  23–46px（移动端 min 24）；15 架/15 匣/15 层板/63 书；hover 抽书、点击进「作品档案」
  详情、selectMode 63 标记 + ✓、网格 63 卡 / 列表 3 组 均正常；无 JS 错误（仅 Electron
  开发模式 CSP 警告）。截图 `bookshelf_new_1920/1440/1024/768/390.png`。
- 下一步：待确认，不自动进入下一阶段。

### 2026-08-12 Bookshelf-1.5 纯视觉审计（零改动）

只读审计确认三个视觉结构问题（详见审计报告）：P1 碎片化（15 个全宽架、8 个单册架
95–98% 空、页面 4670px）；P2 整架同色（同标签=同 seed=同色，TV 22 本全同色）；P3 390
仍是缩小版桌面架（15 条横滚行、4660px）。判定需要 Bookshelf-2。截图
`bookshelf_audit_1920/1440/1024/768/390.png`；**本阶段零代码修改**。

### 2026-08-12 Bookshelf-2：共享架 / 色彩节奏 / 移动紧凑（P1·P2·P3，ADR 0079）

基于 Bookshelf-1.5 审计，第二轮视觉结构重设计（数据/API/交互全冻结，bookshelf.js
零改动）：

- **P1 碎片化 → 视觉合架 + 贴近书本架面**：`groupBookshelf` 结果中册数 ≤2 的小分类
  连续合进「零散藏书」共享架（同一匣+同一条层板，各小组保留 mono 小索引，标签在书
  左侧同行）；大分类仍独立单元；`.shelf-case` 改 `width: fit-content; max-width: 100%`
  ——层板只承托书本区域。实测：case/board **15→7**、页面 **4670→2196px**（390
  1825px）、TV 架空 36%→21%。
- **P2 整架同色 → 色相确定性扰动**：新增纯函数 `spineColorVaried(accent, it)`，在标签
  基色上按 `stringHash(it.id)` 做 **±12°** 确定性扰动——同分类相近色系、每本有差异，
  TV 22 本从全同色 → 63 本 51 种微差色。
- **P3 390 → 紧凑书架**：书高 210→**172px**、单元间距 30→18px、共享架收紧；书脊最小
  24px 保持（仍为实体书非细柱）；hover preview 极右书修复（溢出时移到书左侧，不再
  clamp 出怪位）。
- **改**：`Bookshelf.jsx`（合架 plan + spineColorVaried + preview 修复，导出
  spineColorVaried 供测试）、`index.css`（`.shelf-*`）、bookshelf 测试（+6）、ADR 0079。
- **测试**：vitest **270 passed**（264+6：合架不丢书/独立 button、大分类独立架、混合架、
  selectMode+aria-pressed、context menu、颜色 deterministic+±12°+同架有差异）；build
  通过。
- **真实浏览器（真实库 63 册）**：5 视口全 `hOverflow=false`；63 书/7 匣/7 层板/15 小组/
  1 共享架；书脊 23–46px（390 min 24、高 172）；hover 最左/最右/最窄/最宽均无截断、
  390 极右书 preview 移到书左（修复）；点击进「作品档案」、context menu、selectMode
  63 标记+aria-pressed=1、网格 63 卡/列表 3 组 全正常；无 JS 错误。截图
  `bookshelf2_1920/1440/1024/768/390.png`。
- 遗留：共享架（零散藏书）在 1440 宽约 954px（8 个小组标签所致），多余空间属"零散
  架"预期、thin 滚动条可看全；若希望更紧凑需改"小组索引"呈现，留给后续（非本轮）。
- 下一步：待确认，不自动进入下一步。

### 2026-08-12 Bookshelf-2.5：视觉丰富度（姿态微差 / 架体内影 / 明度节奏，ADR 0080）

Bookshelf-2 结构已成立，本阶段只解决"整体偏单调"——增加空间感/层次感/收藏感/轻微
不规则，不重做结构/共享架/交互，bookshelf.js 零改动（全部由 `stringHash(it.id)`
确定性派生，无 Math.random）：

- **书本姿态 `bookPose()`**（新增纯函数）：高度微差 ±8px（桌面 202–218、移动 164–180，
  CSS `--bh` + `calc(var(--bh) - 38px)`）；宽度微差 ±2px（叠加 bookWidth，厚薄相对关系
  保留 → 21–44px）；约 1/4 的书微倾 ±0.6°（`--tilt` + `transform-origin:50% 100%`，书底
  站层板、顶缘轻靠邻书；hover 保留 `rotate(var(--tilt)) translateY(-5px)`）。
- **明度节奏**：`spineColorVaried` 增加 ±3% 明度抖动（色相 ±12° 不变）→ 63 本 56 种
  微差色（"22 本不同的紫书"而非整块紫墙）。
- **架格内影（CSS）**：`.shelf-case` 顶部留白 26px + `::before` 顶部内影（暗示上方还有
  层板）+ `overflow-x: clip`（防微倾横向滚动）；`.shelf-books` 顶部 inset 软影（书后方
  recess）；`.shelf-board` 前缘底部略深（厚度感）。分类索引/共享架/hover/交互全不动。
- **测试**：vitest **273 passed**（270+3：bookPose 确定性/范围/非全直立、渲染
  --bh/--tilt/--sw 确定性+范围、倾斜书仍可点击）；build 通过。
- **真实浏览器（真实库 63 册）**：5 视口全 `hOverflow=false`；书宽 21–44px、高
  202–218px（17 种）、16/63 本微倾（≤0.6°）、56 种微差色；hover 最左/最右/最窄/最宽
  （1440+390）均无截断；点击进「作品档案」、context menu、selectMode 63 标记+
  aria-pressed、网格 63 卡/列表 3 组 全正常；无 JS 错误。截图
  `bookshelf25_1920/1440/1024/768/390.png`。
- 下一步：待确认，不自动进入下一步（不自动进入 Bookshelf-3）。

### 2026-08-12 Bookshelf-2.6 纯视觉审计（零改动）

只读审计（git 干净）：确认 Bookshelf-2.5 结构成立，但共享架被 9 个 subgroup mono 标签
撑成 **954px / 73% 空 / 1440 内滚 302px / 390 内滚 892px**（标签合计 ~738px vs 书
~254px），读作"横向目录"而非"书架"。判定 B（基本成立，共享架需最小修正），推荐方案 C
（一条层板 + 轻量顶部索引）。截图沿用 bookshelf25_*；零代码修改。

### 2026-08-12 Bookshelf-2.7：共享架「轻量索引」最小修正（ADR 0081）

按 2.6 审计方案 C 实施，只改共享架：**标签不再逐组撑宽，书决定架宽**。

- **标签带移出 shelf-case**：9 个小组 mono 索引从"subgroup 内并排侧标签"改为共享架顶部
  一行**可换行的极轻索引带**（`.shelf-shared-index`，flex-wrap，9.5px mono/ink-2，非
  badge/pill/背景块）；`.shelf-subgroup` 保留（data-testid="shelf-group"）只包裹书，
  每本书仍独立 button、Tab 顺序不变。
- **书决定架宽**：标签带不再贡献 case 的 max-content → 共享架 954px → **345px**（≈
  剧场版 344 同宽级），不再是页面最宽匣。
- **实测（真实库 63 册）**：1440 共享架 345px / 空 **26%**（原 73%）/ 内滚 **0**（原
  302）；390 空 12% / 内滚 **31px**（原 892）；5 视口 `hOverflow=false`；大架 caseWidths
  [635,344,194,189,134,151] 与 2.5 **完全一致**（零视觉变化）；hover/点击进「作品档案」/
  context menu/selectMode 63 标记+aria-pressed/网格 63 卡/列表 3 组 全正常；无 JS 错误。
- **测试**：vitest **274 passed**（273+1：共享架标签移出 case / subgroup 只包裹书）；
  build 通过。截图 `bookshelf27_1920/1440/1024/768/390.png`。
- 下一步：Bookshelf-2.7 完成，Bookshelf 视觉重构建议收口，不自动进入 Bookshelf-3。

### 2026-08-12 Phase 8-0 现状审计（零改动）

只读全局审计（git 干净）：真实页面/组件映射 + 逐 section 浏览器测量。结论：Character
Archive 为**混合状态 D**（大圆角卡片墙 + pill/chip-card + 实心 accent，与已完成页面割裂）
→ 推荐 Phase 8-1；Settings（工具化·部分收敛）、Inspector（定义上整页 Dashboard 但低价值
工具）、Voice（工具化·基本一致）为 B；Ask/Summary/Memories 为 C。截图
`audit80_{characters,voice,inspector,settings,summary,memories,ask_empty}_1440.png` +
`audit80_settings_390.png`；零代码修改。

### 2026-08-12 Phase 8-1-A：Character Archive 主列表档案化收敛（ADR 0082）

只改 CharacterWall 主列表/详情呈现 + `.char-*` CSS + Character 测试；数据/API/交互全冻结。

- **主列表 → 档案索引条目**（`.char-entry`）：`borderRadius:16` → **`--radius-card`(6px)**；
  新增 mono 编目行 `№ 001`（`.char-entry-no`）、serif 人名（`.char-entry-name`）、quiet mono
  metadata（`.char-entry-meta` + `.char-entry-cv` 声优）——不再逐项 pill/chip；portrait 作档案
  小像 `--radius-cover`(4px)；无渐变/glow/实心 accent。
- **档案索引行**：mono `CHARACTER INDEX · N 位` + hairline。
- **选中详情**（`.char-detail`）：rounded-2xl → radius-card 档案卡；声优 pill → quiet mono
  链接（hover accent-soft）；作品白底 chip-card → hairline radius-card 档案行；文案/行为不变。
- **A11y**：全部 1605 个 button 显式 `type="button"`（Tab/Enter/Space 原生激活）；
  hover = surface-2、selected = accent hairline + accent-soft。
- **测试**：vitest **275 passed**（274+1：档案索引条目结构断言）；build 通过。
- **真实浏览器（真实库 1605 位角色）**：1920/1440/1024/768/390 全 `hOverflow=false`；
  entry radius 6px / portrait 4px / 人名 Georgia serif / 编目 mono；char-archive 内
  pills=0、rounded-2xl=0、accent-filled=0；点击开详情、作品行触发 onOpenWork、type="button"
  全齐。截图 `char81_1920/1440/1024/768/390.png`。
- 遗留：PageHeader「声优关系/刷新」仍为白字实心 accent（位于 DesktopView，严格范围外，
  待 8-1-B 或后续小改）；1605 位角色全量渲染页面较长（数据层既有，非本轮）。
- 下一步：等待确认，不自动进入 Phase 8-1-B。

### 2026-08-12 Phase 8-1-B：Character PageHeader 控件语言收敛（ADR 0083）

只收敛「声优关系」「刷新」两个 PageHeader 控件（P3）：SaaS 按钮（白字实心 accent /
rounded-xl）→ quiet control（transparent + hairline + `--radius-control`；hover=surface-2；
`:focus-visible` accent outline）。`DesktopView.jsx`（去 inline 实心 accent/rounded-xl、
改 `.character-archive-action`、补 type="button"；onClick 链不变）+ `index.css`（局部
类，无新 token）。功能/文案/位置全冻结。

- **测试**：vitest **275 passed**；build 通过。
- **真实浏览器（真实库）**：1920/1440/1024/768/390 全 `hOverflow=false`；header 按钮
  whiteOnAccent=0、pill=0、r2xl=0、rxl=0、radius **8px**、bg transparent、border hairline；
  `.char-entry` radius 6px 不变（8-1-A 未破坏）；「声优关系」点击进声优图谱 ✓、「刷新」
  重新拉取角色 ✓、真实 Tab 遍历正常；`:focus-visible` 规则按既有模式落地（离屏无法复现
  outline 渲染，需真实浏览器目检）。截图 `char81b_1440.png`。
- 下一步：等待确认，不自动进入 Phase 8-1-C。

### 2026-08-12 Phase 8-2-0 Voice 只读审计（零改动）

只读审计 Voice：结果 **PASS WITH NOTES**（无 P0/P1）。确认历史候选问题真实存在：
搜索框 pill(999px)、actor pill ×9、白字实心 accent 按钮 ×3（ego「收起」/概览 active/
阈值 active）、graph rounded-2xl 容器、概览红色警示框；**SVG 节点"鼠标可点但键盘不可达"
（64 g 无 tabindex/role/focus，实测 focusable=0）**——判 P2（倾向 P1 于键盘用户）。功能
无回归（搜索/ego/节点→书库连线 ✓）。截图 `voice820_*.png` + `voice820_ego_1440.png`；
零代码修改。

### 2026-08-12 Phase 8-2-A：Voice Graph 节点键盘可访问性（ADR 0084）

最小独立 a11y 小步：修 SVG 节点键盘可达/激活；Voice 视觉语言（search/pill/accent 按钮/
radius）冻结留待 8-2-B/C。

- **键盘语义**（VoiceGraphView.jsx）：ego 作品/共同出演声优 `g` + 角色 `circle`、overview
  作品/声优 `g` + 角色 `circle` 统一加 `role="button"` + `tabIndex={0}` + `aria-label`
  （复用现有 title 语义，title 保留）；**Enter/Space** 经 `activate(fn)` onKeyDown 触发
  （preventDefault 防滚动），与鼠标 click **共用同一业务函数**（onOpenWork / 抽取的
  selectActorByName，不复制业务）；中心声优圆（非交互）不加语义。
- **focus-visible 环**（index.css）：`svg g:focus-visible > circle` / `svg
  circle:focus-visible` → accent 描边 2.5px（无 glow/gradient/layout shift）+ outline:none。
- **测试**：vitest **281 passed**（275+6：语义/Enter/Space=与 click 同结果且不重复/Enter
  切邻域/不可交互元素无 role=button/mouse click 不变）；build 通过。
- **真实浏览器（真实库 ego 图）**：interactive=64 / focusable=64 / named=64；5 视口
  `hOverflow=false`；真实 Tab 可达节点；鼠标点击节点→书库 ✓；`:focus-visible` 环规则
  就位（离屏不激活 :focus-visible 匹配，需真实浏览器目检一次）。截图
  `voice82a_1920/1440/1024/768/390.png`。
- 下一步：等待确认，不自动进入 Phase 8-2-B。

### 2026-08-12 Phase 8-2-B：Voice Search & Controls 视觉语言收敛（ADR 0085）

把 Voice Search / Controls 从 pill / accent-filled 收敛为 quiet control / quiet link
（Voice 专属类，不跨页复用 `.character-*`）。

- **Search**：`rounded-full`(999px) → `.voice-search-control`（--radius-control 8px）；
  背景/边框/尺寸/icon/placeholder/行为不变。
- **actor 快捷入口**：accent-soft pill ×8 → `.voice-actor-link`（mono accent 文字 +
  radius-control + hover accent-soft，参照 8-1-A）；点击行为不变。
- **ego「收起」**：白字实心 accent → `.voice-graph-action`（transparent+hairline+radius-control）。
- **概览切换 / threshold**：active 白字实心 → `.voice-mode-control` / `.voice-threshold` +
  active 修饰（accent-soft+accent 文字，非白字实心）。全部按钮补 type="button"。
- **冻结**：graph 容器 rounded-2xl、overview 警示框、中心声优实心 accent 圆、Graph 节点
  结构与 8-2-A 键盘语义——全不动。
- **测试**：vitest **282 passed**（281+1 结构断言）；build 通过。
- **真实浏览器（真实库）**：5 视口 `hOverflow=false`；search 8px、actor link 8px（非
  pill）、whiteOnAccent **0**、overview active=accent-soft+accent（非白字）、threshold
  active 非白字；**8-2-A 回归：ego interactive=64 / focusable=64 不变**；功能正常。
  截图 `voice82b_1920/1440/1024/768/390.png`。
- 下一步：等待确认，不自动进入 Phase 8-2-C。

### 2026-08-12 Phase 8-3-0 Settings 只读审计（零改动）

只读审计 Settings：结果 **PASS WITH NOTES**（无 P0/P1）。确认 P2：分段 tab
`rounded-2xl`+`rounded-xl` active 白字实心、theme/style/density active 白字实心、action
按钮白字实心 accent——全 7 tab 合计 **24 处 whiteOnAccent**；P3：desk-askbar 卡 rounded-2xl
（跨页共享遗留）、nav ↑/↓ 小按钮、状态 pill（semantic 例外）。功能/a11y 正常（7 原生
button tab、无假开关、5 视口 hOverflow=false）。截图 `settings830_*.png`；零代码修改。

### 2026-08-12 Phase 8-3-A：Settings 控件语言收敛（ADR 0086）

只收敛 Settings 控件呈现；数据/表单/保存逻辑全冻结。

- **分段 tab**：rounded-2xl 容器 + rounded-xl active 白字实心 → `.settings-tabs` +
  `.settings-tab`/`-active`（hairline+radius-control；active=accent-soft+accent+accent 边框）。
- **选择项**（style theme/theme/density/ProviderSettings preset）：active 白字实心 →
  `.settings-option`/`-active`。
- **操作按钮**（保存/测试/保存凭证/连接/导入/新建数据源/创建/代理保存/编辑文字涂鸦/插件
  确认/导出/导入/**保存设置**）：白字实心（保存设置=rounded-full+shadow+scale）→
  `.settings-action`（transparent+hairline+radius-control+accent 文字；hover surface-2；
  disabled 保留；保存设置保留 fixed 定位）。全部补 type="button"；导航栏行 rounded-xl→lg。
- **保留**：desk-askbar 卡（跨页共享）、状态 pill、进度条、toast（semantic 例外）。
- **测试**：vitest **283 passed**（282+1 结构断言）；build 通过。
- **真实浏览器（真实库）**：**whiteOnAccent=0（7 tab 全 0，原 24）**；active tab =
  accent-soft+accent+accent 边框（8px）；保存设置=transparent+accent+8px（无
  shadow/rounded-full）；新建数据源 quiet + 表单正常展开；5 视口 `hOverflow=false`；tab
  切换正常。截图 `settings83a_1920/1440/1024/768/390.png`。
- 下一步：等待确认，不自动进入 Phase 8-3-B。

### 2026-08-12 Phase 9-0 全局残留审计（零改动）

只读全局审计：结果 **PASS WITH NOTES**。**全局 whiteOnAccent=0**（内联
`color:"#fff"` 仅存于未使用死 CSS `.tsm-accent`）；accent-filled 内联=0 组件；无
clickable 非原生控件；5 视口 hOverflow=false（7 section + Work Detail 实测）。残留：
Inspector 整页 Dashboard（鲜艳图表色+渐变进度条，P2 低价值）、**Voice 过滤搜索结果
accent-soft rounded-full pill（8-2-B 因缩进差异 replaceAll 漏改，P3）**、死 CSS
`.tsm-accent`（P3）、desk-askbar 16px 跨页遗留（P3）。semantic 例外：筛选 chip、状态
pill、tag、进度、toast、HomeShrine 环境光。零代码修改。

### 2026-08-12 Phase 9-1-A：Voice 过滤搜索遗漏 pill 收敛（ADR 0087）

只改 VoiceGraphView 过滤搜索结果的 actor 条目（Phase 9-0 发现的 8-2-B 遗漏项）：
accent-soft `rounded-full` pill → `.voice-actor-link`（radius-control + mono accent 文字 +
hover accent-soft + focus-visible accent outline）。功能/搜索/点击/ego 跳转全不变。

- **测试**：vitest **283 passed**；build 通过；VoiceGraphView 内 `rounded-full`=**0**。
- **真实浏览器（真实库，过滤搜索态）**：5 视口 `hOverflow=false`；过滤结果 actor link
  radius **8px**、非 pill、whiteOnAccent=0；8-2-A 回归：ego 图 interactive=focusable 保持。
  截图 `voice91a_1920/1440/1024/768/390.png`。
- 下一步：等待确认，不自动进入 Inspector。

### 2026-08-12 Phase 10-0 Product Value Audit（零改动）

只读产品价值审计：项目进入"功能完整 + 视觉统一"稳定期；核心价值「作品 × 我的经历」的
瓶颈在"我的记录不够多"。识别 6 个机会：收藏→回顾闭环（P1）、Ask 检索记忆感（P1，后端）、
角色数据规模化（P2）、Inspector 收敛（P2）、死 CSS/token（P3）。推荐 **P1-A 收藏→回顾
闭环**。零代码修改。

### 2026-08-12 Phase 10-1-A-0 Experience Loop 只读审计（零改动）

只读审计"导入→收藏→浏览→记录→回顾"路径：记录基础设施已齐（Work Detail 内联 composer/
里程碑/相遇纪事/MemoryTimeline/往年今日）；断点 = ① 收藏/导入成功后无引导进入记录；
② 浏览面（Grid/Bookshelf）无"已记录 vs 未记录"信号；③ 零记忆作品无空态引导。确认
ArchiveCard 的「─ 记录 N 条」是 chunk 数（非经历密度）。零代码修改。

### 2026-08-12 Phase 10-1-A-1：Work Detail 第一条记录引导（ADR 0088）

解决 Experience Loop 断点①/③：零记忆且零书评的作品，在「我的记忆」composer 上方显示
一行 quiet 引导（`.mc-empty-hint`，serif 12px ink-2），把"这里可以留下第一条记录"显性化。

- **改**：`ItemDetailPanel.jsx`（fetch 改 Promise.all + `memReady` 防空态闪烁；条件渲染
  hint）+ `index.css`（`.mc-empty-hint`）+ 测试 +2。Composer/情绪/附图/里程碑/Timeline/
  数据/API 全不动。
- **测试**：vitest **285 passed**（283+2：零记录显示引导且 composer 仍在、有书评不显示）；
  build 通过。
- **真实浏览器（真实库，遍历 8 部作品）**：3 部零记录作品显示引导、5 部有记录不显示；
  hint=serif 12px ink-2 quiet；hOverflow=false。截图 `mem101a1_1440.png`。
- 下一步：等待确认（可选扩展：收藏后引导 / 浏览面经历密度标记）。

### 2026-08-12 Phase 10-1-A-3：浏览面「我的记录」经历密度标记（ADR 0089）

解决 Experience Loop 断点②（浏览面无法提前知道哪些作品已有「我的经历」）。在浏览面
显示极轻量标记：有 Memory/Review 的作品显示 `§ N 条我的记录`，无记录保持干净。

- **数据**：不新增 API/字段——DesktopView 复用既有 `fetchAllReviews` + `GET /memories?limit=500`
  （简报惯例），前端 `recordCountOf` memo 建 per-item 计数映射，传入 ArchiveCard 与 Bookshelf。
- **Grid 卡片**：`recordCountOf(it)>0` → metadata 行追加 `§ {n} 条我的记录`（quiet mono，
  无 pill/badge/背景，不影响点击区）。
- **书脊**：`recordCountOf(it)>0` → 书脊顶部极轻 mono `§{n}`（aria-hidden 装饰；不覆盖书名/
  不改布局；窄书与移动端隐藏）。
- **测试**：vitest **289 passed**（285+4：Grid 有/无/默认不显示 + 点击保持；Bookshelf 有/无
  标记 + 点击保持）；build 通过。
- **真实浏览器（真实库 63 部）**：Grid 58/63 卡片显示标记、Bookshelf 58 本书脊显示 `§N`；
  5 视口 `hOverflow=false`；有记录可识别、无记录干净。截图 `mem101a3_1920/1440/1024/768/390.png`。
- 下一步：等待确认（可选：收藏后引导 / Bangumi 导入后记录引导 / Ask 记忆增强）。

### 2026-08-12 Phase 10-1-A-4：Bangumi 导入后第一条记录引导（ADR 0090）

解决 Experience Loop 断点④（批量导入完成后无下一步建议）。导入完成后增加 quiet next
step：「去记录第一条回忆」。

- **BangumiPanel**：完成态卡（`导入完成，共 N 部作品加入书库`）+ `.settings-action`
  「去记录第一条回忆」→ `onRecord(imported)`。导入 job 仅含计数（无 item_id 列表）→ 按
  任务回退方案导向 Library。
- **DesktopView**：onRecord → 书库 grid + `libRecordHint`；书架范围上方渲染 hairline+
  radius-control quiet 提示行（「选择一部作品，写下你的第一次记录」）+ 收起。
- **测试**：vitest **292 passed**（289+3：完成态显示入口并回调 imported=7、未导入不显示、
  DesktopView 集成全链路「导入完成→点击→书库提示→收起」）；build 通过。
- **真实浏览器**：默认书库无提示（回归）、Bangumi 面板正常、5 视口 `hOverflow=false`；
  真实 Bangumi OAuth 导入需真实账号（离线环境无法执行），完成态由集成测试验证。截图
  `mem101a4_1920/1440/1024/768/390.png`。
- 下一步：等待确认（可选：Ask 记忆增强 / Inspector 收敛 / 角色数据规模优化）。

### 2026-08-12 Phase 10-1-A-2：单条收藏后的记录引导（补齐审计首选 P1-1，ADR 0091）

**补齐审计报告首选范围中被跳过的 P1-1 项**：Ask 检索台**单条**收藏外部结果后，toast 附加
quiet 引导「去记录第一条回忆」→ 点击打开该作品详情并自动聚焦「我的记忆」composer 输入框。
先完成 `docs/audits/10-1-A-0-experience-loop.md` 完整审计原文存档。

- **toast 系统扩展 action**：`toast.success(msg, duration, action)`；ToastHost 渲染
  `.toast-action` quiet mono accent 按钮（transparent+hairline+radius-control，非 CTA）；
  点击执行并立即消失（dismissToast）。
- **ItemDetailPanel**：`composerFocusTick` prop + textarea callback ref（挂载即聚焦，
  含异步加载后）。
- **DesktopView**：`handleSave`（Ask 外部结果收藏）与详情弹层「收藏入库」均附引导 toast；
  点击 → `setDetailView({itemId, saved:true})` + `setComposerFocusTick(k+1)`。
- **测试**：vitest **296 passed**（292+4：toast action 渲染/点击消失/无 action 回归、
  ItemDetailPanel 聚焦、DesktopView 集成「Ask 收藏→toast→点击→composer 聚焦」）；build
  通过。
- **真实浏览器（真实 Ask 检索台，真实库）**：收藏「命运石之门」→ toast「去记录第一条回忆」
  → 点击 → 详情弹层 + composer 聚焦；hOverflow=false。截图 `mem101a2_1440.png`。
- 下一步：等待确认（可选：Ask 记忆增强 / Inspector 收敛 / 角色数据规模优化）。

### 2026-08-12 Phase 10-1-B-0 Ask 个人语境只读审计（零改动）

只读审计 Ask 是否真正利用 Memory/Review：**PASS WITH NOTES（无 P0/P1）**——记忆与书评
均已向量化入 Chroma（`memory{id}_chunk{i}` / `review{id}_chunk{i}`）并经 `retrieve_chunks`
进入 LLM 上下文（个人来源权重 1.0、external 0.4；来源标签 我的记忆/我的书评/我的笔记/
百科资料 已有）。P2：① 系统提示未把「记忆」列为个人来源类别；② 检索无时间/情绪/里程碑维度 +
top-5 窄上下文（「去年看了什么」「重温推荐」类弱）；③ per-item 去重可能挤掉个人 chunk。P3：
来源展示已含「我的记忆/我的书评」标签。零代码修改。

### 2026-08-12 Phase 10-1-B-1：Ask 个人语境提示词对齐（ADR 0092）

只改 `app/rag.py` 的 `SYSTEM_PROMPT_TEMPLATE` + 对应 pytest（检索架构/向量/权重/API/UI/
schema 全冻结）：

- 个人来源统一定义为 **我的记忆 / 我的书评 / 我的笔记**（原只列 笔记/书评）；
- 新增个人问题优先级规则：为什么喜欢/与我的关系/我的经历/去年/想重温/推荐我 → 优先
  记忆→书评→笔记；无个人记录时明说"没有找到你关于这一点的个人记录"，不臆造；
- 回答区分「你的记录中……」与「资料显示……」；
- **测试**：后端 pytest **430 passed**（cov 85.63%；test_rag 17，含新增 5 例：来源定义/优先级
  关键词+顺序+无记录回退/来源区分措辞/记忆·书评·百科 标签不回归）；前端 vitest **296 passed**；
  build 通过。
- 数据流无变化：Memory/Review 继续入 context（权重 1.0）、external 0.4 不变、来源标签不变、
  Ask UI 不变。
- 下一步：等待确认（可选：检索权重/时间维度/UI 来源增强）。

### 2026-08-10 【里程碑·整体复盘】P1~P8 渐进式重构完成

> 基于 PROJECT_AUDIT_REPORT 的 P1~P8 计划（Work/Collection/Memory 数据层 → Character 实体
> → Library 体验 → Search → RAG → 工程收尾），全部落地。把"二次元数据库工具"推进到
> "能承载作品 × 我的经历 × 记忆"的私人图书馆底座。

**八阶段与决策记录**：

| 阶段 | 内容 | ADR |
| --- | --- | --- |
| P1 | Work 世界轴列（work_type/原名/发行，幂等回填） | 0045 |
| P2 | Collection 收藏关系独立（1:1 collections 表）+ 收藏时刻 Memory | 0046 |
| P3 | Memory 扩展（轻量文字/里程碑/情绪/媒体附件 media 表） | 0047 |
| P4 | Character 角色实体化（characters + character_works 多对多） | 0048 |
| P5 | 真书架（层板线/分架/厚度来自数据/取书浮起） | 0049 |
| P6 | 检索台个人全文检索（统一世界/我的两入口） | 0050 |
| P7 | Personal RAG（Memory 内容进语义检索，标注"我的记忆"） | 0051 |
| P8 | 工程收尾（venv 瘦身/文档归档/Electron 评估） | 0052 |

**数据层变化（真实库回填，幂等，integrity ok）**：
- items 新增 work_type/alternative_title/release_date（anime 57 + game 2 等）；
- 新表：collections（61 行，看完 34/想看 23/未标 4）、characters（1605 行）+ character_works
  （1840 关联）、media（附件）、Memory 扩展（emotion 列 + text/milestone 来源）；
- Chunk 新增 memory_id（直接记忆向量 source_type=memory）。

**有意推翻的旧决策（均在新 ADR 说明理由）**：ADR 0016"角色不建表"→ 建 characters 实体表；
ADR 0019"书脊列表"→ 真书架布局（配色仍保留 0019）。

**接口不变 / 前端零改动点**：角色墙/声优图谱（0048 改走表，结构未变）；book shelf 视觉升级
不影响点击/hover/筛选。

**测试基线**：pytest **420 passed**（覆盖率 85.49%）；vitest **212 passed**；`npm run build` 通过；
累计 ADR 0001–0052。

**未来待办（未排期）**：Memory Composer（05 文档，含角色选择 UI）；检索台 L4（AI 记忆理解）；
Electron 打包 embedding 修复（三条路径，见 0052）；首页"最近记忆"与 Inspector/热力图工具感
收敛（P5 推迟项）；Series/Creator 实体化（无数据源）；Memory↔角色关联。

**一句话总结**：Tsumugi 数据层从"通用条目 + JSON 兜底"演进为 **Work/Collection/Memory/
Character 实体化 + 可检索**，体验层从"书脊列表/两套搜索"演进为**真书架 + 统一检索台 +
Memory 进 RAG**——一个能"收藏作品、记录相遇、多年后找回自己"的私人图书馆底座已成型。

### 2026-08-09 .gitignore：prompts/ 整体忽略（开发提示词/agent 配置不随仓库上传）

- `.gitignore` 末尾新增 `prompts/`：00-03 号开发提示词、`AGENTS.md`、`AGENT_UI.md`
  全部私有化，不再提交/上传（本地磁盘文件保留，仅 git 忽略，agent 指令仍正常加载）；
- `git rm --cached prompts/AGENTS.md prompts/AGENT_UI.md` 取消这两个已跟踪文件的追踪
  （文件保留在磁盘，`git check-ignore` 已确认全部命中）；
- 注意：历史提交里已包含过 prompts/AGENTS.md 与 AGENT_UI.md（此前为让 agent 引用而
  提交），若仓库已推送过，旧历史仍含这些文件；忽略只阻止后续上传。

### 2026-08-10 大规模重构检查点 + 第一阶段项目审查（PROJECT_AUDIT_REPORT）

- **检查点**：工作区已干净（HEAD = 13653cc），打 **git tag `pre-refactor-2026-08-10`**
  作为大规模重构前的回档节点（可 `git checkout pre-refactor-2026-08-10` 回退）。
- **产出 `PROJECT_AUDIT_REPORT.md`（只读，未改代码）**，要点：
  - 现状：FastAPI+React+Electron 底座，410 pytest / 198 vitest / build 全绿；
    Memory→时间轴→回廊→往年今日 情感核心已真实可用（Phase A/B/C/E）；
    43 份 ADR、四套主题、备份闭环、AI 已 Extension 化。
  - 主要差距（对照 14/07 设计文档）：Item 是通用条目非 Work（无作品类型/别名/发行/
    creator/series，世界轴字段埋在 raw_metadata JSON）；Character/Series/Creator 无表；
    Collection 未独立（状态散在 Review.status）；Memory 仅 review 来源、无 Media/emotion；
    搜索未统一（世界/我的两入口，Memory 无文本搜索）；书架非真书架；Electron 打包
    embedding 不可用（ADR 0012）。
  - 重构优先级：P0 稳定基线 → P1 Work 泛化（先出 WORK_MODEL_DESIGN）→ P2 Collection
    独立 → P3 Memory 扩展（Media）→ P4 Character/Series 实体化 → P5 Library 体验 →
    P6 Search Desk → P7 Personal RAG → P8 工程收尾。
  - 风险：数据迁移（63 条目+58 Memory）、范围蔓延、与 ADR 0016/0019 冲突（需新 ADR
    说明有意推翻）、prompts/ 私有化后需仓库外归档。
  - **等待人工确认后再进入下一阶段（渐进式重构）**。

### 2026-08-10 P1 设计阶段：WORK_MODEL_DESIGN.md（Work 模型泛化设计，未改代码）

依据 07/14 号设计文档 + 审查报告 P1，产出 `WORK_MODEL_DESIGN.md`（五部分：核心实体/
数据关系图/扩展策略/数据库设计建议/AI-RAG 方案），**不动数据层，待确认**。

- **核心决策**：统一 Work Core、**单表演进**——保留 `items` 表作作品容器，新增可查询
  世界轴列（`work_type`/`alternative_title`/`release_date`/`creator`/`series_name`），
  `type`(内容形态) 与 `work_type`(作品类型) 正交；**不建 works 表**，`raw_metadata`
  JSON 保留为兜底。
- **回填**：bangumi `metadata.type`→work_type（2动画/4游戏/1漫画/其余 other）、
  `original_name`→alternative_title、`date`→release_date；幂等（只填 NULL，不覆盖用户值）；
  moegirl/vndb 无类型信息留 NULL 由用户手选。
- **明确推迟**：Character/Series/Creator 实体表属 P4；creator/series_name 本轮仅落列
  待填。世界轴不额外向量化（简介已走 external_reference chunk）；work_type 进结构化筛选。
- **待确认点**：galgame 与 visual_novel 是否分开枚举；creator/series_name 是否本轮落列；
  work_type 手动编辑入口位置。

### 2026-08-10 P1 设计定案 + 设计报告归位（项目简报\Tsumugi\设计文档）

**三个决策点定案（用户确认 + 自判）**：
1. **枚举**：`galgame` 与 `visual_novel` **不分开**，合并为 `galgame`（涵盖视觉小说，
   社区常用说法，避免枚举碎片化）——work_type 最终为 `anime/manga/game/galgame/novel/other`；
2. **落列范围（自判）**：`creator`/`series_name` **本轮不落列**——无数据源填充（纯空列
   属提前设计，违背"避免过度设计"），且 P4 实体化时以 Character/Series/Creator 表 + FK
   承载，现在落列是被替换的一次性设计；P1 只落列有真实回填的 `work_type/alternative_title/
   release_date`；
3. **编辑入口（自判）**：work_type 手动编辑放**详情页"外部世界"区内联编辑**（作品档案
   就地修正最自然；设置页是"管理室"）。

**报告归位**：
- 设计/审查报告统一放 **`E:\program\MyProject\项目简报\Tsumugi\设计文档\`**（新建文件夹，
  与讨论简报同级，仓库外不受 git 控制，与 prompts/ 私有化保持一致）；
- `PROJECT_AUDIT_REPORT.md`、`WORK_MODEL_DESIGN.md` 已移入该目录并 `git rm --cached`
  （磁盘保留，仓库不再跟踪）。

### 2026-08-10 P1 实现：Work 模型世界轴列（ADR 0045，含真实库回填）

详见 docs/decisions/0045-work-model-world-axis.md。按 WORK_MODEL_DESIGN 定案实现，
只加列不建表、不推倒 Item。

- **models**：`items` 新增 `work_type`（anime/manga/game/galgame/novel/other，galgame 涵盖
  visual_novel）/ `alternative_title` / `release_date`；`type`（内容形态）与 `work_type`
  （作品类型）正交。creator/series_name 按定案不落列（P4 实体表）。
- **app/work_model.py（新增）**：`extract_work_columns`（bangumi type→work_type 2动画/4游戏/
  1漫画/其余 other、original_name→原名、date→发行）、`apply_work_columns`（入库即提炼，
  只填 NULL）、`backfill_work_columns`（启动时幂等回填，只补 NULL 不覆盖用户值）。
- **ensure_schema** 加 3 列；**main.py** 启动回填；`_save_external_sync` 入库即提炼。
- **接口**：`GET /items?work_type=` 筛选；`PATCH /items/{id}/work`（枚举校验、空串=清除、
  404 兜底）；ItemOut/ItemDetailOut 增 3 字段。
- **前端**：`itemInfoRows` 列优先显示 类型/原名/发行日期；详情页"外部世界"区**类型内联编辑**
  （select，外部作品显示）；图书馆视图**按类型筛选 chips**（有该类型数据才显示）；
  命令面板条目关键词含 work_type。
- 测试：pytest **419 passed**（+9 test_work_model：映射/变体/空源/回填幂等/不覆盖用户值/
  本地不动/加列/筛选详情/PATCH），覆盖率 87.06%；vitest **204 passed**（+6 itemInfoRows
  列优先/内联编辑 PATCH/本地无下拉/筛选 chips/命令面板）；build 通过。
- **实测（真实 App + headless Chrome CDP）**：启动回填后真实库 work_type = anime 57 +
  game 2 + 本地 4 null，原名填 59、发行填 58，integrity ok；详情页（空之境界）InfoTable
  显示 类型=动画/原名=劇場版 空の境界…/发行日期=2008-05-24，类型下拉可选 未分类/动画/漫画/
   游戏/Galgame/小说/其它；图书馆出现"全部类型/动画/游戏"筛选 chips。截图 p1_detail/
  p1_filter.png。无临时数据，未做数据回退（回填为期望的真实派生数据）。

### 2026-08-10 P2 实现：Collection 收藏关系独立（ADR 0046）

详见 docs/decisions/0046-collection-entity.md。把收藏状态从 Review 分离为独立
`collections` 表（我的轴独立于世界轴），收藏动作生成最轻 Collection Memory。

- **models**：`collections`（item_id 主键 FK、status、favorite、added_at）1:1 关联 items；
  Item 增 `collection` 关系（级联删除）。
- **app/collections.py（新增）**：`ensure_collection`（1:1 建行）、`set_collection`（枚举
  校验/空串清除/favorite）、`ensure_collection_memory`（source_type=collection，"这一天，
  我把它带回了图书馆"，幂等）、`on_collect`（首次收藏建行+生成收藏时刻 Memory，re-save
  不重复）、`backfill_collections`（启动回填，status 从"从 Bangumi 导入"Review 迁移，
  **不为历史批量造收藏 Memory** 避免污染回廊）。
- **接口**：`PATCH /items/{id}/collection`、`GET /collections`（item_id→status，前端
  statusMap）、`GET /items?collection_status=` 筛选；ItemOut/ItemDetailOut 增
  collection_status/collected_at/favorite；列表 `selectinload` 避免 N+1。
- **状态源迁移**：前端 statusMap 改读 `/collections`（不再从书评取状态）；Bangumi 导入
  同时写 collection.status（Review 保留书评/评分职责，状态书评不删）。
- **前端**：详情页"我的记录"区收藏状态下拉 + 喜欢 toggle + 收藏时间展示（内联编辑 PATCH）。
- 测试：pytest **426 passed**（+7 test_collections：建表迁移/回填迁移 status 幂等不造
  收藏 Memory/本地不动/on_collect 首次建行+Memory/重复不造/set 校验/PATCH+筛选+detail/
  collections 映射），覆盖率 87.27%；vitest **206 passed**（+2 我的记录收藏编辑 PATCH、
  本地无收藏编辑区）；build 通过。
- **实测（真实 App + CDP）**：启动回填真实库 collections 61 条（看完 34 + 想看 23 +
  未标 4），收藏 Memory 0（正确）；详情页"我的记录"显示 收藏状态=看完 + ♡ 喜欢 toggle +
  收藏于 2026-08-07；状态分组列表变为 想看23/看完34/未收藏6（基于 collection 分组）。
  截图 p2_detail/p2_group.png。无临时数据，未回退。

### 2026-08-10 P3 实现：Memory 扩展（轻量文字/里程碑/情绪/媒体附件，ADR 0047）

详见 docs/decisions/0047-memory-extension.md。Memory 从"只能由书评生成"扩展为可
**直接创建**：一句话轻量记录、里程碑（完成/重新打开）、可选情绪、可选截图/插图。

- **models**：`Media` 表（item_id/memory_id/file_path/media_type/size）+ `Memory.media`
  关系（级联删除）；`Memory.emotion` 列（ensure_schema 加列）。
- **memories.py**：`create_direct_memory`（source_type ∈ text/milestone、summary 即正文、
  occurred_at=现在、source_ref=NULL）、`delete_direct_memory`（仅 text/milestone 可删，
  review/collection 不允许误删）。
- **接口**：`POST /items/{id}/memories`（multipart：summary/source_type/emotion/file，
  文件存 data/uploads，/static/uploads 服务）；`DELETE /memories/{id}`；`MemoryOut` 增
  emotion 与 media[{id,url,media_type}]。
- **前端**：`MemoryReviewModal` 泛化（review 拉书评全文；直接记忆展示 summary+情绪+媒体图）；
  时间轴条目显示**类型徽标**（书评/记录/里程碑/收藏）+ **情绪标签** + **媒体缩略图** +
  直接记忆可删除；详情页"我的记录"区**轻量记录 composer**（输入 + 情绪下拉 + 附图按钮 +
  记录）+ 里程碑快捷按钮（✓完成了 / ↺重新打开），创建后刷新时间轴。
- 测试：pytest **433 passed**（+7：text/milestone 创建、非法校验、delete 仅 direct、API
  含 emotion+media 落库、列表含 emotion），覆盖率 87.47%；vitest **207 passed**（+1 text
  记忆徽标/情绪/缩略图/弹层/删除）；build 通过。
- **实测（真实 App + CDP）**：对真实作品（空之境界 item3）创建 text（情绪=怀念 + 上传
  截图）与 milestone（情绪=满足）→ 时间轴完整显示 3 条（书评/记录·怀念/里程碑·满足），
  text 带媒体缩略图、可直接删除。截图 p3_timeline.png。测试记忆已删除（media 级联清理 +
  上传文件移除），库回 58 条真实 Memory，integrity ok。

### 2026-08-10 P4 实现：Character 角色实体化（ADR 0048，含真实库回填）

详见 docs/decisions/0048-character-entity.md。把角色从 raw_metadata 内嵌提炼为独立实体表
（**有意推翻 ADR 0016 的"角色不建表"部分**），角色墙/声优图谱改从表读取，前端零改动。

- **models**：`characters`（source/external_id/name/image_url/relation/summary/actors[JSON]）+
  `character_works`（character_id+item_id 多对多）；Item.characters 关系。
- **app/characters.py（新增）**：`extract_characters`（从 raw_metadata）、`sync_characters`
  （重建单作品索引：断旧链接→upsert→建链接→删孤儿；去重键 (source,external_id)|(source,
  name)；relation 主角优先合并）、`backfill_characters`（启动时全量幂等重建）。
- **接入**：`_save_external_sync` 收藏入库后 sync；main.py 启动回填；`GET /characters` 与
  `GET /voice-relations` 改为查表（selectinload works），返回结构与前端一致（零改动）。
- **明确推迟**：Series/Creator（无数据源，建空表属提前设计）；Memory↔角色（表已就绪，
  待 Memory Composer 05 排期时连同角色选择 UI 一起）。
- 测试：pytest **416 passed**（重写 test_characters 4 例：去重/主角优先/跨作品 works/重建
  删旧链接删孤儿/回填/API 走表；test_voice_relations 适配 5 例）；vitest **207 passed**
  （前端零改动）；build 通过。
- **实测（真实 App + CDP）**：启动回填真实库 **1605 角色 / 1840 关联**；GET /characters
  1605、voice 965 声优/56 作品/170 缺失声优角色；角色墙从表渲染正常（1570 张角色图）。
  截图 p4_wall.png。无临时数据，integrity ok。

### 2026-08-10 P5 实现：真书架（ADR 0049，对 ADR 0019 布局的有意推翻）

详见 docs/decisions/0049-real-bookshelf.md。书架从"书脊列表"升级为真书架（纯前端，
后端零改动）。

- **保留 ADR 0019**：书脊配色仍按主标签哈希（同标签相近色相）、ADR 0037 的 CoverAmbient
  hover 取色联动。
- **推翻/新增（布局→空间）**：**层板线**（每层书下方书架板，--surface-1/2 渐变+下边框，
  书"站"在层板上）；**按主标签分架**（`groupBookshelf`：每层一个分类标签+册数，未分类
  排最后、数量多的靠前）；**书脊厚度来自数据**（`spineThickness` = max(正文长度, chunk 数)
  → 9~28px 钳制，"书有多厚"）；**hover 取书浮起**（`.shelf-book:hover` translateY(-5px) +
  保留抽书预览）。
- **推迟**：首页"最近记忆"（现有首页经猫娘/往年今日已呈现记忆，再加条有违神殿极简）；
  "去除工具感"（Inspector/热力图收敛 = 持续 UI 纪律）。
- 测试：vitest **210 passed**（+3 spineThickness 钳制与 chunk 兜底、groupBookshelf 分架
  排序、真书架分架渲染+层板线+浮起类）；原 14 例 bookshelf 测试全兼容。build 通过。
- **实测（真实 App + CDP）**：书架视图按主标签分 15 层（TV 22 / 剧场版 11 / 空之境界 6 /
  原创 6 / 热血 4…）+ 15 条层板线；书脊厚度 10~28px 有体积差异。  截图 p5_shelf.png。

### 2026-08-10 P6 实现：检索台 · 个人全文检索（ADR 0050）

详见 docs/decisions/0050-search-desk.md。问答检索台升级为**统一检索台**：提交查询后
同时展示 我的检索（个人全文匹配）+ 外部检索（已有）+ AI 回答（已有）——兑现 v1.1
修正二标注的目标状态（世界/我的两套入口合并）。

- **后端**：`GET /api/search/my?q=`（Item 标题/内容、Review 标题/内容、Memory.summary
  的 SQLite LIKE 子串匹配，返回 works/reviews/memories 三组；RAG 语义检索属 Phase G）；
  `/memories` 与 `query_memories` 增 `search` 参数（记忆回廊可按文本筛）。
- **前端**：问答提交并行调 `searchMy`，渲染"我的检索 ·「q」"块（作品→详情、书评/记忆→
  只读弹层复用 MemoryReviewModal）；`hasContent` 扩展为"提交过检索即切换内容分支"
  （无命中显示"没有匹配的我的记录"）。
- **明确不做**：L4 个人记忆理解（AI）、个人搜索接 RAG 语义（Phase G）、检索台结构化语法
  （work_type/collection_status 已覆盖）。
- 测试：pytest **418 passed**（+2 search/my 三组/空查询/无命中、memories?search）；vitest
  **212 passed**（+2 我的检索展示作品/书评/记忆、无命中空提示）；build 通过。
- **实测（真实 App + CDP）**：检索"空之境界"命中 6 个作品；"Bangumi"命中书评+记忆多组；
  检索台「我的检索 · Bangumi」渲染 书评/记忆 分组 + AI 回答并行流式。截图 p6_search.png。

### 2026-08-10 P7 实现：Personal RAG · Memory 进入个人语义检索（ADR 0051）

详见 docs/decisions/0051-personal-rag-memory.md。直接 Memory（轻量文字/里程碑）的文本
进入**个人语义检索**：创建时切分 summary → embedding → Chroma（`source_type=memory`），
RAG 引用标注"（我的记忆）"，与 我的书评/我的笔记/百科资料 并列区分。

- **延续 ADR 0041**：不把 Memory 表整个向量化，只把直接 Memory 的 summary 文本作为
  "素材"切分建 chunk（`Chunk.memory_id` 关联）；review 类记忆素材是底层 Review（已参与
  RAG），不重复建。
- **生命周期同 review 向量模式**：创建写向量（失败回滚+清理）、删除删向量与 chunk、
  启动 `backfill_memory_vectors` 幂等补齐历史直接记忆。
- **来源区分**：`source_type=memory` 权重 1.0（用户内容，与 note/review 同级，非外部百科
  0.4）；rag.py 标注"（我的记忆）"；前端来源角标"我的记忆"。
- **models**：Chunk.memory_id + Memory.chunks（cascade）；ensure_schema 加列。
- 测试：pytest **420 passed**（+2：直接 Memory 建向量可检索、删除清向量与 chunk、backfill
  幂等）；vitest **212 passed**；build 通过。
- **实测（真实 App）**：对真实作品创建一条轻量记忆（"夜里又想起孤独这个话题…"）→ Chunk
  落库 source_type=memory；RAG 问答"我夜里关于孤独写过什么？"→ 回答引用该记忆，来源标注
  **「（我的记忆）」**（区别于百科）。测试记忆已删除（chunk/向量级联清理），库回 58 条，
  integrity ok。
